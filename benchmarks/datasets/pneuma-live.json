{
  "name": "pneuma-live",
  "description": "Snapshot of Pneuma's own documentation (README, REST_API, MCP_API, TELEMETRY, CKG_IMPROVEMENTS, CLAUDE, CHANGELOG, PARTIO_REMOVAL_PLAN, docker/PORTS, archive/TEST_PLAN) at commit 3d64869 on 2026-09-24, split at H2 boundaries into 62 verbatim markdown documents (small adjacent sections merged, oversized sections split at H3 or bullet boundaries; README HTML badges/images stripped), with 130 hand-written questions: paraphrase 40, lexical 20, multi 15, detail 15, filter 10, negative 20, confusable 10. Every answerable question carries a short gold answer and verbatim evidence spans checked by script against its relevant documents; negatives were checked by term search. Relevance labels are good but not gold: other documents may partially answer some questions (the corpus repeats facts across CHANGELOG, README, and the API docs).",
  "corpora": [
    {
      "id": "pneuma",
      "categories": [
        {
          "name": "readme",
          "description": "README.md: product overview, features, Docker quick start, pipeline flow, API/MCP overview, architecture, builds, SDKs."
        },
        {
          "name": "rest-api",
          "description": "REST_API.md: REST endpoint reference — auth, pagination, tenancy/RBAC, subjects, prompts, history, ingestion, model runners, settings, search/ask."
        },
        {
          "name": "mcp-api",
          "description": "MCP_API.md: the in-process MCP (JSON-RPC) endpoint — auth, methods, paging protocol, tool catalog, metadataFilter, streaming."
        },
        {
          "name": "telemetry",
          "description": "TELEMETRY.md: Prometheus metrics inventory, OTLP/Tempo traces, collection, Grafana dashboards, and troubleshooting workflows."
        },
        {
          "name": "ckg-improvements",
          "description": "CKG_IMPROVEMENTS.md: scored roadmap of ingestion/retrieval improvements with implementation notes (RRF, GraphRAG, MMR, caching, ...)."
        },
        {
          "name": "claude",
          "description": "CLAUDE.md: contributor and agent guide — C# code style, backend architecture, auth model, frontend rules, integration contracts, build/test."
        },
        {
          "name": "changelog",
          "description": "CHANGELOG.md: Keep-a-Changelog history of the Unreleased work and the 0.1.0 release (added/changed/fixed/removed)."
        },
        {
          "name": "partio-removal-plan",
          "description": "PARTIO_REMOVAL_PLAN.md: plan to replace the external Partio service with native chunking, embedding, summarization, and an endpoint registry."
        },
        {
          "name": "ports",
          "description": "docker/PORTS.md: host ports and default credentials for the dashboards and services in the Docker stack."
        },
        {
          "name": "test-plan",
          "description": "archive/TEST_PLAN.md: archived assessment of automated-test coverage, gaps by layer, and a phased plan to extend it."
        }
      ],
      "documents": [
        {
          "id": "readme--overview",
          "category": "readme",
          "title": "README — Overview / Who Pneuma is for",
          "summary": "What Pneuma is (self-hosted knowledge-graph platform), its alpha status, and the three audiences it targets.",
          "body": "# Pneuma — breathing life into your information\n\n**v0.1.0 · ALPHA**\n\n**Pneuma** is a self-hostable **knowledge-graph data platform**. Point it at a body of source material — documents, web pages, and other artifacts about any kind of subject — and it ingests each one, extracts its semantic content, classifies it into an editable **ontology**, merges it into a **knowledge graph**, and indexes it for grounded, cited retrieval. You get a queryable graph of entities and relationships, a hybrid (lexical + semantic) search and question-answering layer that always cites its sources, and full provenance, rights, and multi-tenant access control — exposed through a REST API, an in-process **Model Context Protocol (MCP)** server, and three operator dashboards.\n\nPneuma ships as a fully orchestrated **Docker Compose** stack: one command brings up the platform, the knowledge graph, the vector + full-text store, the document-processing pipeline, a local LLM runner, object storage, and the observability stack. It is the **intelligence layer** you build on — not a finished end-user product.\n\n> **This is alpha software.** Everything in Pneuma — APIs, database schemas, configuration, dashboards, and defaults — is subject to change without notice while the project is in its `0.x` series. Pneuma will adopt [semantic versioning](https://semver.org/) at its stable **1.0** release; until then, treat every build as a moving target and pin the exact version you deploy.\n\n## Who Pneuma is for\n\n- **Data engineers & data users** — turn a pile of unstructured documents and pages into a **structured, queryable knowledge graph** with typed entities, relationships, provenance, and rights — without hand-rolling an extraction pipeline. Explore it visually, search it, and pull it into downstream systems over a clean REST API.\n- **AI engineers** — get a **production-grade RAG backend** out of the box: hybrid retrieval, graph-neighbor expansion, prompt rewrite, optional LLM re-ranking, conversation compaction, cited answers with an explicit \"insufficient support\" refusal, a built-in LLM-judged evaluation harness, and a first-class **MCP** surface so agents drive the platform through the same auth and audit path as everything else. Bring your own model — OpenAI, Azure OpenAI, OpenAI-compatible, Gemini, Vertex AI, Anthropic, Bedrock, Voyage AI, or fully offline **Ollama**.\n- **Software developers** — a self-hostable platform with an **OpenAPI-described REST API**, C#/JS/Python SDKs, multi-tenant **RBAC** with a full audit trail, request-history capture, Prometheus metrics + OpenTelemetry traces, and a provider-neutral data layer that runs on PostgreSQL, SQLite, MySQL, or SQL Server. One `docker compose up` and you're building.",
          "format": "md",
          "labels": [
            "readme"
          ],
          "tags": {}
        },
        {
          "id": "readme--screenshots",
          "category": "readme",
          "title": "README — Screenshots",
          "summary": "Tour of the admin dashboard: overview tiles, per-subject settings modal, links/ingestion view, and the grounded Ask view.",
          "body": "## Screenshots\n\n**Overview.** The admin home surfaces the current state of a deployment — tenants, users, subjects, links, and queued/failed ingestion jobs — above a live request-activity chart (success vs. failed) with selectable time ranges.\n\n**Per-subject configuration.** Each subject has its own settings: display name and URL slug, embedding/inference models (and optional reranking/prompt-rewrite models), a fixed-dimensionality collection, a chat-history retention window, a show-thinking toggle, and per-subject prompt overrides (system, reranking, prompt-rewrite, and ontology classification/definition) that layer on top of the global defaults.\n\n**Ingestion & links.** The Links view lists every content link and its ingestion status (Ingested / Failed, with the last error), filterable by subject and paginated like every other table — each row is a source that flowed through the document → graph → search pipeline.\n\n**Grounded Q&A.** The Ask surface answers questions from the curated corpus and shows its work: an expandable tool-call trace (each `pneuma_search` / `pneuma_get_node` call with timing) above a grounded, Markdown-formatted answer, with the conversation switcher for managing threads.",
          "format": "md",
          "labels": [
            "readme"
          ],
          "tags": {}
        },
        {
          "id": "readme--features",
          "category": "readme",
          "title": "README — Features",
          "summary": "Feature list: two-phase ingestion, editable ontology, graph, hybrid retrieval, facets, conversations, eval, analytics, MCP, RBAC, providers, observability.",
          "body": "## Features\n\n- **Ingestion pipeline** — Submit a document or URL and Pneuma runs it through an explicit **Categorize** phase (fetch → type-detect → extract semantic cells → classify into the ontology) and a **Hydrate** phase (merge into the graph, chunk, embed, and index), with per-stage live logs, prompt-provenance capture for reproducibility, and best-effort artifact storage (source, atoms, chunks, vectors, subgraph) in S3. Chunking (in the **`Pneuma.Chunking`** library), embedding, and summarization all run in-process — no external processing service.\n- **Editable, natural-language ontology** — The ontology that drives classification is a **prompt you can rewrite** — reshape the graph's node and edge types (Subject, Person, Organization, Work, Collection, Event, Place, Topic, …) without touching code. The graph merge accepts whatever types your ontology defines.\n- **Knowledge graph** — Every source becomes provenance-anchored nodes and edges in **LiteGraph**, with entity resolution (canonical-name dedup), per-tenant isolation, and a node explorer for contents, links, adjacent nodes, relationships, rights, and authority.\n- **Hybrid retrieval & grounded answers** — Blends lexical (full-text) and semantic (vector) search over **RecallDB** (Postgres + pgvector) with optional **graph-neighbor expansion**, optional **prompt rewrite** and **LLM re-ranking**, and returns a cited answer or an explicit refusal. One shared service backs both the REST and MCP answer paths.\n- **Metadata / facet scoping** — Ingest links with **labels** and **tags** that are stamped onto every chunk and graph node; then scope any search or answer to those facets with a per-request `metadataFilter` (required/excluded labels + tag conditions) merged with a subject's default filter. Distinct labels/tags are discoverable over REST and MCP.\n- **Conversations** — Chat turns are grouped into named, rehydratable **threads** (auto-titled from the conversation), with an agentic tool-call trace, automatic conversation compaction near the context window, per-turn timing/telemetry, and 👍/👎 feedback. Reopen, rename, and delete conversations from a management table on every dashboard.\n- **Per-subject configuration** — Each **subject** carries its own models (embedding/inference, optional reranking/prompt-rewrite), collection, retention window, thinking-visibility toggle, URL slug, and prompt overrides that layer on the global defaults.\n- **RAG evaluation harness** — Define ground-truth facts per subject and run **LLM-judged** evaluation passes over the real answer pipeline: async, cancellable runs with live SSE progress and per-fact verdict / score / reason / failure-mode results.\n- **Analytics** — Per-subject rollups: turn volume, latency percentiles (p50/p95/p99), per-stage timing, throughput, and feedback trends, rendered with hand-rolled SVG charts.\n- **Model Context Protocol (MCP)** — An in-process MCP endpoint (`POST /mcp`, JSON-RPC 2.0) exposes ~35 read/management tools — search, graph traversal, grounded query (with `metadataFilter`), subjects, jobs, links, threads, history, feedback, analytics, eval, request history, settings, and model-endpoint health — each **RBAC-mapped and audited** exactly like its REST twin, with bounded, paged enumerations.\n- **Multi-tenant RBAC, fully audited** — Accounts, tenants, admins, users, credentials (email/password sessions or `access_`/`secret_` API keys), roles, permissions, and assignments, with explicit-deny > permit > implicit-deny evaluation and **every decision — including admin bypasses — persisted to the audit stream**.\n- **Bring your own model** — LLM access goes through a provider-neutral layer (**PolyPrompt**). Model endpoints are managed natively by Pneuma and cover all nine providers for embeddings and completions: **OpenAI, OpenAI-compatible, Gemini, Ollama, Azure OpenAI, Anthropic** (completions only), **Bedrock, Voyage AI** (embeddings only), and **Vertex AI** — so you can run **fully offline** on Ollama or plug in a hosted model with a key. Endpoints take a `provider` plus provider-specific fields (deployment, API version, region, project, access-key id) and write-only secrets.\n- **Observability** — Prometheus metrics for HTTP, ingestion, integrations, and the retrieval/answer path; OpenTelemetry/Tempo traces with per-stage spans; and provisioned, domain-sectioned **Grafana** dashboards. See [`TELEMETRY.md`](TELEMETRY.md).\n- **Operator surfaces** — Three React dashboards (admin, subject, user) with Home, Request History (+ inspector), an OpenAPI-driven API Explorer, Settings, History, Feedback, Analytics, Evaluation, and a grounded **Ask** experience — light/dark themes, responsive, i18n-ready.\n- **Provider-neutral persistence** — A handwritten, provider-neutral data layer runs on **PostgreSQL**, SQLite, MySQL, or SQL Server, with versioned, idempotent, tracked migrations and idempotent first-boot seeding.",
          "format": "md",
          "labels": [
            "readme"
          ],
          "tags": {}
        },
        {
          "id": "readme--quick-start",
          "category": "readme",
          "title": "README — Quick start (Docker)",
          "summary": "Running the Docker Compose stack: services and ports, dashboard logins, configuration file, factory reset, and upgrading from Partio.",
          "body": "## Quick start (Docker)\n\n> **Docker only.** The supported way to run Pneuma is the Docker Compose stack. You do **not** need the .NET SDK, Node, or any language toolchain to run it — only Docker and Docker Compose.\n\n```bash\ngit clone https://github.com/jchristn/Pneuma.git\ncd Pneuma/docker\ndocker compose up -d\n```\n\nThe stack comes up with a seeded administrator, built-in RBAC roles, a default API key, and default model runners (Ollama active for offline use; OpenAI/Gemini activate when you supply keys). Once the services are healthy, open the **Admin dashboard** at [http://localhost:3010](http://localhost:3010) and sign in with `admin@pneuma` / `password`.\n\n> **Change every default credential before exposing any service beyond your machine.** Override the seeded admin with `PNEUMA_ADMIN_EMAIL` / `PNEUMA_ADMIN_PASSWORD` (see `docker/pneuma.json`).\n\n### Services\n\nThe Docker Compose stack orchestrates the following. Pneuma's own images are published as `jchristn77/pneuma-*`; the remaining services are pinned third-party images.\n\n| Service | Port | Description |\n|---|---|---|\n| **pneuma-server** | 8080 | Core REST API + in-process MCP server (C# on Watson 7.1). Orchestrates ingestion, hosts the API, enforces RBAC, and emits metrics/traces. OpenAPI at `/openapi.json`; admin API key `pneumaadmin`. |\n| **admin-dashboard** | 3010 | Full operator dashboard (React/Vite, nginx): tenants, users, RBAC, subjects, links, ingestion, collections, model runners, prompts, history, feedback, analytics, eval, settings. |\n| **subject-dashboard** | 3011 | Creator dashboard: manage subjects, submit links, follow ingestion, ask, review history/feedback/analytics, run evals. |\n| **user-dashboard** | 3012 | Consumer dashboard: browse subjects and use the grounded **Ask** experience. |\n| **pneuma-postgres** | 15432 | PostgreSQL 17 with **pgvector** installed. Backs Pneuma, RecallDB, LiteGraph, and Less3. |\n| **litegraph** / litegraph-ui | 8701 / 3001 | Knowledge-graph store (nodes, edges, labels, tags) and its management UI. |\n| **documentatom** / documentatom-ui | 8000 / 3002 | Document type detection + semantic cell extraction, and its UI. |\n| **recalldb-server** / recalldb-dashboard | 8600 / 8601 | Vector + full-text retrieval store (Postgres/pgvector) and its UI. |\n| **ollama** | 11434 | Local LLM inference engine (embeddings + completions) for fully offline operation. |\n| **less3** | — | S3-compatible object storage for pipeline artifacts (source, atoms, chunks, vectors, subgraph). |\n| **prometheus** / **tempo** / **grafana** | 9090 / 3200 / 3000 | Metrics, traces, and provisioned dashboards. |\n\nThe full port + default-credential list is in [`docker/PORTS.md`](docker/PORTS.md).\n\n### Dashboards\n\n| Dashboard | URL | Default login |\n|---|---|---|\n| Pneuma Admin | http://localhost:3010 | `admin@pneuma` / `password` |\n| Pneuma Subject | http://localhost:3011 | `admin@pneuma` / `password` |\n| Pneuma User | http://localhost:3012 | `admin@pneuma` / `password` |\n| Grafana | http://localhost:3000 | `admin` / `admin` |\n\n### Configuration\n\nConfiguration is a mounted `docker/pneuma.json`: web server, CORS, logging, database, auth, request-history capture, ingestion concurrency, integration endpoints (DocumentAtom, RecallDB, LiteGraph, S3), retrieval tuning, model-runner gate, telemetry, and first-boot seeding. Secrets can be overridden with `PNEUMA_*` environment variables. Most settings are also editable at runtime from the admin **Settings** page (`GET/PUT /v1.0/settings`), with secrets masked on read; changes that require a restart are annotated.\n\n### Factory reset\n\nTo wipe the stack back to factory defaults (fresh databases, seeded admin, and starter configuration), use `docker/reset.bat` (Windows). To pull the latest published images and restart, use `docker/update.bat`.\n\n### Upgrading from a Partio-based build\n\nPneuma now removes Partio and manages model endpoints natively. Because Partio no longer supplies embedding/completion endpoints, on upgrade:\n\n1. Pull the new images and restart (`docker/update.bat`, or `docker compose pull && docker compose up -d`). Schema **migrations apply automatically** on first boot.\n2. **Re-create your model endpoints** from the admin **Model Endpoints** page — pick a `provider`, fill in its provider-specific fields, and supply the write-only secrets. Default endpoints are seeded (Ollama), so an offline stack works out of the box.\n3. **Re-select each subject's embedding and inference models** (and any optional reranking / prompt-rewrite models) so ingestion and answering point at the new endpoints.",
          "format": "md",
          "labels": [
            "readme"
          ],
          "tags": {}
        },
        {
          "id": "readme--how-it-works",
          "category": "readme",
          "title": "README — How it works",
          "summary": "Diagrams of the six-step ingestion flow and the question-answering flow with optional rewrite, expansion, and re-ranking.",
          "body": "## How it works\n\nIngestion runs as two explicit phases with per-stage live logs:\n\n```\nSubject submits a link  →  queued ingestion job  →  worker pool\n  1. DocumentAtom   type detection            (unknown type → fail)\n  2. DocumentAtom   semantic cell extraction\n  3. PolyPrompt     classify cells → candidate subgraph (your ontology)\n  4. LiteGraph      merge subgraph → Cell + entity nodes, record node/edge IDs\n  5. Pneuma         summarize + chunk (Pneuma.Chunking) + embed, all in-process\n  6. RecallDB       store chunk text + vectors, each linked back to its graph node\n```\n\nRetrieval and answering blend lexical and semantic search with optional graph expansion:\n\n```\nAn app or agent asks a question\n  → (optional) prompt rewrite grounded in the subject\n  → hybrid retrieval over RecallDB (vector + full-text) honoring any metadataFilter\n  → optional graph-neighbor expansion over LiteGraph\n  → optional LLM re-ranking\n  → grounded, cited answer  (REST /v1.0/query[/stream] or the MCP pneuma_query tool)\n    — or an explicit \"insufficient support\" refusal\n```\n\nEvery chat turn is persisted with its full timing/metadata, grouped into named conversation threads, and available in the History/Feedback/Analytics views and to the evaluation harness.",
          "format": "md",
          "labels": [
            "readme"
          ],
          "tags": {}
        },
        {
          "id": "readme--api-and-mcp-overview",
          "category": "readme",
          "title": "README — API overview / MCP server",
          "summary": "Table of REST endpoint categories plus a summary of the MCP server's tool groups and paging.",
          "body": "## API overview\n\nPneuma exposes a versioned REST API at `/v1.0/`. Authenticated endpoints take a bearer token (an opaque session token, or an `access_`/`secret_` API key) in the `Authorization` header. Full request/response schemas are in [`REST_API.md`](REST_API.md), and the running server serves OpenAPI at `/openapi.json` (also browsable from each dashboard's **API Explorer**).\n\n| Category | Representative endpoints | Description |\n|---|---|---|\n| Health / metrics | `GET /`, `GET /v1.0/api/health`, `GET /metrics` | Server info, health, Prometheus metrics |\n| Auth & sessions | `POST /v1.0/token`, `POST /v1.0/token/refresh`, `GET/DELETE /v1.0/token` | Login, refresh (rotates), inspect, revoke |\n| Tenancy & RBAC | `/v1.0/{tenants,users,credentials,roles,permissions,assignments}` | CRUD for the multi-tenant RBAC model |\n| Audit | `GET /v1.0/audit` | The authorization/authentication audit stream |\n| Subjects | `/v1.0/subjects`, `GET /v1.0/subjects/by-slug/{slug}` | Subject CRUD + slug resolution (delete cascades in the background) |\n| Links & ingestion | `/v1.0/subjects/{id}/links[/bulk]`, `/v1.0/jobs`, `GET /v1.0/links/{id}/{source,atoms,chunks,vectors,subgraph,log}` | Submit content, drive jobs, inspect per-stage artifacts |\n| Collections | `GET/PUT/DELETE /v1.0/collections` | RecallDB collection management (proxied) |\n| Search & graph | `GET /v1.0/search`, `GET /v1.0/subjects/{id}/search`, `GET /v1.0/graph/nodes/{id}[/edges,/neighbors]` | Full-text search and graph traversal |\n| Grounded Q&A | `POST /v1.0/query`, `POST /v1.0/query/stream`, `POST /v1.0/chat/stream`, `POST /v1.0/warmup` | Cited answers (JSON or SSE), agentic chat, model warm-up |\n| Retrieval facets | `GET /v1.0/subjects/{id}/retrieval/{labels,tags}` | Discover distinct labels/tags for scoping |\n| Conversations | `/v1.0/threads`, `GET /v1.0/history[/{id}]`, `/v1.0/feedback` | Threads, chat history with telemetry, feedback |\n| Analytics | `GET /v1.0/analytics` | Per-subject windowed rollups |\n| Evaluation | `/v1.0/eval/facts`, `/v1.0/eval/runs[/{id}/{stream,cancel}]` | Ground-truth facts + async LLM-judged runs |\n| Model runners | `/v1.0/model-runners`, `GET .../health`, `GET /v1.0/ingestion/endpoints` | Model endpoints + health |\n| Prompts & settings | `/v1.0/prompts`, `GET/PUT /v1.0/settings` | Editable prompts; server settings (secrets masked) |\n| Request history | `GET /v1.0/api/request-history[/summary,/{id}]` | Captured requests (secrets redacted) |\n| MCP | `POST /mcp` | In-process MCP endpoint (see below) |\n\n---\n\n## MCP server\n\nPneuma serves a **Model Context Protocol** endpoint in-process at `POST /mcp` (JSON-RPC 2.0), so an AI agent drives the platform through the *same* `AuthenticateRequest` hook, `AuthorizationService`, and audit trail as the REST API — the same bearer/API-key credentials apply and every decision is audited identically.\n\n- **Read/query tools** — `pneuma_capabilities`, `pneuma_search`, `pneuma_get_node`, `pneuma_get_neighbors`, `pneuma_query` (grounded answer, optional `metadataFilter`, optional SSE stream), `pneuma_enumerate_subjects`/`_get_subject`, links, jobs, `pneuma_distinct_labels`/`_tags`.\n- **History & ops** — threads, history turns, feedback, analytics, eval runs/facts, request history, redacted settings, and model-endpoint health.\n- **Management/write** — create/update subjects, eval facts + runs (start/cancel/delete), thread delete.\n\nEnumerations are **bounded and paged** (`EnumerationResult` envelope: advance `skip` by the page size until `endOfResults`), full objects are fetched one at a time, and secret-bearing tools redact by default. The full tool catalog and RBAC mapping are in [`MCP_API.md`](MCP_API.md).",
          "format": "md",
          "labels": [
            "readme"
          ],
          "tags": {}
        },
        {
          "id": "readme--architecture",
          "category": "readme",
          "title": "README — Architecture",
          "summary": "ASCII architecture diagram, technology-by-layer table, and repository layout.",
          "body": "## Architecture\n\n```\n                       ┌───────────────────────────────┐\n                       │  Dashboards (React / Vite)    │\n                       │  admin 3010 · subject 3011 ·  │\n                       │  user 3012                    │\n                       └───────────────┬───────────────┘\n                                       │ HTTP\n                                       ▼\n                       ┌───────────────────────────────┐\n                       │  Pneuma Server (C# / Watson)  │\n                       │  REST /v1.0  +  MCP /mcp      │\n                       │  in-process chunk / embed /   │\n                       │  summarize (PolyPrompt)       │\n                       │  Port 8080                    │\n                       └──┬─────┬─────┬─────┬──────────┘\n              ┌───────────┘     │     │     └───────────┐\n              ▼                 ▼     ▼                 ▼\n     ┌────────────────┐ ┌────────────┐ ┌────────────┐ ┌────────────────┐\n     │  DocumentAtom  │ │  LiteGraph │ │  RecallDB  │ │ Model endpoints│\n     │ (extract cells)│ │  (graph)   │ │(vec+text)  │ │ Ollama/OpenAI/…│\n     │   Port 8000    │ │  Port 8701 │ │  Port 8600 │ │  (PolyPrompt)  │\n     └────────────────┘ └─────┬──────┘ └─────┬──────┘ └────────────────┘\n                              │              │\n                              ▼              ▼\n                       ┌────────────┐ ┌────────────┐\n                       │ PostgreSQL │ │   Less3    │\n                       │ + pgvector │ │ (S3 store) │\n                       │ Port 15432 │ └────────────┘\n                       └────────────┘\n\nObservability: Prometheus (9090) · Tempo (3200) · Grafana (3000)\n```\n\n| Layer | Technology |\n|-------|-----------|\n| Backend | C# on **Watson 7.1** (REST + in-process MCP) |\n| Control-plane DB | **PostgreSQL** (SQLite / MySQL / SQL Server also supported via a provider-neutral data layer) |\n| Knowledge graph | **LiteGraph** |\n| Type detection / cell extraction | **DocumentAtom** |\n| Chunking / embedding / summarization | In-process — **`Pneuma.Chunking`** (cl100k_base + BERT tokenizers) + **PolyPrompt** |\n| Retrieval store (vector + full-text) | **RecallDB** (Postgres + pgvector) |\n| LLM access | **PolyPrompt** (OpenAI, OpenAI-compatible, Gemini, Ollama, Azure OpenAI, Anthropic, Bedrock, Voyage AI, Vertex AI) |\n| Object storage | **Less3** (S3-compatible) via **Blobject** |\n| Observability | **Prometheus**, **Grafana**, **Tempo** |\n| Dashboards | Three React / Vite apps: **admin**, **subject**, **user** |\n\nRepository layout:\n\n```\nsrc/               Pneuma.Core library, Pneuma.Server (Watson host), Test.* suites\nadmin-dashboard/   subject-dashboard/  user-dashboard/   three React/Vite apps\nsdk/               csharp/  js/  python/   client SDKs\ndocker/            compose.yaml, factory/ (resettable defaults), PORTS.md, reset.bat, update.bat\nassets/            logo, favicon, Grafana dashboards, screenshots\nbuild-*.bat        image build/publish scripts (see below)\nREST_API.md · MCP_API.md · TELEMETRY.md · CHANGELOG.md\n```",
          "format": "md",
          "labels": [
            "readme"
          ],
          "tags": {}
        },
        {
          "id": "readme--building-sdks-contributing",
          "category": "readme",
          "title": "README — Building images / SDKs / Filing issues / License",
          "summary": "Image build scripts and multi-arch publishing, developer build commands, the three SDKs, issue/PR guidance, and license.",
          "body": "## Building images\n\nPneuma's five images (`jchristn77/pneuma-server`, `-postgres`, `-admin-ui`, `-subject-ui`, `-user-ui`) are built and pushed with the repo-root `build-*.bat` scripts, each taking a version tag:\n\n```bat\nbuild-all.bat v0.1.0          :: build + push all five\nbuild-server.bat v0.1.0       :: just the server\nbuild-admin-ui.bat v0.1.0     :: just the admin dashboard\n```\n\nEach script uses `docker buildx` to publish multi-architecture (`linux/amd64` + `linux/arm64`) images tagged `:latest` and `:<tag>`. The Compose stack references the pinned images, so a deployment `docker compose pull`s rather than building from source.\n\nTo work on the code directly:\n\n```bash\ndotnet build src/Pneuma.sln\ndotnet run --project src/Test.Automated       # Touchstone test suites\ncd admin-dashboard && npm ci && npm run build  # (and subject-/user-dashboard)\n```\n\n---\n\n## SDKs\n\nClient libraries mirror the REST surface:\n\n| SDK | Location | Notes |\n|---|---|---|\n| **C#** | [`sdk/csharp/`](sdk/csharp/) | System.Text.Json, typed models, async streaming |\n| **JavaScript** | [`sdk/js/`](sdk/js/) | Native fetch, SSE streaming helper, no runtime deps |\n| **Python** | [`sdk/python/`](sdk/python/) | httpx client, typed models |\n\nEach SDK directory has its own README with install and usage examples.\n\n---\n\n## Filing issues, enhancement requests, and pull requests\n\nPneuma is developed on GitHub at **https://github.com/jchristn/Pneuma**.\n\n- **Bugs** — open an [issue](https://github.com/jchristn/Pneuma/issues) with what you did, what you expected, what happened, and enough detail to reproduce (relevant logs, the service and version, and your configuration with secrets redacted).\n- **Enhancement requests** — open an issue describing the use case and the outcome you want; because Pneuma is in alpha, this is the best time to influence the direction of an API or feature before it stabilizes.\n- **Pull requests** — contributions are welcome. Please open an issue first to discuss anything non-trivial, keep changes focused, and match the existing code style (the backend conventions are documented in `CLAUDE.md`). Build the backend with `dotnet build src/Pneuma.sln`; each dashboard builds with `npm ci && npm run build`.\n\nGiven the alpha status, interfaces may change between `0.x` releases — please note the version you're running when you report an issue.\n\n## License\n\nPneuma is released under the **MIT License**. See [`LICENSE`](LICENSE).\n\nCopyright (c) 2026 Joel Christner.",
          "format": "md",
          "labels": [
            "readme"
          ],
          "tags": {}
        },
        {
          "id": "rest-api--authentication",
          "category": "rest-api",
          "title": "Pneuma REST API — Authentication / System / Tokens",
          "summary": "Base URL, auth headers, error format and status codes, system/health routes, and session token routes.",
          "body": "# Pneuma REST API\n\nBase URL (local): `http://127.0.0.1:8080`. All JSON is camelCase; enums are strings. OpenAPI lives at `GET /openapi.json`.\n\n## Authentication\n\nMost routes require a bearer token: `Authorization: Bearer <token>`. Obtain one by logging in with email/password. Machine credentials (access key + secret) may authenticate directly with `x-access-key` + `x-secret-key`, and a system admin key may be sent as `x-api-key`.\n\n| Header | Purpose |\n|--------|---------|\n| `Authorization: Bearer <token>` | Session token (preferred) |\n| `x-token` | Alternate carrier for the session token |\n| `x-api-key` | System administrator API key |\n| `x-access-key` / `x-secret-key` | Credential (API key) authentication |\n| `x-email` / `x-password` / `x-tenant-guid` | Header login (session-creation only) |\n\nErrors use `{ \"error\": \"<code>\", \"message\": \"<text>\", \"context\": <optional> }`. Status codes: 200 OK, 201 Created, 204 No Content, 400 Bad Request, 401 Unauthorized, 403 Forbidden, 404 Not Found, 409 Conflict, 429 Too Many Requests, 500 Internal Server Error.\n\n## System\n\n| Method | Path | Auth | Description |\n|--------|------|------|-------------|\n| GET | `/` | none | Server banner |\n| HEAD | `/` | none | Liveness |\n| GET | `/v1.0/api/health` | none | Health check → `{ status, serviceName, version, timeUtc }` |\n| GET | `/openapi.json` | none | OpenAPI 3 document |\n\n## Tokens\n\n| Method | Path | Auth | Description |\n|--------|------|------|-------------|\n| POST | `/v1.0/token` | none | Create a session token. Body `{ email, password, tenantId? }` → `{ token, expiresUtc, principalType, tenantId, userId, displayName, email, isAdmin, isTenantAdmin }` |\n| GET | `/v1.0/token` | bearer | Validate the current token; returns principal summary |\n| GET | `/v1.0/token/details` | bearer | Decoded authentication context |\n| DELETE | `/v1.0/token` | bearer | Revoke the current session (logout), 204 |",
          "format": "md",
          "labels": [
            "rest-api"
          ],
          "tags": {}
        },
        {
          "id": "rest-api--pagination",
          "category": "rest-api",
          "title": "Pneuma REST API — Pagination (list endpoints)",
          "summary": "EnumerationResult envelope and the maxResults/skip/order/search query parameters for list endpoints.",
          "body": "## Pagination (list endpoints)\n\nEvery collection `GET` (tenants, users, credentials, roles, permissions, assignments, audit, subjects, links, jobs, model-runners, prompts) returns a paginated **`EnumerationResult`** envelope rather than a bare array. Paging is controlled with query-string parameters:\n\n| Query param | Default | Description |\n|-------------|---------|-------------|\n| `maxResults` | `100` | Page size, clamped to `1..1000` |\n| `skip` | `0` | Records to skip from the start of the ordered set |\n| `order` | `desc` | `asc` or `desc` by creation time (`CreatedAscending`/`CreatedDescending`) |\n| `search` | — | Case-insensitive substring filter over the entity's primary label (name/email/url/etc.) |\n\nResponse envelope:\n\n```json\n{\n  \"success\": true,\n  \"maxResults\": 100,\n  \"skip\": 0,\n  \"totalRecords\": 42,\n  \"recordsRemaining\": 0,\n  \"endOfResults\": true,\n  \"objects\": [ /* the page of records */ ]\n}\n```\n\n`totalRecords` is the count after filtering (before paging), so a client can read a total cheaply with `?maxResults=1`. `recordsRemaining` and `endOfResults` describe whether more pages follow.",
          "format": "md",
          "labels": [
            "rest-api"
          ],
          "tags": {}
        },
        {
          "id": "rest-api--tenancy-rbac-audit",
          "category": "rest-api",
          "title": "Pneuma REST API — Tenants / Users / Credentials / RBAC / Audit / Request History",
          "summary": "Admin routes for tenants (with provisioning cascade), users, credentials, roles/permissions/assignments, audit, and request history.",
          "body": "## Tenants (admin)\n\n`GET /v1.0/tenants` · `POST /v1.0/tenants` · `GET /v1.0/tenants/{id}` · `PUT /v1.0/tenants/{id}` · `DELETE /v1.0/tenants/{id}`. Requires admin. Creating a tenant cascades to provision its first administrator + API key **and** its subordinate-service resources — a RecallDB tenant (same id) with a default collection, and an **isolated LiteGraph tenant + graph** (its GUIDs are recorded on the tenant as `liteGraphTenantGuid`/`liteGraphGraphGuid`). Each tenant's knowledge graph lives in its own LiteGraph tenant, so graphs never cross tenant boundaries. Provisioning is best-effort — an unavailable subordinate service never blocks tenant creation and is retried on next boot.\n\n## Users\n\n`GET /v1.0/users` (tenant-scoped; admin may pass `?tenantId=`) · `POST /v1.0/users` (body `CreateUserRequest { tenantId?, firstName, lastName, email, password, isAdmin, isTenantAdmin }`) · `GET|PUT|DELETE /v1.0/users/{id}`. Passwords are never returned.\n\n## Credentials\n\n`GET /v1.0/credentials` · `POST /v1.0/credentials` (body `{ name, userId?, expiresUtc? }`; the raw `secretKey` is returned **once**) · `GET /v1.0/credentials/{id}` · `DELETE /v1.0/credentials/{id}`.\n\n## Roles, Permissions, Assignments (RBAC, admin)\n\n- Roles: `GET|POST /v1.0/roles`, `GET|PUT|DELETE /v1.0/roles/{id}` (built-in roles are protected).\n- Permissions: `GET|POST /v1.0/permissions`, `GET|PUT|DELETE /v1.0/permissions/{id}`.\n- Assignments: `GET /v1.0/assignments?userId=<id>`, `POST /v1.0/assignments`, `DELETE /v1.0/assignments/{id}`.\n\n## Audit\n\n`GET /v1.0/audit` — recent security events (denials, bypasses, session/RBAC changes), tenant-scoped; admin may pass `?tenantId=`. Returns a paginated `EnumerationResult` (see [Pagination](#pagination-list-endpoints)).\n\n## Request History\n\n| Method | Path | Description |\n|--------|------|-------------|\n| GET | `/v1.0/api/request-history` | Paginated list; filters `method, statusCode, pathContains, fromUtc, toUtc, pageNumber, pageSize` (admin `tenantId, userId`). Bodies omitted. |\n| GET | `/v1.0/api/request-history/summary` | Time-bucketed counts; `fromUtc, toUtc, bucketMinutes` |\n| GET | `/v1.0/api/request-history/{id}` | Full entry including headers and bodies |\n| DELETE | `/v1.0/api/request-history/{id}` | Delete one, 204 |\n| DELETE | `/v1.0/api/request-history` | Bulk delete matching a filter → `{ deletedCount }` |",
          "format": "md",
          "labels": [
            "rest-api"
          ],
          "tags": {}
        },
        {
          "id": "rest-api--subjects",
          "category": "rest-api",
          "title": "Pneuma REST API — Subjects",
          "summary": "Subject fields, required models and collection, per-subject concurrency overrides, and asynchronous cascading delete.",
          "body": "## Subjects\n\n`GET|POST /v1.0/subjects` · `GET|PUT|DELETE /v1.0/subjects/{id}` · `GET /v1.0/subjects/by-slug/{slug}`. A subject has `displayName`, `type` (Person…), `description`, `tagline`, `graphRootNodeId`, plus: `urlSlug` (unique per tenant; auto-generated from the name when omitted — an explicit clash returns **409**), `thinkingEnabled` (show model reasoning in this subject's chats), `systemPrompt` (appended after the global system prompt for its chats), `ontologyClassifyPrompt` / `ontologyDefinitionPrompt` (appended after the global ontology prompts during ingestion), `historyRetentionDays` (chat-history retention, clamped ≥ 1), and a read-only `deletionStatus` (`None`/`Pending`/`Deleting`/`Failed`).\n\n**A subject owns its models and collection** (moved off link submission): `embeddingModel` (embedding model-endpoint id) and `inferenceModel` (completion model-endpoint id) are **required** before links can be ingested to it or questions answered about it; `collection` (RecallDB collection id, dimensionality must match the embedding model) is **required** to ingest. `rerankingModel` and `promptRewriteModel` (completion model-endpoint ids) are **optional** — when set, the answer pipeline re-ranks retrieved passages / rewrites the question before searching; when null those steps are skipped. `rerankingPrompt` / `promptRewritePrompt` are subject-level overrides appended after the global `reranking` / `prompt.rewrite` prompts (like `systemPrompt`), with sensible defaults applied on create. `tagline` is the subtitle shown beneath the subject's name on its ask page (defaults to the built-in label when omitted). `GET /v1.0/subjects/by-slug/{slug}` resolves a subject by its slug (tenant-scoped).\n\n**Per-subject ingestion concurrency.** A subject may carry an optional `concurrencyOverrides` object with the same integer keys as the system-wide `IngestionTuning` (see [`/v1.0/settings/ingestion`](#settings-admin)) — `contentRetrieval`, `typeDetection`, `cellExtraction`, `classification`, `graphMerge`, `summarization`, `chunking`, `embedding`, `indexing`, `maxConcurrentTasks`, `summarizationConcurrency`, `summarizationMinCellLength`, `classificationBatchSize`, `classificationBatchOverlap`, `classificationBatchConcurrency`, `stageTimeoutSeconds`. **Each field is nullable**: a set value overrides the system default for this subject's ingestion, while `null`/absent inherits the system default (effective value = override ?? system default). Accepted on create and update and applied live to subsequent ingestion work.\n\n**`DELETE` is asynchronous.** It marks the subject for deletion and returns **`202 Accepted`** immediately; a background worker then runs the full cascade — all of the subject's links (jobs, per-step processing logs, S3 pipeline artifacts + raw blobs, RecallDB chunk documents), the subject's entire LiteGraph subgraph (matched on the `subjectId` tag), and the subject's chat history + feedback — before removing the subject row. The subject shows `deletionStatus = Deleting` until it is gone; an interrupted deletion resumes on the next server start. External-store cleanup is best-effort so an unavailable subordinate service never blocks removal. When `graphRootNodeId` is omitted on create it is derived from `displayName` as a slug (lowercased, non-alphanumeric runs collapsed to single dashes) — e.g. `\"The Example Project\"` → `the-example-project`.",
          "format": "md",
          "labels": [
            "rest-api"
          ],
          "tags": {}
        },
        {
          "id": "rest-api--subject-prompts",
          "category": "rest-api",
          "title": "Pneuma REST API — Subject Prompts",
          "summary": "Per-subject prompt overrides with Append/Replace merge modes and global fallback.",
          "body": "## Subject Prompts\n\nEach subject can **override any keyed prompt** on top of the global default, choosing how the override merges with the global content. When a subject has no override for a key, the global prompt applies (fallback).\n\n| Method | Path | Description |\n|--------|------|-------------|\n| GET | `/v1.0/subjects/{id}/prompts` | List the subject's effective prompts → array of `{ key, name, effectiveContent, globalContent, overrideContent｜null, source (\"Global\"｜\"SubjectOverride\"), mergeMode (\"Append\"｜\"Replace\") }`. `effectiveContent` is what the pipeline actually uses: the `globalContent` alone when `source` is `Global`, or the global merged with the override per `mergeMode` when `source` is `SubjectOverride`. |\n| PUT | `/v1.0/subjects/{id}/prompts/{key}` | Set (or clear) the subject's override for `key`. Body `{ content, mergeMode (\"Append\"｜\"Replace\") }` → the resulting `SubjectPrompt` DTO. `Append` layers the override after the global prompt; `Replace` uses the override alone. **Empty `content` clears the override** (the subject falls back to the global prompt). |\n| DELETE | `/v1.0/subjects/{id}/prompts/{key}` | Remove the subject's override for `key` (falls back to the global prompt). Returns 204. |",
          "format": "md",
          "labels": [
            "rest-api"
          ],
          "tags": {}
        },
        {
          "id": "rest-api--chat-history-feedback",
          "category": "rest-api",
          "title": "Pneuma REST API — Chat History & Feedback",
          "summary": "Routes for chat turns, threads, eval facts and runs (with SSE progress), analytics, and feedback.",
          "body": "## Chat History & Feedback\n\n| Method | Path | Description |\n|---|---|---|\n| GET | `/v1.0/history` | List persisted chat turns (newest first), optionally `?subjectId=`. Paginated `EnumerationResult`. Each turn carries the question, answer, `thinking`, `model`, token counts, timing (`timeToFirstTokenMs`, `generationMs`, `thinkingMs`), `contextSize`, `citationsJson`, and structured per-stage telemetry in `performanceJson` (a serialized `{ schemaVersion, wallTimeMs, stages[] }`; each stage has `name`, `kind`, `provider`, `model`, `durationMs`, `timeToFirstTokenMs`, `promptTokens`, `completionTokens`, `success`) |\n| GET | `/v1.0/history/{id}` | A single turn with its feedback and tool-call trace: `{ turn, feedback: [...], toolCalls: [...] }`. The turn's `performanceJson` drives the dashboard History detail stage table and timing bars; `toolCalls` (tool, args, output, success, duration) drives the Tool activity table |\n| GET | `/v1.0/history?threadId=` | The history list also accepts `threadId` to return only a thread's turns |\n| GET | `/v1.0/threads` | List conversation threads (most-recently-active first), optionally `?subjectId=`. Paginated `EnumerationResult` |\n| POST | `/v1.0/threads` | Create a thread. Body `{ subjectId?, title? }` → the created thread |\n| GET | `/v1.0/threads/{id}` | A thread with its turns: `{ thread, turns: [...] }` |\n| PUT | `/v1.0/threads/{id}` | Rename a thread. Body `{ title }` |\n| DELETE | `/v1.0/threads/{id}` | Delete a thread and cascade its turns + tool calls. Returns 204 |\n| GET | `/v1.0/eval/facts` | List a subject's ground-truth facts (`?subjectId=` required) → `{ objects: [...] }` |\n| POST | `/v1.0/eval/facts` | Create a fact. Body `{ subjectId, question, expectedAnswer, category? }` |\n| DELETE | `/v1.0/eval/facts/{id}` | Delete a fact. Returns 204 |\n| GET | `/v1.0/eval/runs` | List evaluation runs, optionally `?subjectId=` → `{ objects: [...] }` |\n| POST | `/v1.0/eval/runs` | **Queue** a run. Body `{ subjectId, category? }` → returns the created run immediately with `status: \"Pending\"` (**non-blocking**). A background worker claims it, answers each fact through the real grounded pipeline, LLM-judges it (each fact admitted through the model-runner gate so eval yields to interactive traffic), and drives it to a terminal status (`Completed`/`Failed`/`Cancelled`). Status flow: `Pending → Running → {Completed｜Failed｜Cancelled}` |\n| GET | `/v1.0/eval/runs/{id}/stream` | Live run progress over server-sent events: `{type:\"metadata\",runId,total,status}`, `{type:\"result\",factId,question,verdict,score,category,failureMode}` per judged fact, `{type:\"progress\",completed,total,pass,partial,fail,status}` each tick, and a final `{type:\"complete\",run}` (or `{type:\"error\",message}`) |\n| POST | `/v1.0/eval/runs/{id}/cancel` | Cancel a queued or running run → 200 with the run (status becomes `Cancelled`; the worker observes it between facts and stops). Idempotent on an already-terminal run |\n| GET | `/v1.0/eval/runs/{id}` | A run with its results: `{ run, results: [{ question, expectedAnswer, producedAnswer, verdict, score, reason, failureMode, category }] }` |\n| DELETE | `/v1.0/eval/runs/{id}` | Delete a run and its results. Returns 204 |\n| GET | `/v1.0/analytics` | Per-subject chat analytics over a window. Query `subjectId?` + `days?` (default 30, clamped 1–365) → `{ overview: { turnCount, avgGenerationMs, p50/p95/p99GenerationMs, avgTimeToFirstTokenMs, avgPromptTokens, avgCompletionTokens, avgTokensPerSecond, thumbsUp, thumbsDown }, timeseries: [{ bucketUtc, count, avgGenerationMs }], stages: [{ stage, count, avgDurationMs, p95DurationMs }] }` |\n| GET | `/v1.0/feedback` | List feedback (newest first), optionally `?subjectId=`. Each item is `{ feedback, turn }` (the rated turn is enriched inline). Paginated `EnumerationResult` |\n| POST | `/v1.0/feedback` | Submit feedback on a turn: `{ turnId, rating: \"Up\"\\|\"Down\"\\|\"None\", comment? }`. Requires a rating and/or comment; unknown `turnId` → 404 |\n\nChat turns are persisted automatically as chats complete (agentic `POST /v1.0/chat/stream`), and each answering turn's `complete` SSE event now carries a `turnId` for feedback. History is pruned per subject by its `historyRetentionDays`.",
          "format": "md",
          "labels": [
            "rest-api"
          ],
          "tags": {}
        },
        {
          "id": "rest-api--links-ingestion",
          "category": "rest-api",
          "title": "Pneuma REST API — Content Links & Ingestion",
          "summary": "Link submission (with labels/tags), reingest, artifacts, job listing/live/summary/control, cascades, and ingestion stages.",
          "body": "## Content Links & Ingestion\n\n| Method | Path | Description |\n|--------|------|-------------|\n| POST | `/v1.0/subjects/{subjectId}/links` | Submit a link `{ url, title?, labels?: [string], tags?: { key: value } }` → creates the link and **enqueues an ingestion job**. The embedding/inference models and collection are taken from the **subject** (configure them on the subject first); a subject missing any returns **400**. Any `labels`/`tags` are attached to every chunk this link produces (RecallDB) and to the link's source graph node (LiteGraph) so retrieval can later be scoped to them via a query/chat `metadataFilter` (see **Search & Ask**) |\n| POST | `/v1.0/subjects/{subjectId}/links/bulk` | Submit many links at once `{ urls: [string], labels?: [string], tags?: { key: value } }` → `{ created, links: [...] }` (one link + job per URL). Models and collection come from the subject; any `labels`/`tags` are applied identically to every URL in the batch |\n| GET | `/v1.0/ingestion/endpoints` | Available model endpoints for ingestion → `{ embedding: [{ id, name, model, apiFormat, active }], completion: [...] }` (from Pneuma's native model-endpoint store) |\n| GET | `/v1.0/subjects/{subjectId}/links` | List a subject's links (with `status`, `lastIngestedUtc`, `lastError`). Paginated `EnumerationResult` |\n| GET | `/v1.0/links` / `GET /v1.0/links/{id}` | List / read links |\n| DELETE | `/v1.0/links/{id}` | Delete a link and **cascade** through everything it produced: ingestion jobs + per-step processing logs, pipeline document artifacts (source/atoms/chunks/vectors/subgraph + raw blobs), the chunk documents (RecallDB), and the graph nodes/edges it asserted (LiteGraph). Shared entity nodes reused by other links are preserved. Returns 204; external-store cleanup is best-effort so an unavailable subordinate service never blocks removal of the link. |\n| POST | `/v1.0/links/{id}/reingest` | **Reingest a link** — queues a **fresh** ingestion job that forces a full re-run. The link's stored content hash is cleared first so the delta-skip can't short-circuit the pipeline, then a brand-new job is enqueued (works even when the link has no prior job). Returns **202 Accepted** with the created job. |\n| POST | `/v1.0/links/reingest` | **Bulk reingest** — body `{ ids: [string] }`. Queues a fresh ingestion job per link (each clears the link's content hash to force a full re-run). Returns **202 Accepted** with `{ queued, skipped }` (`skipped` counts ids that could not be resolved to a link). |\n| GET | `/v1.0/links/{id}/log` | **Per-step ingestion log** for a link → `[{ job, events }]` (one entry per ingestion run, newest last). Each `event` is `{ stage, status, message, durationMs, createdUtc }`. |\n| GET | `/v1.0/links/{id}/source` | **Pipeline artifact** — the raw crawled source document in its original content type (may be HTML/PDF/binary). `404` if not present yet. |\n| GET | `/v1.0/links/{id}/atoms` | **Pipeline artifact** — DocumentAtom semantic cells as `application/json`. `404` if that stage hasn't run yet. |\n| GET | `/v1.0/links/{id}/chunks` | **Pipeline artifact** — the link's chunks as `application/json`. `404` if that stage hasn't run yet. |\n| GET | `/v1.0/links/{id}/vectors` | **Pipeline artifact** — the link's embeddings as `application/json`. `404` if that stage hasn't run yet. |\n| GET | `/v1.0/links/{id}/subgraph` | **Pipeline artifact** — candidate subgraph as `application/json`. `404` if that stage hasn't run yet. |\n| GET | `/v1.0/jobs?status=` | List ingestion jobs (optional status filter). Paginated `EnumerationResult` |\n| GET | `/v1.0/jobs/summary` | Time-bucketed ingestion activity, broken down by pipeline stage → `IngestionActivitySummary` `{ totalCount, totals: [{ stage, count }], buckets: [{ bucketStartUtc, bucketEndUtc, totalCount, stages: [{ stage, count }] }] }`. Query: `fromUtc`, `toUtc`, `bucketMinutes` (1–1440, default 15), `subjectId` (optional). Tenant-scoped. |\n| GET | `/v1.0/jobs/live` | **Live pipeline snapshot** → `IngestionLiveSnapshot` `{ generatedUtc, running: [], waitingForSlot: [], queued: [] }`. Each entry is an `IngestionLiveJob` `{ jobId, subjectId, sourceUrl, stage｜null, stateSinceUtc }`: `running` = jobs executing a stage now, `waitingForSlot` = jobs whose current stage is waiting for a free per-stage concurrency slot, `queued` = jobs waiting in the pool to start (`stage` is null). Each list is ordered longest-in-state first. `stateSinceUtc` is absolute so a client can tick the elapsed \"time in this state\" between polls. Query: `subjectId` (optional). Tenant-scoped. |\n| GET | `/v1.0/jobs/{id}` | Job detail with per-stage events → `{ job, events }` |\n| GET | `/v1.0/jobs/{id}/log` | Live per-stage log for a job → `{ job, events }` (poll for a \"follow logs\" view) |\n| POST | `/v1.0/jobs/{id}/restart` | Requeue a failed job |\n| POST | `/v1.0/jobs/{id}/stop` | Stop (cancel) a queued or in-flight job → job set to `Cancelled`. 409 if already finished |\n| DELETE | `/v1.0/jobs/{id}` | Delete an ingestion job (queue/job entry) and **cascade** its per-step processing log, the graph nodes/edges it asserted (LiteGraph), its chunk documents (RecallDB, deleted by `jobId` tag), and its raw blob. The link and its per-link S3 pipeline artifacts are left intact (they belong to the link). Returns 204; external-store cleanup is best-effort. |\n\n**Ingestion labels & tags.** Operator-supplied `labels` (plain strings) and `tags` (key/value) on link submission are stamped onto every chunk the link produces and onto its source graph node. Each label `L` is stored as a distinct chunk tag `label:L` (so a chunk can carry several labels at once), and the detected document type is exposed the same way; tags are stored verbatim (reserved provenance keys — `litegraphNodeId`, `linkId`, `tenantId`, `subjectId`, `jobId`, `sourceUrl`, `documentType`, and any `label:*` — are never overwritten). A query/chat/search `metadataFilter` then scopes retrieval to them: a required label matches with an equals condition on its `label:L` tag; a required tag matches by key/value.\n\nIngestion stages, each written to the log with a descriptive message and duration: **TypeDetection → CellExtraction → Classification (ontology mapping) → GraphMerge (knowledge-graph insertion) → Embedding (chunking + embedding generation) → Indexing (search index)**. Unknown document types fail at TypeDetection. Each stage records a completion log entry with counts (e.g. cells extracted, chunks produced, embeddings generated, nodes/edges inserted, documents indexed); failures record the stage and error.",
          "format": "md",
          "labels": [
            "rest-api"
          ],
          "tags": {}
        },
        {
          "id": "rest-api--model-runners-prompts-settings",
          "category": "rest-api",
          "title": "Pneuma REST API — Model Runners / Prompts / Settings (admin)",
          "summary": "Native model-endpoint CRUD and providers, endpoint health monitoring, global prompts, and settings incl. ingestion tuning.",
          "body": "## Model Runners (admin)\n\n`GET|POST /v1.0/model-runners` · `GET|PUT|DELETE /v1.0/model-runners/{id}`. Pneuma **manages model endpoints natively** in its own `modelrunners` store; LLM access (embeddings, completions, summarization) runs in-process through PolyPrompt. Each item is an embedding or completion endpoint: `{ id, type (Embedding|Completion), provider, name, model, endpoint, apiFormat, deployment, apiVersion, region, project, accessKeyId, active, maxConcurrentRequests, maxQueueDepth, contextSize }`.\n\n`provider` selects the PolyPrompt provider and determines which fields apply. Supported providers: **OpenAI, OpenAICompatible, Gemini, Ollama, AzureOpenAI, Anthropic** (completions only), **Bedrock, VoyageAI** (embeddings only), **VertexAI**. Provider-specific fields: `deployment` and `apiVersion` (AzureOpenAI), `region` and `accessKeyId` (Bedrock), `project` and `region` (VertexAI).\n\nCreate/update body `{ type (Embedding|Completion), provider, name?, model, endpoint?, apiFormat?, deployment?, apiVersion?, region?, project?, accessKeyId?, apiKey?, secretAccessKey?, sessionToken?, active, maxConcurrentRequests?, maxQueueDepth?, contextSize? }`. **Secrets are write-only** — `apiKey`, `secretAccessKey`, and `sessionToken` are accepted on create/update and **never returned** on read. `maxConcurrentRequests` caps concurrent connections to the endpoint (minimum 1, default 2); `maxQueueDepth` is how many requests may wait for a slot once that cap is reached (minimum 0, default 0 rejects over-limit requests immediately with 429, a queued wait past the endpoint timeout returns 504); `contextSize` is the completion model's context window in tokens and drives automatic chat conversation compaction (0 disables). Deletes resolve the endpoint type automatically.\n\n### Model endpoint health\n\nA background monitor probes each model endpoint's **base URL**, deduplicated so a host shared by several endpoints is checked once and the result is shared. Healthy/unhealthy transitions use a two-consecutive-check hysteresis; uptime, a rolling 24-hour history, latency, and the last HTTP status code are accumulated in memory.\n\n| Method | Path | Description |\n|--------|------|-------------|\n| GET | `/v1.0/model-runners/health` | Health of **every** model endpoint (deduplicated by base URL). Array of `ModelEndpointHealth`. |\n| GET | `/v1.0/model-runners/{id}/health` | Health of a single model endpoint. `404` if the endpoint is not found. |\n| POST | `/v1.0/model-runners/{id}/health/check` | Run a single health probe against the endpoint **immediately** (no request body). `200` returns the endpoint health object (same `ModelEndpointHealth` shape as `GET /v1.0/model-runners/{id}/health`). `400` if health checks are disabled for the endpoint; `404` if the endpoint is not found. |\n\n`ModelEndpointHealth`: `{ endpointId, endpointName, type, baseUrl, isHealthy, statusCode?, latencyMs?, firstCheckUtc?, lastCheckUtc?, lastHealthyUtc?, lastUnhealthyUtc?, lastStateChangeUtc?, totalUptimeMs, totalDowntimeMs, uptimePercentage, consecutiveSuccesses, consecutiveFailures, lastError?, history: [{ timestampUtc, success }] }`. Before a base URL's first probe, timestamps are null (a \"pending\" state).\n\n## Prompts (admin)\n\n`GET|POST /v1.0/prompts` · `GET|PUT|DELETE /v1.0/prompts/{id}`. Keyed prompts: `ontology.classify`, `cell.summarize`, `user.answer`.\n\n## Settings (admin)\n\nServer configuration, restricted to the **system administrator** (`isAdmin`). Non-admins receive 403.\n\n| Method | Path | Description |\n|--------|------|-------------|\n| GET | `/v1.0/settings` | Read the current settings. Secrets are masked as `********`. Returns `{ success, settings, meta }` |\n| PUT | `/v1.0/settings` | Overwrite the settings file with the supplied `AppSettings` body → `{ success, restartRequired, message, meta }` |\n| GET | `/v1.0/settings/ingestion` | Read the dashboard-tunable ingestion concurrency (`IngestionTuning`) — the system defaults every ingestion job uses |\n| PUT | `/v1.0/settings/ingestion` | Persist the supplied `IngestionTuning` body. **Applied live — no restart required** |\n\n`meta` describes the form: `sections[] { key, label, requiresRestart }` annotates which top-level sections need a server restart, `secretFields[]` lists the masked dot-paths, and `secretMask` is the sentinel. **Submitting a secret field unchanged (still equal to the mask) preserves the stored secret**; supplying a new value replaces it. Most sections require a restart to take effect; `cors` and `requestHistory` are applied live.\n\n**Ingestion concurrency (`/v1.0/settings/ingestion`).** `IngestionTuning` is a flat object of integers. Per-stage parallelism caps: `contentRetrieval`, `typeDetection`, `cellExtraction`, `classification`, `graphMerge`, `summarization`, `chunking`, `embedding`, `indexing`. Job pool & summarization: `maxConcurrentTasks` (how many ingestion jobs run at once), `summarizationConcurrency`, `summarizationMinCellLength` (cells shorter than this many characters are not summarized). Classification batching: `classificationBatchSize` (cells classified per model call — a document with more cells is split into batches so a slow model never classifies a whole document in one call), `classificationBatchOverlap` (context cells included on each side of a batch so relationships spanning a batch boundary are still seen; duplicates are merged by the graph-merge stage), `classificationBatchConcurrency` (batches from one document processed concurrently). Timeouts: `stageTimeoutSeconds` (abort a single stage past this many seconds). `PUT` persists and applies the new values live to subsequent ingestion work. A **subject** may override any of these per-subject via its `concurrencyOverrides` field (see below); the effective value for a subject is its override if set, otherwise the system default here.",
          "format": "md",
          "labels": [
            "rest-api"
          ],
          "tags": {}
        },
        {
          "id": "rest-api--graph-and-collections",
          "category": "rest-api",
          "title": "Pneuma REST API — Knowledge Graph / Collections",
          "summary": "Graph node/neighbor/edge routes and per-tenant RecallDB collection management.",
          "body": "## Knowledge Graph (user)\n\n| Method | Path | Description |\n|--------|------|-------------|\n| GET | `/v1.0/graph/nodes/{id}` | Node contents |\n| GET | `/v1.0/graph/nodes/{id}/neighbors` | Adjacent nodes |\n| GET | `/v1.0/graph/nodes/{id}/edges` | Relationships (edges) for the node |\n\n## Collections\n\nVector collections live in the retrieval store (RecallDB), which is the **authority** for tenants and collections; Pneuma simply relays administration to it and keeps no local collection state. Collections are **per-tenant**: each Pneuma tenant maps to a RecallDB tenant of the same id, and these endpoints operate within the caller's tenant. RecallDB assigns collection ids. A collection's `dimensionality` is **fixed at creation** and must match the embedding model used to ingest into it. Collections are create/read/list/delete (no update).\n\nProvisioning is automatic: at first-boot and whenever a **tenant is created** (`POST /v1.0/tenants`), Pneuma provisions the tenant on RecallDB and creates a **default collection** for it, so ingestion has a target out of the box.\n\n| Method | Path | Description |\n|--------|------|-------------|\n| GET | `/v1.0/collections` | List vector collections → paginated `EnumerationResult` of `{ id, name, description, dimensionality, active }` |\n| GET | `/v1.0/collections/{id}` | Read one collection |\n| PUT | `/v1.0/collections` | Create a collection `{ name, description?, dimensionality }` (dimensionality defaults to 768) → `201` with the created collection |\n| DELETE | `/v1.0/collections/{id}` | Delete a collection **and all of its documents** → `204` |",
          "format": "md",
          "labels": [
            "rest-api"
          ],
          "tags": {}
        },
        {
          "id": "rest-api--search",
          "category": "rest-api",
          "title": "Pneuma REST API — Search & Ask (user)",
          "summary": "Search routes, retrieval facet discovery, grounded query/stream, warmup, agentic chat stream, and the model-runner concurrency gate.",
          "body": "## Search & Ask (user)\n\n| Method | Path | Description |\n|--------|------|-------------|\n| GET | `/v1.0/search?q=<query>&max=20&collection=<id>&filter=<json>` | Full-text search (RecallDB) resolved to a representative set of graph nodes → `{ query, results: [{ node, score, snippet }] }`. `collection` is optional — when omitted the configured default (or the tenant's first active) collection is used. `filter` is an optional URL-encoded `RetrievalFilter` JSON (same shape as `metadataFilter` below) that scopes results to matching labels/tags |\n| GET | `/v1.0/subjects/{subjectId}/search?q=<query>&maxResults=20&skip=0&collection=<id>&filter=<json>` | Search a single subject's ingested documents (RecallDB, filtered by `subjectId`). Returns a paginated `EnumerationResult` ranked by score; each hit is `{ documentId, score, snippet, linkId, linkUrl, linkTitle, nodeId }`, linked back to the originating content link. Applies the subject's default `retrievalFilterJson` merged with the optional per-request `filter` (URL-encoded `RetrievalFilter` JSON), narrowing by label/tag. |\n| GET | `/v1.0/subjects/{id}/retrieval/labels` | The distinct retrieval labels an operator has applied to the subject's links → `{ subjectId, labels: [string] }`. A bounded aggregate over the subject's links (not an enumeration), used to populate the Ask **Scope** filter with real values for a `metadataFilter`'s `requiredLabels`/`excludedLabels`. |\n| GET | `/v1.0/subjects/{id}/retrieval/tags` | The distinct retrieval tag keys and their values applied to the subject's links → `{ subjectId, tags: { key: [value, …] } }`. A bounded aggregate (not an enumeration), used to build a `metadataFilter`'s `requiredTags`/`excludedTags` conditions. |\n| POST | `/v1.0/query` | Grounded answer. Body `{ question, maxResults?, subjectId?, metadataFilter? }` → `{ answer, sources: [node], grounded }`. When `subjectId` is set, retrieval is restricted to that subject's documents. `metadataFilter` is an optional facet filter `{ requiredLabels: [string], excludedLabels: [string], requiredTags: [{ key, condition, value }], excludedTags: [...] }`. Labels are a `List<string>` matched against each chunk's per-label `label:<value>` tags (a chunk must carry every required label; any excluded label removes it) — these are exactly the labels supplied at ingestion (plus the document type); tags are key/value conditions over chunk tags (conditions: `Equals`, `NotEquals`, `Contains`, `StartsWith`, `EndsWith`, `GreaterThan`, `LessThan`, `IsNull`, `IsNotNull`). It is merged with the subject's default `retrievalFilterJson` (union of all four lists), narrowing — never widening — the subject default. |\n| POST | `/v1.0/query/stream` | Same as `/v1.0/query` (incl. optional `subjectId`) but streamed over server-sent events (`metadata` / `delta` / `complete` / `error`). |\n| POST | `/v1.0/warmup` | Best-effort warm-up of the answering model. Body `{ subjectId? }` → `{ warmed: bool }`. Resolves the subject's (or tenant default) answering runner and issues a minimal completion so the model is loaded before the first real question. The dashboards call this when the Ask page opens / a subject is selected. Never fails the caller. |\n| POST | `/v1.0/chat/stream` | Multi-turn agentic chat over the corpus (the assistant may call read tools), streamed over SSE. Body `{ messages: [{ role, content }], maxResults?, subjectId?, metadataFilter? }`. When `subjectId` is set, the search and grounded-answer tools are restricted to that subject. `metadataFilter` (same shape as `/v1.0/query`) scopes the assistant's retrieval to matching labels/tags for this turn, merged with the subject's default filter. SSE event types: `delta`, `tool_call`, `tool_result`, `compacting` (the running history is being summarized to fit the completion model's `contextSize`), `complete`, `error`. The `complete` event carries `{ answer, model, …token telemetry…, toolCalls, citations: [{ linkId, url, title, score }], compacted, compactedSummary }`; when `compacted` is true the client should replace its prior message history with `compactedSummary`. |\n\n**Model-runner concurrency gate.** The grounded-answer and chat endpoints (`/v1.0/query`, `/v1.0/query/stream`, `/v1.0/chat/stream`, and the MCP `pneuma_query` tool) are admitted through a process-wide gate that caps concurrent model-runner usage. Requests beyond the cap queue up to a bounded depth; when the queue is full the request is rejected with **HTTP 429 Too Many Requests** (`{ \"error\": \"TooManyRequests\", ... }`) before any streaming begins — callers should back off and retry. Configured via the `ModelRunner` settings block (`MaxConcurrentRequests`, default 4; `MaxQueueDepth`, default 16).",
          "format": "md",
          "labels": [
            "rest-api"
          ],
          "tags": {}
        },
        {
          "id": "mcp-api--authentication-and-methods",
          "category": "mcp-api",
          "title": "Pneuma MCP API — Authentication / Methods",
          "summary": "MCP endpoint overview, how calls authenticate, and the JSON-RPC methods and result wrapper.",
          "body": "# Pneuma MCP API\n\nPneuma exposes a Model Context Protocol (MCP) endpoint so agents can drive the platform with the same credentials, permissions, and audit trail as the REST API. The endpoint is served **in-process by the Pneuma host** — there is no separate MCP process, no second auth model, and no separate port. It speaks JSON-RPC 2.0 over `POST /mcp`.\n\n## Authentication\n\nMCP calls authenticate exactly like REST calls. Present a bearer session token or an `access_`/`secret_` API-key credential in the standard headers; the request passes through Pneuma's `AuthenticateRequest` hook, and each tool authorizes against `AuthorizationService` for the resource and operation it touches. A call without a valid principal is rejected with `401` before any tool runs, and denials and system-admin bypasses are audited the same way REST denials are.\n\n## Methods\n\nThe endpoint implements the core MCP method set:\n\n- `initialize` — returns the protocol version, server info, and the `tools` capability.\n- `tools/list` — returns the available tools with their JSON-Schema input contracts.\n- `tools/call` — invokes a tool by name with an `arguments` object.\n- `ping` / `notifications/initialized` — liveness and the post-initialize notification.\n\nA `tools/call` result wraps the tool's output as MCP content: `{ \"content\": [ { \"type\": \"text\", \"text\": \"<JSON>\" } ], \"isError\": false }`, where the text is the tool's JSON payload.",
          "format": "md",
          "labels": [
            "mcp-api"
          ],
          "tags": {}
        },
        {
          "id": "mcp-api--enumerating-objects",
          "category": "mcp-api",
          "title": "Pneuma MCP API — Enumerating objects",
          "summary": "The bounded paging protocol for enumerate/get tool pairs, with a worked example.",
          "body": "## Enumerating objects\n\nThis is the one rule an agent must internalize before reading data: **Pneuma never returns an unbounded collection.** There is no \"list everything\" tool. Every collection is retrieved through paired tools — an `pneuma_enumerate_*` tool that returns small summaries a page at a time, and an `pneuma_get_*` tool that returns one full object by id.\n\nAn enumeration tool takes an `EnumerationQuery` — `maxResults`, `skip`, `order` (`asc`/`desc`), and an optional `search` — and returns an `EnumerationResult`:\n\n```json\n{\n  \"success\": true,\n  \"maxResults\": 50,\n  \"skip\": 0,\n  \"totalRecords\": 214,\n  \"recordsRemaining\": 164,\n  \"endOfResults\": false,\n  \"objects\": [ { \"id\": \"sub_...\", \"displayName\": \"...\", \"type\": \"...\", \"active\": true } ]\n}\n```\n\nTo read a full collection, page it to completion:\n\n1. Call the enumerate tool with `skip: 0`. The **first** result's `totalRecords` is the exact number of records that exist — read it once and you know how far you have to go.\n2. Advance `skip` by your page size and call again.\n3. Stop when `endOfResults` is `true` (equivalently, when `recordsRemaining` reaches `0`).\n\nThe objects in an enumeration are deliberately small — an id, a name, a type, a status flag. They are enough to decide what you want, not to work with. When you need the whole object — a subject's full record, a node's content, a job's log, the raw source bytes, atoms, chunks, vectors, or a subgraph — fetch it individually with the matching `pneuma_get_*` tool. Keeping enumeration payloads small is what lets an agent scan thousands of records without drowning a context window.\n\nA worked example, reading every subject in pages of 50:\n\n```\ntools/call pneuma_enumerate_subjects { \"maxResults\": 50, \"skip\": 0 }   → totalRecords 214, endOfResults false\ntools/call pneuma_enumerate_subjects { \"maxResults\": 50, \"skip\": 50 }  → endOfResults false\ntools/call pneuma_enumerate_subjects { \"maxResults\": 50, \"skip\": 100 } → endOfResults false\ntools/call pneuma_enumerate_subjects { \"maxResults\": 50, \"skip\": 150 } → endOfResults false\ntools/call pneuma_enumerate_subjects { \"maxResults\": 50, \"skip\": 200 } → endOfResults true\ntools/call pneuma_get_subject { \"id\": \"sub_abc123\" }                   → the full subject\n```\n\nEvery `pneuma_enumerate_*` tool follows this exact protocol — `pneuma_enumerate_subjects`, `pneuma_enumerate_jobs`, `pneuma_enumerate_links`, `pneuma_enumerate_threads`, `pneuma_enumerate_feedback`, `pneuma_enumerate_eval_runs`, `pneuma_enumerate_eval_facts`, `pneuma_enumerate_request_history`, and `pneuma_enumerate_model_runner_health` all take `maxResults`/`skip`/`order`, return the `EnumerationResult` envelope (`totalRecords`/`recordsRemaining`/`endOfResults`) with small summaries, and pair with a matching `pneuma_get_*` tool for the full object. Start at `skip: 0` and advance `skip` by your page size until `endOfResults` is `true`.",
          "format": "md",
          "labels": [
            "mcp-api"
          ],
          "tags": {}
        },
        {
          "id": "mcp-api--tools",
          "category": "mcp-api",
          "title": "Pneuma MCP API — Tools",
          "summary": "Catalog of MCP tools with their purposes and required permissions.",
          "body": "## Tools\n\nThe current tool set is small and growing; `pneuma_capabilities` and `tools/list` are always the source of truth for what is available.\n\n| Tool | Purpose | Auth |\n|------|---------|------|\n| `pneuma_capabilities` | Describe the platform and restate the paging protocol. | Any authenticated principal |\n| `pneuma_enumerate_subjects` | Page subject summaries (`EnumerationQuery` in, `EnumerationResult` out). | Subject / Read |\n| `pneuma_get_subject` | Fetch one full subject by id. | Subject / Read |\n| `pneuma_create_subject` | Create a subject (`displayName` required; `type`, `description`, `tagline`, `urlSlug`, `thinkingEnabled`, `systemPrompt`, `ontologyClassifyPrompt`, `ontologyDefinitionPrompt`, `historyRetentionDays`, `embeddingModel`, `inferenceModel`, `collection`, `rerankingModel`, `promptRewriteModel`, `rerankingPrompt`, `promptRewritePrompt`, `concurrencyOverrides` optional). `embeddingModel`/`inferenceModel`/`collection` are required before the subject can ingest or answer; `rerankingModel`/`promptRewriteModel` are optional (steps skipped when null). Prompt fields default sensibly. `concurrencyOverrides` is an object of optional integer per-subject ingestion concurrency caps (see note below). A unique slug is auto-generated when omitted; an explicit slug clash is rejected. | Subject / Create |\n| `pneuma_update_subject` | Update a subject (`id` required; only supplied fields change — including the models, collection, rerank/rewrite prompts, and `concurrencyOverrides`; a changed `urlSlug` must stay unique). | Subject / Update |\n| `pneuma_enumerate_jobs` | Page ingestion-job summaries (id, status, stage, source url). | IngestionJob / Read |\n| `pneuma_get_job` | Fetch one full ingestion job by id. | IngestionJob / Read |\n| `pneuma_ingestion_summary` | Summarize ingestion activity over time, broken down by pipeline stage: fixed-width time buckets with per-stage event counts plus overall per-stage totals. Optional `subjectId`, `fromUtc`/`toUtc` window, and `bucketMinutes` (1–1440, default 15). | IngestionJob / Read |\n| `pneuma_enumerate_links` | Page content-link summaries (id, url, title, status). | Subject / Read |\n| `pneuma_get_link` | Fetch one full content link by id. | Subject / Read |\n| `pneuma_search` | Full-text search the corpus (RecallDB) against the resolved default collection; bounded, ranked node summaries (a top-N query, not an enumeration). Accepts an optional `metadataFilter` to scope retrieval to specific labels/tags. | GraphNode / Read |\n| `pneuma_get_node` | Fetch one full knowledge-graph node by id. | GraphNode / Read |\n| `pneuma_get_neighbors` | Fetch a node's adjacent nodes as a bounded set of summaries. | GraphNode / Read |\n| `pneuma_query` | Ask a grounded question; returns a cited answer, supporting sources, and an `insufficientSupport` flag. Accepts an optional `metadataFilter` to scope retrieval to specific labels/tags. | GraphNode / Read |\n| `pneuma_get_history_turn` | Fetch one chat turn with its feedback, tool-call trace, and per-stage performance telemetry (`id` required). | Subject / Read |\n| `pneuma_enumerate_threads` | Enumerate conversation thread summaries (paged; `EnumerationResult` out), optional `subjectId`. Use `pneuma_get_thread` for a thread's full turns. | Subject / Read |\n| `pneuma_get_thread` | Fetch one conversation thread with its ordered turns (`id` required). | Subject / Read |\n| `pneuma_delete_thread` | Delete a conversation thread and cascade its turns + tool-call trace (`id` required). Irreversible. | Subject / Delete |\n| `pneuma_enumerate_feedback` | Enumerate chat-feedback summaries (paged; `EnumerationResult` out), optional `subjectId`. | Subject / Read |\n| `pneuma_analytics` | Per-subject chat analytics over a window (`subjectId?`, `days?` default 30): volume, latency percentiles, per-stage timing, feedback. | Subject / Read |\n| `pneuma_enumerate_eval_runs` | Enumerate RAG evaluation-run summaries (paged; `EnumerationResult` out), optional `subjectId`. Use `pneuma_get_eval_run` for a run's full results. | Subject / Read |\n| `pneuma_get_eval_run` | Fetch one evaluation run with its per-fact results (`id` required). | Subject / Read |\n| `pneuma_enumerate_eval_facts` | Enumerate a subject's ground-truth evaluation facts (paged; `EnumerationResult` out; `subjectId` required). | Subject / Read |\n| `pneuma_create_eval_fact` | Create a ground-truth fact (`subjectId`, `question`, `expectedAnswer` required; `category?`). | Subject / Update |\n| `pneuma_delete_eval_fact` | Delete an evaluation fact by id (`id` required). | Subject / Delete |\n| `pneuma_start_eval_run` | Queue an evaluation run for a subject (`subjectId` required; `category?`). Returns the `Pending` run immediately; poll `pneuma_get_eval_run` for progress. | Subject / Update |\n| `pneuma_cancel_eval_run` | Cancel a queued or running evaluation run (`id` required). Idempotent on a finished run. | Subject / Update |\n| `pneuma_delete_eval_run` | Delete an evaluation run and its per-fact results (`id` required). Irreversible. | Subject / Delete |\n| `pneuma_distinct_labels` | Return a subject's distinct retrieval labels (`subjectId` required) — a bounded aggregate for building a `metadataFilter`, not an enumeration. | Subject / Read |\n| `pneuma_distinct_tags` | Return a subject's distinct retrieval tag keys and values (`subjectId` required) — a bounded aggregate for building a `metadataFilter`, not an enumeration. | Subject / Read |\n| `pneuma_enumerate_request_history` | Enumerate captured request-history summaries (paged; `EnumerationResult` out). Optional `tenantId`, `userId`, `method`, `pathContains`, `statusCode` filters. Use `pneuma_get_request_history` for a full entry. | System administrator only |\n| `pneuma_get_request_history` | Fetch one captured request-history entry with full detail (`id` required; secrets redacted at capture). | System administrator only |\n| `pneuma_request_history_summary` | Summarize request history over an optional filter (`tenantId?`, `method?`, `pathContains?`, `statusCode?`): totals, status-code breakdown, latency. | System administrator only |\n| `pneuma_get_settings` | Return the server settings with every secret field redacted. | System administrator only |\n| `pneuma_enumerate_model_runner_health` | Enumerate model-endpoint health summaries (embedding + completion; paged; `EnumerationResult` out). | ModelRunner / Read |\n| `pneuma_get_model_runner_health` | Fetch the health of one model endpoint by id (uptime, latency, last status; `id` required). | ModelRunner / Read |\n\nFurther tools (ingest a link) follow the same contract as they land. The grounded-answer logic is shared with the REST `/v1.0/query` endpoint, so the two surfaces cannot drift.",
          "format": "md",
          "labels": [
            "mcp-api"
          ],
          "tags": {}
        },
        {
          "id": "mcp-api--concurrency-overrides",
          "category": "mcp-api",
          "title": "Pneuma MCP API — Tools: per-subject ingestion concurrency",
          "summary": "How MCP subject create/update accept per-subject ingestion concurrency overrides and their keys.",
          "body": "**Per-subject ingestion concurrency (`concurrencyOverrides`).** `pneuma_create_subject` and `pneuma_update_subject` accept an optional `concurrencyOverrides` object that overrides the system-wide ingestion tuning (`/v1.0/settings/ingestion`) for this subject's jobs. Every field is an optional integer; a set value replaces the system default and a `null`/absent field inherits it (effective value = override ?? system default), matching the REST subject `concurrencyOverrides` field. On update, passing `concurrencyOverrides: null` clears the subject's overrides. Keys: `contentRetrieval`, `typeDetection`, `cellExtraction`, `classification`, `graphMerge`, `summarization`, `chunking`, `embedding`, `indexing`, `maxConcurrentTasks`, `summarizationConcurrency`, `summarizationMinCellLength`, `classificationBatchSize` (cells classified per model call; larger documents are split into batches), `classificationBatchOverlap` (context cells per side so boundary relationships are still seen), `classificationBatchConcurrency` (batches per document run concurrently), `stageTimeoutSeconds`. Changes apply live to subsequent ingestion work (no restart).",
          "format": "md",
          "labels": [
            "mcp-api"
          ],
          "tags": {}
        },
        {
          "id": "mcp-api--metadata-filter-and-streaming",
          "category": "mcp-api",
          "title": "Pneuma MCP API — Retrieval scoping (metadataFilter) / Streaming / Transport note",
          "summary": "The metadataFilter shape and matching rules, SSE streaming for pneuma_query, and the shared-port transport note.",
          "body": "## Retrieval scoping (`metadataFilter`)\n\n`pneuma_search` and `pneuma_query` accept an optional `metadataFilter` object that narrows retrieval to chunks carrying specific labels and tags:\n\n```json\n{\n  \"requiredLabels\": [\"string\"],\n  \"excludedLabels\": [\"string\"],\n  \"requiredTags\": [ { \"key\": \"string\", \"condition\": \"Equals\", \"value\": \"string\" } ],\n  \"excludedTags\": [ { \"key\": \"string\", \"condition\": \"Contains\", \"value\": \"string\" } ]\n}\n```\n\nA chunk is eligible only when it carries **every** required label and satisfies **every** required tag condition, and **none** of the excluded labels or tag conditions match. `condition` is one of `Equals`, `NotEquals`, `Contains`, `StartsWith`, `EndsWith`, `GreaterThan`, `LessThan`, `IsNull`, `IsNotNull` (`value` is ignored for `IsNull`/`IsNotNull`). Discover the valid label and tag values for a subject with `pneuma_distinct_labels` and `pneuma_distinct_tags` before building a filter.\n\n## Streaming\n\n`pneuma_query` accepts `stream: true`. When set, the `tools/call` responds as a **Server-Sent Events** stream (Streamable-HTTP style): `metadata` and `delta` events carry the answer as it is generated, and the **final** SSE event is the JSON-RPC result message. Without `stream`, the same call returns a single JSON response.\n\n## Transport note\n\nMCP shares the Pneuma host and its port; it does not run as a separate container or on a distinct port. Point your MCP client at the Pneuma base URL with the path `/mcp`.",
          "format": "md",
          "labels": [
            "mcp-api"
          ],
          "tags": {}
        },
        {
          "id": "telemetry--metrics",
          "category": "telemetry",
          "title": "Pneuma Telemetry Guide — At a glance / Metrics inventory",
          "summary": "Overview of metrics vs traces and the full pneuma_* metric inventory by domain.",
          "body": "# Pneuma Telemetry Guide\n\nPneuma emits two complementary streams of operational telemetry — **metrics** (aggregate counters and\nlatency histograms) and **traces** (distributed spans of individual requests) — across the whole product:\nHTTP, the ingestion pipeline, the retrieval/answer path, and every outbound integration. This guide explains\nwhat is emitted, how it is exposed and collected, how to reach Grafana, and how to read the data to answer\nreal operational questions.\n\n---\n\n## 1. At a glance\n\n| | Metrics | Traces |\n|---|---|---|\n| **Emitted by** | `PneumaMetrics` (in-process, Prometheus text format) | `TelemetryService` (OpenTelemetry via Radiant) |\n| **Exposed at** | `GET /metrics` on the Pneuma server | OTLP gRPC push |\n| **Collected by** | Prometheus (`:9090`) scraping `/metrics` | Tempo (`:3200`, OTLP ingest `:4317`) |\n| **Visualized in** | Grafana (`:3000`) dashboard panels | Grafana → Explore → Tempo |\n\nEverything is provisioned in the docker stack under `docker/` and `docker/factory/`; a plain\n`docker compose up -d --build` brings up Pneuma, Prometheus, Tempo, and Grafana wired together.\n\n---\n\n## 2. What Pneuma measures (metrics inventory, by domain)\n\nAll metric names are prefixed `pneuma_`. Labels are deliberately **low-cardinality** (no ids), so series\nstay bounded.\n\n### HTTP\n- `pneuma_http_requests_total{method,route,status}` — request counter.\n- `pneuma_http_request_duration_seconds{...}` — request-latency histogram (p50/p95/p99 derived in Grafana).\n\n### Ingestion\n- `pneuma_ingestion_jobs_total{outcome}` — jobs by outcome (started/completed/failed/cancelled).\n- `pneuma_ingestion_completed_total`, `pneuma_ingestion_failed_total` — lifetime counters.\n- `pneuma_ingestion_stage_total{stage,outcome}` — per-stage event counter across **every** pipeline stage\n  (ContentRetrieval, TypeDetection, CellExtraction, Classification, GraphMerge, Summarization, Chunking,\n  Embedding, Indexing), including `queued` when a stage waited for a concurrency slot.\n- `pneuma_ingestion_stage_duration_seconds{stage}` — per-stage duration histogram (where stalls show up).\n\n### Retrieval & Answer\n- `pneuma_chat_answers_total{outcome}` — answered chat/query turns by outcome.\n- `pneuma_chat_answer_duration_seconds{outcome}` — total answer-latency histogram.\n- `pneuma_chat_stage_duration_seconds{stage}` — per-stage answer-pipeline latency histogram, one series per\n  coarse stage: `prompt_rewrite`, `compaction`, `tool`, `final_inference` (the same stages persisted per turn\n  in `performanceJson` and surfaced in the dashboard History detail and Analytics views).\n\n### Integrations\n- `pneuma_integration_requests_total{service,operation,outcome}` — outbound calls to RecallDB,\n  LiteGraph, DocumentAtom, PolyPrompt (embeddings/completions/summarization), by outcome (ok/error).\n- `pneuma_integration_request_duration_seconds{service,operation}` — per-integration latency histogram.\n\n### Authorization / uptime\n- `pneuma_authz_decisions_total{result}` — permit/deny decisions.\n- `pneuma_uptime_seconds` — process uptime gauge.",
          "format": "md",
          "labels": [
            "telemetry"
          ],
          "tags": {}
        },
        {
          "id": "telemetry--traces-and-collection",
          "category": "telemetry",
          "title": "Pneuma Telemetry Guide — Traces (spans) / Exposure and collection",
          "summary": "What spans are emitted, per-turn DB telemetry, and how metrics and traces are exposed and collected.",
          "body": "## 3. What Pneuma traces (spans)\n\nTraces are emitted over OTLP and stored in Tempo. Spans exist for:\n\n- **Ingestion** — a root span per job (`ingestion <sourceUrl>`, tagged `pneuma.job.id`, `pneuma.tenant.id`,\n  `pneuma.subject.id`) with a child span per pipeline stage (`stage:<Name>`), so a slow job's stage breakdown\n  is visible in one trace.\n- **Requests & integrations** — request capture and downstream integration calls carry spans, tagged with the\n  service and operation, so a slow answer resolves to the specific RecallDB/PolyPrompt/LiteGraph call that caused\n  it.\n\nPer-turn answer telemetry is additionally persisted structurally on each chat turn (`performanceJson`, the\n`chatturnperfevents` table) and rendered in the dashboard History detail modal and the Analytics view — a\nDB-native complement to the Prometheus metrics and Tempo spans.\n\n---\n\n## 4. How it is exposed and collected\n\n- **Metrics exposure.** The Pneuma server serves the current metrics in Prometheus text-exposition format at\n  `GET /metrics`. Health is at `GET /v1.0/api/health`.\n- **Metrics collection.** Prometheus (`docker/prometheus.yaml`) scrapes the Pneuma `/metrics` endpoint on an\n  interval and stores the series in its TSDB (`:9090`).\n- **Trace exposure & collection.** The server pushes spans over OTLP to Tempo (`:4317` ingest); Tempo serves\n  trace queries on `:3200`.\n- **Datasources & dashboards.** Grafana is provisioned (`docker/grafana/provisioning/…` and the `docker/factory`\n  equivalent) with **Prometheus** and **Tempo** datasources and a set of **per-domain dashboards** mounted\n  from `assets/grafana/*.json` — one dashboard each for Overview, HTTP, Ingestion, Chat & Retrieval, and\n  Integrations (all in the Grafana **Pneuma** folder).",
          "format": "md",
          "labels": [
            "telemetry"
          ],
          "tags": {}
        },
        {
          "id": "telemetry--grafana-dashboards",
          "category": "telemetry",
          "title": "Pneuma Telemetry Guide — Accessing Grafana / Reading the dashboards",
          "summary": "How to reach Grafana and what each of the five domain dashboards shows.",
          "body": "## 5. Accessing Grafana\n\n1. Bring up the stack: `docker compose up -d --build` (from `docker/`, or `docker/factory/` for the seeded\n   demo).\n2. Open **http://localhost:3000**.\n3. Log in with the default credentials **`admin` / `admin`** (set via `GF_SECURITY_ADMIN_*`; change them for\n   any non-local deployment).\n4. Open **Dashboards → Browse → the Pneuma folder**. Five domain dashboards auto-load from provisioning:\n   **Pneuma — Overview**, **— HTTP**, **— Ingestion**, **— Chat & Retrieval**, and **— Integrations**.\n\nRelated URLs: Prometheus **http://localhost:9090**, Tempo API **http://localhost:3200**.\n\n---\n\n## 6. Reading the dashboards (one per domain)\n\nThe observability dashboards are split by domain — open the one that matches your question:\n\n- **Pneuma — Overview** — uptime, total request rate, error ratio, ingestion completed-vs-failed. Start here\n  for a health snapshot.\n- **Pneuma — HTTP** — request rate by route and status class, latency quantiles (p50/p95/p99), and the top\n  routes by p95. Use it to find a slow or erroring endpoint.\n- **Pneuma — Ingestion** — job rate by outcome, per-stage throughput, per-stage failure rate, and **per-stage\n  p95 duration**. This is where a stuck or slow ingestion stage becomes obvious — a rising p95 on `Embedding`\n  or `Classification` points straight at the bottleneck (usually model-runner contention).\n- **Pneuma — Chat & Retrieval** — answer rate by outcome, answer p95 latency, and **per-stage p95 latency**\n  (`prompt_rewrite` / `retrieval` / `rerank` / `tool` / `final_inference`). Use it to see whether answer\n  slowness is generation, retrieval, or reranking.\n- **Pneuma — Integrations** — request and error rate by service and the p95 latency per service+operation. When\n  an answer or ingestion is slow, this tells you whether a downstream (RecallDB/PolyPrompt/LiteGraph/DocumentAtom)\n  is the cause.",
          "format": "md",
          "labels": [
            "telemetry"
          ],
          "tags": {}
        },
        {
          "id": "telemetry--workflows-and-notes",
          "category": "telemetry",
          "title": "Pneuma Telemetry Guide — Reading traces / Typical workflows / Notes & extension",
          "summary": "Reading traces in Tempo, troubleshooting recipes for slow or failing paths, and notes on extending metrics.",
          "body": "## 7. Reading traces\n\nFor a single slow request rather than an aggregate:\n\n1. In Grafana, go to **Explore** and pick the **Tempo** datasource.\n2. Search by service/span name or a trace id (chat/query responses and ingestion logs surface correlation\n   ids). Ingestion jobs appear as `ingestion <url>` roots.\n3. Open the trace to see the span waterfall — the ingestion stage or the specific integration call that\n   dominated the time is the widest bar.\n\n## 8. Typical workflows\n\n- **\"Ingestion feels slow.\"** Ingestion row → per-stage p95. The tallest stage is the bottleneck; if it's\n  `Embedding`/`Classification`, check the Integrations row for PolyPrompt (embedding/completion) latency, then open the job's trace.\n- **\"Answers are slow.\"** Retrieval & Answer row → answer p95 + per-stage p95. If `final_inference` dominates\n  it's the model; if `retrieval`/`rerank`, check RecallDB / the rerank model in Integrations.\n- **\"Something is erroring.\"** Overview error ratio → HTTP status-class panel to find the route → Integrations\n  error rate to see if a downstream is failing → the trace for the exact call.\n\n## 9. Notes & extension\n\n- Metrics are per-process and in-memory (reset on restart); Prometheus retains the history. There is no remote\n  write by default.\n- Labels avoid ids to keep cardinality bounded; scope by `tenant`/`subject` only where the set is small.\n- To add a metric, extend `PneumaMetrics` (a `Record*` method + a family in `Render()`); to add a panel, edit\n  the relevant domain dashboard under `assets/grafana/pneuma-*.json` (mirror the change into\n  `docker/factory/assets/grafana/`).",
          "format": "md",
          "labels": [
            "telemetry"
          ],
          "tags": {}
        },
        {
          "id": "ckg-improvements--scoring-and-table",
          "category": "ckg-improvements",
          "title": "CKG Improvements — Overview / Scoring / Prioritized table",
          "summary": "Purpose of the roadmap, the Simplicity/Value scoring scheme, the status table, and the note on items 1–4.",
          "body": "# CKG Improvements — Ingestion & Retrieval\n\nA prioritized set of improvements to Pneuma's document→graph→search ingestion pipeline and its grounded retrieval layer. Recommendations are grounded in the current code: the pipeline is a two-phase orchestration (Categorization → Hydration) driven by `IngestionProcessor`/`IngestionStages`, and retrieval is centralized in `GroundedQueryService` (shared by REST and the MCP tool surface). Several capabilities a mature system needs are **already present** — per-subject prompt-rewrite, optional LLM re-ranking, single-hop graph-neighbor expansion, conversation compaction, cited answers with an explicit \"insufficient support\" refusal, and an LLM-judged eval harness — so the items below deliberately avoid re-litigating those and focus on confirmed gaps.\n\n## Scoring\n\nEach item is scored on two axes, **1–5**:\n\n- **Simplicity** — how easy to implement (5 = small, localized change; 1 = cross-cutting, multi-service effort).\n- **Value** — impact on retrieval quality, graph fidelity, cost, or operability (5 = transformative; 1 = marginal).\n- **Total** = Simplicity + Value (max 10). Ties are ordered by Value, then Simplicity.\n\n## Prioritized table\n\nStatus legend: **✅ Done** · ⬜ Not started.\n\n| # | Item | Simplicity | Value | Total | Status |\n|---|------|:---------:|:-----:|:-----:|:------:|\n| 1 | Reciprocal-rank fusion for hybrid retrieval | 5 | 4 | **9** | ✅ Done |\n| 2 | Automatic retry with backoff on transient ingestion failures | 5 | 3 | **8** | ✅ Done |\n| 3 | Content-hash delta detection & document de-duplication on (re)ingest | 3 | 4 | **7** | ✅ Done (delta-skip; cross-link dedup deferred) |\n| 4 | Human review/approval gate before a graph goes live | 3 | 4 | **7** | ✅ Done (simplified: per-subject \"publish to consumer chat\" toggle) |\n| 5 | Ontology type-validation guardrail | 4 | 3 | **7** | ✅ Done (canonicalize, not reject — preserves custom types) |\n| 6 | Per-subject configurable chunking | 4 | 3 | **7** | ✅ Done (strategy/size/overlap; native `Pneuma.Chunking`) |\n| 7 | First-class vector + hybrid search endpoint | 4 | 3 | **7** | ✅ Done (text / vector / hybrid via `mode`) |\n| 8 | Multi-hop traversal + community detection (GraphRAG-style) | 3 | 5 | **8** | ✅ Done (on LiteGraph 9 traversal + algorithms) |\n| 9 | Fuzzy / embedding-based entity resolution | 2 | 4 | **6** | ⬜ |\n| 10 | Query decomposition + retrieval gate / routing | 2 | 4 | **6** | ⬜ |\n| 11 | MMR / diversity de-duplication of retrieved passages | 3 | 3 | **6** | ✅ Done (MMR over a widened candidate pool) |\n| 12 | Cross-source edge weighting & relationship consolidation | 3 | 3 | **6** | ✅ Done (consolidate + noisy-OR weight) |\n| 13 | Dedicated cross-encoder re-ranker option | 3 | 3 | **6** | ✅ Done (per-subject; global `/rerank` endpoint) |\n| 14 | Embedding cache | 3 | 3 | **6** | ✅ Done (bounded LRU, global size limit) |\n| 15 | True token streaming on the single-shot answer stream | 3 | 3 | **6** | ✅ Done (real token streaming, non-stream fallback) |\n\n> **Implementation note (items 1–4).** Delivered previously. Item 3 ships the content-hash delta-skip (unchanged sources short-circuit re-processing); cross-link document de-duplication remains a follow-up. Item 4 was intentionally reduced from a per-ingestion approval gate to a per-subject `PublishedForChat` attribute (default on) that gates a subject's visibility in the consumer chat experience.\n>",
          "format": "md",
          "labels": [
            "ckg-improvements"
          ],
          "tags": {}
        },
        {
          "id": "ckg-improvements--graphrag-implementation-note",
          "category": "ckg-improvements",
          "title": "CKG Improvements — Prioritized table: item 8 implementation note",
          "summary": "How item 8 (multi-hop traversal and community summaries) was implemented on LiteGraph 9, with new APIs and follow-ups.",
          "body": "> **Implementation note (item 8 — done, built on LiteGraph 9).** The graph store was upgraded to 9.0.0 (compose image pins + the mounted `litegraph.json` migrated to the v9 settings schema). Then #8 was implemented across three layers. **Traversal:** added `GetSubgraphAsync` (wraps v9 `GET .../nodes/{guid}/subgraph?maxDepth=…`), plus `DetectCommunitiesAsync` (wraps `POST .../algorithms` with `AlgorithmType=Louvain`) and `DeleteNodeAsync` to `IGraphRepository` (+ LiteGraph client and fake). Retrieval neighbor-expansion now honours a `NeighborExpansionMaxHops` setting (default 1 = unchanged; >1 uses the bounded server-side subgraph). **Community summaries:** a new `CommunityService` runs Louvain over the tenant graph, groups the subject's entity nodes by community, LLM-summarizes each community above `CommunityMinSize`, and persists each as a `CommunitySummary` graph node (co-located with the graph, cascade-cleaned with the subject; rebuild replaces prior summaries). **Global query mode:** `GroundedQueryService.AnswerGlobalAsync` ranks a subject's community summaries against a thematic question and synthesizes a cited answer. **API:** `POST /v1.0/subjects/{id}/communities/build`, `GET /v1.0/subjects/{id}/communities`, and `POST /v1.0/query/global`. Residual/optional follow-ups (noted, not built): scheduled community recompute (currently operator-triggered via the build endpoint) and auto-routing global-vs-local (overlaps #10); quality still improves with #9. Dashboard buttons for build/global-ask are a thin follow-up.\n>",
          "format": "md",
          "labels": [
            "ckg-improvements"
          ],
          "tags": {}
        },
        {
          "id": "ckg-improvements--litegraph-9-and-later-notes",
          "category": "ckg-improvements",
          "title": "CKG Improvements — Prioritized table: LiteGraph 9 upgrade and items 5–7, 11–15 notes",
          "summary": "What the LiteGraph 9 upgrade added and how items 5–7 and 11–15 were implemented.",
          "body": "> **Graph-store upgrade (LiteGraph 7.0.0 → 9.0.0) and #8 re-score.** The knowledge-graph store was upgraded to LiteGraph 9.0.0 (compose image pins only; the REST CRUD contract Pneuma uses is unchanged, so no client code changed). v9 is an additive release that adds, server-side and tenant-scoped: **depth-limited subgraph extraction** (`GET .../nodes/{guid}/subgraph?maxDepth=N&maxNodes=&maxEdges=`), **route/path finding** (`POST .../routes`), and a **native graph-algorithm surface** (`POST .../algorithms`) covering **Louvain and label-propagation community detection**, PageRank, degree/closeness/betweenness/eigenvector centrality, connected components, clustering coefficient, and k-core — on demand with optional **write-back** (per-node community/score persisted into node data), plus node embeddings and GraphML/edge-list projection export.\n>\n> This removes the hardest parts of #8: multi-hop traversal no longer means client-side BFS with fan-out explosion (one bounded `/subgraph` call), and community detection no longer means loading the whole tenant graph into Pneuma's process and implementing Louvain (one `/algorithms` call with write-back — LiteGraph builds the adjacency and computes it). #8 is re-scored **Simplicity 1 → 3 (Total 6 → 8)**, making it the **highest-value remaining item**. Broken down: the *multi-hop traversal* half is now ~Simplicity 4 (wrap `/subgraph` and `/algorithms` in `IGraphRepository` + use in retrieval); the *community-detection GraphRAG* half is what keeps the combined score at 3 — LiteGraph now **detects and tags** communities, but Pneuma must still add the **hierarchical community-summarization stage** (LLM-summarize each community, persist summaries), a **freshness/recompute trigger** (community structure drifts as documents are ingested; LiteGraph gives the compute button, not the schedule), and a **global-vs-local query router** (overlaps #10). What did *not* change: those residual pieces, and the dependency on clean entities (#9). Runtime note: the mounted `litegraph.json` is v7-era; since v9 is additive it should load with new settings defaulted, but confirm a clean first boot after the image bump.\n>\n> **Implementation note (items 12–14).** **#12** consolidates a re-asserted relationship into a single edge instead of duplicating it, accumulating a corroboration count and a noisy-OR `weight` tag (each corroborating source pushes the weight toward 1.0) — added an `UpdateEdgeAsync` to the graph store to support it. **#13** adds a per-subject reranker type (`LlmListwise` default, or `CrossEncoder`); the cross-encoder calls a globally-configured `/rerank` endpoint (Cohere/Jina/TEI-compatible) and falls back to LLM listwise when unconfigured or on failure. Pneuma's native processing has no rerank capability, so this is a separate HTTP integration. **#14** caches embedding vectors in a bounded, thread-safe LRU (the already-referenced `Caching` package) keyed by (embedding model, content hash), with a **global system size limit** via `IngestionSettings.EmbeddingCacheSize` (0 disables); identical text is served from the cache instead of being re-embedded.\n>\n> **Implementation note (items 5–7, 11, 15).** Delivered in an earlier pass. **#5** canonicalizes emitted node/edge types (coerce known variants to built-ins, normalize custom ones) rather than rejecting them, so the editable-ontology flexibility is preserved while drift is curbed. **#6** exposes per-subject chunking (strategy/size/overlap); the strategy is validated against the native `Pneuma.Chunking` supported enum (`FixedTokenCount`, `SentenceBased`, `ParagraphBased`) so an invalid strategy is never used. **#7** adds a `mode` parameter (`text` | `vector` | `hybrid`, default hybrid) to `/v1.0/search` and `/v1.0/subjects/{id}/search`, served by the shared retrieval service so search and grounded answering can't drift. **#11** applies Maximal-Marginal-Relevance selection over a candidate pool wider than the final `k`, so near-duplicate passages don't crowd the grounding context. **#15** streams real answer tokens over SSE (with a non-streaming fallback) instead of re-chunking a finished answer.",
          "format": "md",
          "labels": [
            "ckg-improvements"
          ],
          "tags": {}
        },
        {
          "id": "ckg-improvements--items-1-4",
          "category": "ckg-improvements",
          "title": "CKG Improvements — Items 1–4 (RRF, retry, content-hash delta, review gate)",
          "summary": "Proposals for rank fusion, transient-failure retry, content-hash delta detection, and a review gate.",
          "body": "## 1. Reciprocal-rank fusion for hybrid retrieval\n\n**Total 9 (Simplicity 5, Value 4)**\n\n**Today.** `GroundedQueryService.RetrieveSourcesAsync` runs full-text and vector search independently, then merges them with a naive dedup-union: full-text hits are appended first, vector hits second, and duplicates are dropped by graph-node id. There is no score normalization and no rank fusion, so the two channels never actually combine their evidence — a chunk ranked #1 by vector similarity and #2 by lexical match is treated identically to one that appears in a single channel, and ordering is effectively \"whichever list we concatenated first.\"\n\n**Do.** Replace the dedup-union with **Reciprocal-Rank Fusion**: `score(d) = Σ 1/(k + rank_i(d))` across the two ranked lists (k≈60), then sort by fused score. Optionally expose per-subject fusion weights (lexical vs. semantic) alongside the existing retrieval settings. RRF needs no score calibration — it operates purely on ranks — which is why it is both cheap and robust.\n\n**Where.** `GroundedQueryService.RetrieveSourcesAsync`; add a small pure fusion helper. Keep the existing `OrderForReconstruction` document-order pass as a post-fusion step for the answer context. This is a localized change behind an already-centralized retrieval method.\n\n## 2. Automatic retry with backoff on transient ingestion failures\n\n**Total 8 (Simplicity 5, Value 3)**\n\n**Today.** `IngestionJob` already carries an `AttemptCount` field, but nothing increments or acts on it: any stage failure is terminal until an operator manually calls the job restart endpoint. Transient faults in the extraction, chunk/embed, or store services (timeouts, 429s, brief unavailability) permanently fail an otherwise-healthy job.\n\n**Do.** In `IngestionWorkerService` / `IngestionProcessor`, on a **transient** failure (distinguish from `JobCancelledException`, `Unknown`-type, and zero-cell hard fails, which should stay terminal), increment `AttemptCount` and requeue with exponential backoff up to a configurable `MaxAttempts`. Only mark `Failed` once attempts are exhausted. The stage classification already exists in `ProcessAsync`; this reuses it.\n\n**Where.** `IngestionProcessor.ProcessAsync` failure branch; `IngestionWorkerService` claim/requeue loop; a `MaxAttempts` + base-delay setting. The field and error taxonomy are already in place, making this nearly free.\n\n## 3. Content-hash delta detection & document de-duplication on (re)ingest\n\n**Total 7 (Simplicity 3, Value 4)**\n\n**Today.** Reingestion (`restart`) fully reprocesses a source every time; there is no content hashing. Identical or unchanged sources are re-extracted, re-classified, re-chunked, and re-embedded from scratch, and two links pointing at the same bytes both pay full ingestion cost and can create redundant graph/vector entries.\n\n**Do.** After content retrieval, compute a stable hash of the fetched bytes (and optionally of the extracted-cell set). Persist it on the job/link. On (re)ingest, if the hash is unchanged, short-circuit to `Done` (or skip straight to indexing) instead of rebuilding. Across links, detect identical hashes to skip duplicate graph merges. This is the foundation the scheduled-ingestion and cost-reduction items build on.\n\n**Where.** Content-retrieval stage in `IngestionStages`; a hash column on `IngestionJob`/`SubjectLink`; a guard early in `IngestionProcessor`. Cascade-deletion semantics already track per-job contributions, so dedup can be reconciled cleanly.\n\n## 4. Human review/approval gate before a graph goes live\n\n**Total 7 (Simplicity 3, Value 4)**\n\n**Today.** The pipeline is fully auto-approved: Categorization flows straight into Hydration and the merged graph becomes queryable immediately. There is no seam for an operator to inspect what will be asserted before it lands, which matters in any domain where a wrong entity or edge can surface in an executive-facing answer.\n\n**Do.** Make the **candidate subgraph** — already produced as a discrete artifact between the two phases — an optional approval checkpoint. Add a per-subject \"require approval\" toggle; when on, the job pauses after Categorization in a `PendingReview` state, surfaces the candidate nodes/edges in the dashboard, and only proceeds to Hydration (GraphMerge onward) on approve. Reject discards the candidate. Because Categorization and Hydration are already separate phases with a persisted candidate artifact, the seam exists; this adds a state and a UI.\n\n**Where.** `IngestionProcessor` (split the phase transition on a subject setting), a new job status, the candidate-subgraph artifact (already persisted), and a dashboard review view.",
          "format": "md",
          "labels": [
            "ckg-improvements"
          ],
          "tags": {}
        },
        {
          "id": "ckg-improvements--items-5-7",
          "category": "ckg-improvements",
          "title": "CKG Improvements — Items 5–7 (ontology guardrail, per-subject chunking, hybrid search endpoint)",
          "summary": "Proposals for ontology type validation, per-subject chunking, and a vector/hybrid search endpoint.",
          "body": "## 5. Ontology type-validation guardrail\n\n**Total 7 (Simplicity 4, Value 3)**\n\n**Today.** The ontology is defined in natural language and the classifier's emitted node/edge type names are accepted verbatim as graph labels. Any type the model invents (a near-synonym, a typo, an off-ontology label) becomes a first-class type, so the ontology silently drifts and downstream tag/label facets fragment.\n\n**Do.** Validate each candidate node/edge `nodeType`/`edgeType` against the ontology's known set in `Ontology`. Coerce close matches to the canonical type (case-insensitive + a small alias map), and drop or quarantine truly unknown types with a journal warning. Optionally feed the closed type list into the classifier prompt's output contract so the model is constrained up front. Keeps the \"editable prompt\" flexibility while stopping uncontrolled drift.\n\n**Where.** `SubgraphMerger.MergeAsync` (validation pass over the candidate subgraph) and/or the classifier output-contract construction; `Ontology` as the source of truth.\n\n## 6. Per-subject configurable chunking\n\n**Total 7 (Simplicity 4, Value 3)**\n\n**Today.** Chunking is hard-coded to a fixed 256-token window with 32-token overlap for every subject and document type. The downstream chunk/embed service supports other strategies, but none are exposed — so a subject full of short FAQ entries and one full of long technical PDFs are chunked identically, hurting retrieval granularity on both ends.\n\n**Do.** Surface the chunking configuration (strategy, target size, overlap) as **per-subject settings** layered over a global default, mirroring how embedding/inference models and prompt overrides are already per-subject. Pass the resolved config through to the chunk request instead of the hard-coded literal.\n\n**Where.** Subject settings model + edit modal; the chunk-request construction in the chunking client wrapper; thread the value from the job/subject through `IngestionStages`.\n\n## 7. First-class vector + hybrid search endpoint\n\n**Total 7 (Simplicity 4, Value 3)**\n\n**Today.** The standalone search route (`GET /v1.0/search` and the per-subject variant) is **full-text only**. Vector and hybrid search are only exercised inside the grounded-answer path, so external consumers and the dashboards cannot run a semantic or fused search without triggering full answer generation and its LLM cost.\n\n**Do.** Expose vector and hybrid (RRF, from item 1) modes on the search routes via a `mode` parameter, reusing `GroundedQueryService.RetrieveSourcesAsync`. Return ranked hits with scores, provenance tags, and source rollups — no answer synthesis. This makes retrieval independently testable and useful to downstream systems.\n\n**Where.** `SearchRoutes`; delegate to the existing retrieval method rather than duplicating logic.",
          "format": "md",
          "labels": [
            "ckg-improvements"
          ],
          "tags": {}
        },
        {
          "id": "ckg-improvements--item-8-graphrag",
          "category": "ckg-improvements",
          "title": "CKG Improvements — Item 8: Multi-hop traversal + community detection (GraphRAG-style)",
          "summary": "Phased proposal for multi-hop traversal, community detection, and a global query mode.",
          "body": "## 8. Multi-hop traversal + community detection (GraphRAG-style)\n\n**Total 8 (Simplicity 3, Value 5)** — re-scored up from Simplicity 1 / Total 6 after the LiteGraph 7.0.0 → 9.0.0 upgrade.\n\n**Today.** Graph use at query time is limited to **single-hop** neighbor expansion (`IGraphRepository.GetNeighborsAsync`), and Pneuma exposes no traversal/path/subgraph primitives beyond it. There is no community detection or hierarchical summarization, so the system cannot answer global/thematic questions (\"what are the main themes across this subject?\") — only local ones anchored to retrieved chunks. **However, the graph store (LiteGraph 9.0.0) now provides the heavy machinery server-side**: bounded depth-limited subgraph extraction (`GET .../nodes/{guid}/subgraph?maxDepth=N&maxNodes=&maxEdges=`), path finding (`POST .../routes`), and a native graph-algorithm surface (`POST .../algorithms`) including Louvain and label-propagation community detection with optional per-node write-back. So the missing pieces are now integration and orchestration, not algorithm implementation.\n\n**Do (phased).**\n1. **Multi-hop traversal (now ~Simplicity 4).** Add `GetSubgraphAsync` (and, if useful, `FindRoutesAsync`) to `IGraphRepository`, wrapping LiteGraph's `/subgraph` (and `/routes`) endpoints — a bounded server-side call, no client-side BFS. Let retrieval expand beyond one hop along high-weight edges (pairs with item 12's weights for pruning).\n2. **Community detection (now a call, not an implementation).** Add `RunAlgorithmAsync` wrapping `POST .../algorithms` with `AlgorithmType=Louvain` (or label propagation) and `WriteBack=true`, so LiteGraph computes communities over the whole tenant graph and tags each node with its community id. Trigger it on a schedule / after ingestion rather than per document.\n3. **Hierarchical summarization + global query mode (the residual effort).** Group nodes by community tag, LLM-summarize each community, persist the summaries, keep them fresh as the graph changes, and add a global query mode that answers thematic questions from community summaries — routed to by item 10.\n\n**Where.** `IGraphRepository` + `LiteGraphClient` (thin wrappers over the v9 `/subgraph`, `/routes`, `/algorithms` endpoints) + the graph fake; a scheduled/triggered community-recompute + a new summarization stage with its own storage; `GroundedQueryService` for the global mode. **Re-scored Simplicity 3** (was 1): LiteGraph 9 eliminates the two hardest parts — the traversal primitive and the whole-graph-in-RAM algorithm implementation — leaving the summarization pipeline, recompute freshness, and query routing (which overlaps item 10). Still gated on clean entities (item 9) for quality.",
          "format": "md",
          "labels": [
            "ckg-improvements"
          ],
          "tags": {}
        },
        {
          "id": "ckg-improvements--items-9-11",
          "category": "ckg-improvements",
          "title": "CKG Improvements — Items 9–11 (fuzzy entity resolution, query routing, MMR)",
          "summary": "Proposals for fuzzy entity resolution, query decomposition/routing, and MMR diversity.",
          "body": "## 9. Fuzzy / embedding-based entity resolution\n\n**Total 6 (Simplicity 2, Value 4)**\n\n**Today.** Entity resolution during merge is an **exact `(nodeType, canonicalName)` string match** within a subject. Any spelling, casing, abbreviation, or phrasing variance (\"IBM\" vs \"I.B.M.\" vs \"International Business Machines\") creates a distinct node, so duplicate/variant entities proliferate and fragment the graph and its provenance.\n\n**Do.** Augment exact matching with (a) a normalized/alias lookup and (b) an **embedding-similarity** candidate search over existing same-type node names, with a threshold above which the candidate is treated as the same entity (and optionally recording surface variants as aliases). Keep exact match as the fast path; fall through to fuzzy only on miss.\n\n**Where.** `SubgraphMerger` resolution step; `IGraphRepository.FindNodeByCanonicalAsync` gains a similarity-search sibling. Requires an embedding call per unresolved entity, hence Simplicity 2.\n\n## 10. Query decomposition + retrieval gate / routing\n\n**Total 6 (Simplicity 2, Value 4)**\n\n**Today.** Query understanding is a single-shot rewrite only. Every question takes the same path: there is no decomposition of multi-part questions, no multi-query expansion, and no gate to skip retrieval for conversational turns that need none — so simple chit-chat pays retrieval cost and complex compound questions get one blended retrieval instead of targeted sub-retrievals.\n\n**Do.** Add a lightweight **router/gate** ahead of retrieval: classify the turn as (a) no-retrieval, (b) single retrieval, (c) decompose-into-sub-questions (retrieve per sub-question, then synthesize), or (d) global/thematic (route to item 8's community mode). Start with the gate + simple decomposition; expand later.\n\n**Where.** `GroundedQueryService` (a pre-retrieval routing step) and/or `AgenticChatService`, which already runs a tool-calling loop and is a natural home for decomposition. Reuses the per-subject rewrite model.\n\n## 11. MMR / diversity de-duplication of retrieved passages\n\n**Total 6 (Simplicity 3, Value 3)**\n\n**Today.** Retrieved chunks are ordered and reconstructed but never de-duplicated by semantic similarity. Near-identical passages (common with overlapping chunks and repeated boilerplate across sources) can fill the answer context, crowding out diverse evidence and wasting the token budget.\n\n**Do.** Apply **Maximal Marginal Relevance** (or a similarity-threshold cluster-and-pick) over the fused candidate set before building the answer context, balancing relevance against novelty so the model sees diverse support rather than the same point five times.\n\n**Where.** `GroundedQueryService.RetrieveSourcesAsync`, after fusion (item 1) and before `OrderForReconstruction`. Uses the chunk embeddings already retrieved.",
          "format": "md",
          "labels": [
            "ckg-improvements"
          ],
          "tags": {}
        },
        {
          "id": "ckg-improvements--items-12-15-and-sequencing",
          "category": "ckg-improvements",
          "title": "CKG Improvements — Items 12–15 (edge weighting, cross-encoder, embedding cache, token streaming) / Suggested sequencing",
          "summary": "Proposals for edge weighting, a cross-encoder reranker, an embedding cache, real token streaming, and the suggested order.",
          "body": "## 12. Cross-source edge weighting & relationship consolidation\n\n**Total 6 (Simplicity 3, Value 3)**\n\n**Today.** Every ingestion job asserts its own edges, deduped only structurally by reference within that job. Confidence is stored per assertion but relationships are **not weighted or consolidated across sources**, so an edge corroborated by twenty documents looks the same as one asserted once, and retrieval/traversal has no signal for which relationships are strongly supported.\n\n**Do.** On merge, when an edge between two resolved nodes already exists, **consolidate** rather than duplicate: accumulate a weight from corroboration count and per-assertion confidence (and optionally an LLM-scored strength). Expose the weight so multi-hop traversal (item 8) and neighbor expansion prefer strong edges.\n\n**Where.** `SubgraphMerger` edge-creation path (upsert-with-weight instead of create); an edge-weight tag; consumed by `GetNeighbors`/traversal ordering.\n\n## 13. Dedicated cross-encoder re-ranker option\n\n**Total 6 (Simplicity 3, Value 3)**\n\n**Today.** Re-ranking exists but is an **LLM listwise reorder** using the subject's answering-class model over truncated passage snippets — effective but comparatively slow and costly, and it competes for the same model runner. There is no option for a purpose-built re-ranker.\n\n**Do.** Add a re-ranker **model type** (cross-encoder / dedicated reranking endpoint) as an alternative to the LLM listwise path, selectable per subject alongside the existing reranking model setting. Cross-encoders give better precision-per-dollar and lower latency for the top-k reorder.\n\n**Where.** `GroundedQueryService.RerankAsync` (strategy branch on reranker type); subject settings; a reranking client wrapper. The re-rank seam and per-subject model plumbing already exist.\n\n## 14. Embedding cache\n\n**Total 6 (Simplicity 3, Value 3)**\n\n**Today.** There is no embedding cache: the same chunk text is re-embedded on every reingest, and identical text across documents is embedded repeatedly. Embedding is a per-batch network + compute cost paid needlessly.\n\n**Do.** Cache embeddings keyed by `(embedding-model, content-hash)`. On embed, look up first and only send cache misses to the embed service. Pairs directly with the content-hashing from item 3 and sharply reduces reingest cost.\n\n**Where.** The embedding stage in `IngestionStages`; a cache store (table or object store) keyed by model + hash.\n\n## 15. True token streaming on the single-shot answer stream\n\n**Total 6 (Simplicity 3, Value 3)**\n\n**Today.** The single-shot answer stream endpoint generates the **full answer first**, then re-chunks it client-side into fake SSE deltas — so \"streaming\" has the same time-to-first-token as the non-streaming call. (The agentic chat path already streams tokens for real, so the building blocks exist.)\n\n**Do.** Wire the single-shot stream endpoint to the model runner's **real token stream** (as the agentic path does), emitting deltas as generated and reusing the existing `<think>`-separation handling. Materially improves perceived latency.\n\n**Where.** The answer-stream route and `GroundedQueryService` generation call; reuse the token-streaming plumbing already present in the agentic chat service.\n\n---\n\n## Suggested sequencing\n\n- **Quick wins first (items 1, 2, 5, 7, 15):** small, localized changes that improve retrieval quality, resilience, and API completeness with minimal risk.\n- **Cost & correctness (items 3, 6, 14, 11, 13):** hashing/dedup, configurable chunking, embedding cache, diversity, and a cheaper re-ranker — mostly independent, several share the content-hash foundation.\n- **Graph fidelity (items 4, 9, 12):** approval gate, better entity resolution, and edge consolidation — these compound, and 9+12 make item 8 far more valuable.\n- **Strategic (items 8, 10):** GraphRAG traversal/community detection and query routing — highest ceiling, highest effort; sequence last and build on the graph-fidelity work.",
          "format": "md",
          "labels": [
            "ckg-improvements"
          ],
          "tags": {}
        },
        {
          "id": "claude--overview",
          "category": "claude",
          "title": "Contributor & Agent Guide (CLAUDE.md) — What Pneuma is",
          "summary": "One-paragraph description of Pneuma's stack and pointers to the plan and API docs.",
          "body": "# Pneuma — Contributor & Agent Guide\n\nPneuma (Pneuma - information brought to life) is a subject knowledge-graph platform. This file captures the non-negotiable conventions for working in this repo. It mirrors the standards in `C:\\code\\agents\\requirements`; where this file and those documents disagree, the requirement documents win.\n\n## What Pneuma is\n\nA C# (Watson 7.1) backend + Postgres control plane + LiteGraph knowledge graph, with a document→graph→search ingestion pipeline built from DocumentAtom, RecallDB, and PolyPrompt (with in-process chunking via `Pneuma.Chunking` and in-process embedding/summarization via PolyPrompt), plus three React/Vite dashboards (admin, subject, user). See `PNEUMA_PLAN.md` for the phased plan and progress checkboxes, and `REST_API.md` for the API.",
          "format": "md",
          "labels": [
            "claude"
          ],
          "tags": {}
        },
        {
          "id": "claude--code-style",
          "category": "claude",
          "title": "Contributor & Agent Guide (CLAUDE.md) — Backend C# code style",
          "summary": "Strict C# style rules: typing, file layout, naming, docs, async, disposal, exceptions, logging, JSON.",
          "body": "## Backend C# code style (STRICT)\n\n- **No `var`.** Use the actual type.\n- **No tuples.** Set response state / return typed objects instead.\n- **No partial classes.** One class or one enum per file.\n- `namespace` first; `using` directives **inside** the namespace block. System/Microsoft usings first (alphabetical), then others (alphabetical).\n- XML documentation on every public class, constructor, property, method, and enum. No doc comments on private members.\n- Public members `LikeThis`; private members `_LikeThis` (underscore + PascalCase, never `_likeThis`).\n- Properties with range/null rules use explicit getters/setters over a backing field; throw `ArgumentNullException` for required nulls; `Math.Clamp` numeric ranges.\n- Every async method that does cancellable work takes a `CancellationToken`. Every `await` in library/server code uses `.ConfigureAwait(false)`.\n- Classic `using (...) { }` blocks, not `using` declarations. Full dispose pattern where resources are held.\n- Specific exception types with meaningful messages; document thrown exceptions with `/// <exception>`.\n- **No `Console.WriteLine` in library code** — use SyslogLogging.\n- No `System.Text.Json` DOM types (`JsonElement`, `JsonNode`, …) for fixed contracts — define typed DTOs. JSON columns only for genuinely schemaless properties, named `*Json`.\n- `Nullable` enabled; guard-clause inputs; prefer `.Any()`, `.FirstOrDefault()` with null checks.\n- Region order when used: `Public-Members`, `Private-Members`, `Constructors-and-Factories`, `Public-Methods`, `Private-Methods` (optional under 500 lines).",
          "format": "md",
          "labels": [
            "claude"
          ],
          "tags": {}
        },
        {
          "id": "claude--backend-architecture",
          "category": "claude",
          "title": "Contributor & Agent Guide (CLAUDE.md) — Backend architecture",
          "summary": "Server bootstrapping, routing hooks, auth context, IDs, provider-neutral data layer, tenancy, migrations, request capture.",
          "body": "## Backend architecture\n\n- Thin `Program.cs` → `Bootstrapper` → `PneumaServer` (Watson host). Feature route registrars call `server.Routes.{Pre,Post}Authentication.{Static,Parameter}.Add(...)`. Mandatory `Preflight` (CORS OPTIONS) and `PostRouting` (timestamp, CORS, request capture) hooks. `Server.UseOpenApi()`.\n- Auth via the `AuthenticateRequest` hook building a typed `RequestContext` stashed in `ctx.Metadata`.\n- `PrettyId` prefixed IDs via `Helpers/IdGenerator.cs`; prefixes centralized in `Constants.cs`.\n- Provider-neutral data layer: `DatabaseDriverBase` + `DatabaseDriverFactory` + domain `I*Methods` interfaces; provider folders `Sqlite`, `Mysql`, `Postgresql`, `SqlServer` (exact casing), each with `Implementations/` and `Queries/`. Handwritten SQL. Structured columns/child tables — never BLOB-as-JSON for known shapes.\n- Multi-tenant: every tenant-owned entity carries `TenantId`; queries always scope by tenant; composite unique indexes `(tenant_id, …)`.\n- Versioned, idempotent, tracked migrations (`schema_migrations`). First-boot seeding is idempotent.\n- SQLite writes serialized with `SemaphoreSlim`.\n- Request capture enabled by default (`/v1.0/api/request-history`), secrets redacted, bodies truncated, tenant-aware, retention-pruned.\n- Loopback is `127.0.0.1`, never `localhost`.",
          "format": "md",
          "labels": [
            "claude"
          ],
          "tags": {}
        },
        {
          "id": "claude--auth-and-frontend",
          "category": "claude",
          "title": "Contributor & Agent Guide (CLAUDE.md) — Auth model / Frontend",
          "summary": "RBAC entity model and evaluation order, session tokens, and the dashboard technology and UX rules.",
          "body": "## Auth model\n\nFull RBAC per `AUTHENTICATION.md`: accounts, tenants, admins, users, credentials, authsessions, userroles, userroleassignments, userrolemaps (legacy), credentialscopeassignments, permissions, rolepermissionmaps, audit. Dashboards log in with email/password → AES-256 opaque session token (random IV) presented as `Authorization: Bearer`. API-key credentials (`access_`/`secret_`) also present as bearer. `IsAdmin` (= Pneuma.md IsSystemAdmin) and `IsTenantAdmin` bypass RBAC per the documented order; all denials + bypasses are audited. Explicit deny > permit > implicit deny.\n\n## Frontend (three dashboards)\n\nReact 19 + Vite 6 + React Router 7, hand-rolled fetch `ApiClient` (no axios), i18next runtime, hand-rolled SVG `ActivityChart` (no charting lib). Required operator surfaces where the role warrants: Home, Request History (+ inspector modal), OpenAPI-driven API Explorer, Settings. Custom confirm modals (never browser `confirm`). Light/dark themes; responsive at 1280/768/390. External links from the user dashboard open in a new tab. See `FRONTEND_ARCHITECTURE.md` and `DASHBOARD_STYLE_AND_USABILITY.md`.",
          "format": "md",
          "labels": [
            "claude"
          ],
          "tags": {}
        },
        {
          "id": "claude--integration-contracts",
          "category": "claude",
          "title": "Contributor & Agent Guide (CLAUDE.md) — Integration contracts / Testing / Build",
          "summary": "Confirmed contracts for DocumentAtom, native processing, RecallDB, and per-tenant LiteGraph, plus test/build commands.",
          "body": "## Integration contracts (confirmed from source)\n\n- **DocumentAtom** `:8000`, no auth, no version prefix. `POST /typedetect` (raw bytes) → `TypeResult`; `Type:\"Unknown\"` returns 200 — fail ingestion on it. `POST /atom/{type}` with `{ \"Settings\": null, \"Data\": \"<base64>\" }` → `Atom[]`.\n- **Native processing (no external service).** Embedding, chunking, summarization, and model-endpoint management are **native to Pneuma** — chunking runs in-process via the `Pneuma.Chunking` library (cl100k_base + BERT tokenizers), and embedding/summarization/completions run in-process via **PolyPrompt** across all supported providers. Model endpoints live in the `modelrunners` store (`/v1.0/model-runners`), carry a `provider` + provider-specific fields + write-only secrets, and are no longer proxied to any external service.\n- **RecallDB** `:8600`, Bearer (`recalldbadmin`/`default`), Postgres+pgvector. Retrieval store: bring-your-own-vectors (no embedding/completion API). RecallDB is the authority for tenants and collections; Pneuma relays and stores no local collection state. Each Pneuma tenant maps to a RecallDB tenant of the same id — creating a Pneuma tenant (and first-boot) provisions the RecallDB tenant + a default collection via `TenantProvisioningService`/`ITenantProvisioner` (best-effort, idempotent). Collection admin + retrieval are tenant-scoped (`ICollectionStore`/`IVectorRepository`/`IInvertedIndex` take a `tenantId`); a collection's `dimensionality` is fixed at creation. Per-chunk documents carry content + embedding + tags; `PUT /v1.0/tenants/{tid}/collections`, `POST .../collections/{cid}/documents/batch`, `POST .../collections/{cid}/search` (Vector CosineSimilarity + FullText TsRank + TagFilter), `POST .../documents/delete/filter`. Store LiteGraph node ID in `Tags.litegraphNodeId`; it round-trips on search hits. Ingestion requires a collection; delete-by-`jobId`-tag on cascade.\n- **LiteGraph** `:8701`. Knowledge graph store; node/edge labels + tags carry metadata, provenance, rights, authority. **Per-tenant:** each Pneuma tenant gets its own isolated LiteGraph tenant + graph (created/hydrated at tenant provisioning; GUIDs recorded on the Pneuma `Tenant` as `LiteGraphTenantGuid`/`LiteGraphGraphGuid`). Graph operations resolve a per-tenant client via `IGraphRepositoryFactory.ForTenantAsync(tenantId)` (falls back to the configured default tenant/graph when unprovisioned) — never share one LiteGraph tenant across Pneuma tenants.\n\n## Testing\n\nTouchstone descriptors in `Test.Shared` (no console output), run via `Test.Automated` (CLI), `Test.Xunit`, `Test.Nunit`. Shared DB contract suites run against all four providers. Bind test servers to `127.0.0.1`.\n\n## Build\n\n- `dotnet build src/Pneuma.sln`\n- `dotnet run --project src/Test.Automated`\n- Dashboards: `npm ci && npm run build` in each dashboard dir.\n- Do not commit or push unless explicitly asked.",
          "format": "md",
          "labels": [
            "claude"
          ],
          "tags": {}
        },
        {
          "id": "changelog--unreleased-latest",
          "category": "changelog",
          "title": "Changelog — [Unreleased]: live view, fixes, classification batching, Partio removal",
          "summary": "Newest entries: live ingestion view, permit-leak and timeout fixes, classification batching, Partio removal, providers, subject prompts.",
          "body": "# Changelog\n\nAll notable changes to Pneuma (Pneuma - information brought to life) are documented here. The format follows\n[Keep a Changelog](https://keepachangelog.com/). Pneuma is in its `0.x` alpha series: anything may change\nbetween releases, and the project will adopt semantic versioning at its stable 1.0 release.\n\n## [Unreleased]\n\n### Added\n- **Ingestion Live view.** A real-time pipeline view (admin dashboard) and endpoint\n  `GET /v1.0/jobs/live` → `IngestionLiveSnapshot` `{ running, waitingForSlot, queued }`. Each entry\n  carries the document (`sourceUrl`), the current step (`stage`), and `stateSinceUtc` so the view\n  ticks the time-in-state live: which documents are running which step now, which steps are waiting\n  for a per-stage concurrency slot, and what is queued to start.\n\n### Fixed\n- **Ingestion no longer wedges on leaked concurrency permits.** Jobs could get stuck \"waiting for a\n  slot\" when a contended stage's acquire was abandoned (the \"waiting\" event was written before the\n  acquire was awaited, so a failure there orphaned the about-to-be-granted permit — fatal at a\n  per-stage cap of 1). The stage runner now always awaits and releases the permit; the third-party\n  Padlock limiter was replaced with a self-contained `SemaphoreSlim`-based gate (cancelled waits\n  provably hold no permit; releasing is idempotent); and the worker no longer passes the cancellation\n  token to `Task.Run`, which could skip the `finally` that returns a job-pool slot.\n- **Classification no longer times out on large documents.** Completion calls are no longer hard-capped at the\n  model endpoint's `MaximumTimeoutMs` (default 60s), which was cancelling slow ontology-classification calls\n  mid-flight and failing the job after its retries; ingestion model calls are bounded by the per-stage timeout\n  instead, and query calls by the request lifecycle.\n\n### Added\n- **Classification batching (per-subject and system-default tunables).** A document with more than\n  `classificationBatchSize` cells is now classified in independent, bounded batches instead of one enormous\n  model call — each batch reads `classificationBatchOverlap` context cells on **both** sides so a relationship\n  spanning a batch boundary is still detected, and the partial subgraphs are merged (duplicates deduped) by the\n  graph-merge stage. `classificationBatchConcurrency` bounds how many batches from one document run at once. All\n  three are exposed on `IngestionTuning` (`GET|PUT /v1.0/settings/ingestion`) and per-subject\n  `concurrencyOverrides`, editable live from the admin and subject dashboards. Schema migration 27 applies\n  automatically on startup.\n\n### Removed\n- **Removed Partio; embedding, chunking, summarization, and model-endpoint management are now native.** The\n  external Partio service (`:8400`, chunking/embedding/summarization) has been dropped from the stack\n  entirely. Chunking now runs in-process in the new **`Pneuma.Chunking`** library (cl100k_base and BERT\n  tokenizers), and embedding and summarization run in-process via **PolyPrompt**. Pneuma manages model\n  endpoints natively (the `modelrunners` store and the unchanged `/v1.0/model-runners` routes) instead of\n  proxying them to Partio. The `partio-server` / `partio-dashboard` containers and the Partio integration\n  endpoint in `pneuma.json` are gone.\n\n### Added\n- **All nine PolyPrompt providers for embeddings and completions.** Model endpoints support **OpenAI,\n  OpenAICompatible, Gemini, Ollama, AzureOpenAI, Anthropic (completions only), Bedrock, VoyageAI (embeddings\n  only), and VertexAI**. Model-endpoint create/update now takes a `provider` plus provider-specific fields\n  (`deployment`, `apiVersion`, `region`, `project`, `accessKeyId`) and write-only secrets (`apiKey`,\n  `secretAccessKey`, `sessionToken`, never returned on read).\n- **Per-subject prompt system with operator-selectable merge.** New routes `GET /v1.0/subjects/{id}/prompts`,\n  `PUT /v1.0/subjects/{id}/prompts/{key}`, and `DELETE /v1.0/subjects/{id}/prompts/{key}` let an operator\n  override any keyed prompt per subject with an **Append** or **Replace** merge mode over the global default;\n  a subject with no override falls back to the global prompt.\n- **Provider dropdown in the admin dashboard Model Endpoints UI**, with the provider-specific fields and\n  write-only secret inputs surfaced per selected provider.\n- **New `Pneuma.Chunking` library** — in-process document chunking with cl100k_base and BERT tokenizers,\n  replacing Partio's chunking stage.\n\n### Changed\n- **Schema migrations 19 (model-runner provider fields) and 20 (`subjectprompts`)** apply automatically on\n  startup.\n- **PolyPrompt upgraded to 2.6.0.**",
          "format": "md",
          "labels": [
            "changelog"
          ],
          "tags": {}
        },
        {
          "id": "changelog--unreleased-added-1",
          "category": "changelog",
          "title": "Changelog — [Unreleased] Added (part 1: MCP, conversations, eval, labels)",
          "summary": "Added: broader MCP tools and facets, Conversations view, warm-up and auto titles, thread switcher, async eval, ingestion labels/tags.",
          "body": "### Added\n- **Broader MCP write/management surface + facet discovery.** New MCP tools make the platform fully operable by\n  an agent, each RBAC-mapped exactly like its REST twin: conversation threads `pneuma_get_thread` /\n  `pneuma_delete_thread`; the eval harness `pneuma_enumerate_eval_facts` / `pneuma_create_eval_fact` /\n  `pneuma_delete_eval_fact` / `pneuma_start_eval_run` / `pneuma_cancel_eval_run` / `pneuma_delete_eval_run`;\n  retrieval-facet discovery `pneuma_distinct_labels` / `pneuma_distinct_tags`; and the admin/observability\n  surfaces `pneuma_enumerate_request_history` / `pneuma_get_request_history` / `pneuma_request_history_summary`,\n  `pneuma_get_settings` (secrets redacted), and `pneuma_enumerate_model_runner_health` /\n  `pneuma_get_model_runner_health` (the last four system-administrator only). Facet discovery is also exposed\n  over REST: `GET /v1.0/subjects/{id}/retrieval/labels` and `.../tags` return the distinct labels and tag\n  key/values an operator has applied to a subject's content (a bounded aggregate over its links), so the Ask\n  **Scope** filter and an agent's `metadataFilter` can pick real values.\n- **Conversations management view.** Every dashboard gains a dedicated **Conversations** page listing every\n  conversation, separate from the in-Ask switcher. On the admin and creator dashboards it is a first-class\n  table like every other: **Subject** and **User** filter dropdowns, sortable/paginated columns (title,\n  subject, user, id, last activity), a per-row **context menu** (Open / Rename / Delete), and the standard\n  table chrome (page-size, first/prev/next, jump-to-page, total count, refresh). The user dashboard gets the\n  same list reachable from a top-bar **Conversations** button, and every dashboard's in-Ask switcher links to\n  the full list (\"View all conversations →\"). Opening a row rehydrates that conversation in Ask\n  (`?thread=<id>` deep link).\n- **Ask-page model warm-up + summarized chat titles + composer refinements.** Opening an Ask/chat page (or\n  selecting a subject) now fires a best-effort `POST /v1.0/warmup` that issues a minimal completion to the\n  subject's answering model, so the first question isn't slow to first token (Ollama cold-load). A new\n  conversation is now re-titled by the model: once a turn's prompt + response exceeds 500 characters, the\n  answering model summarizes it into a short chat title shown in the Conversations list (otherwise the\n  first-question title stands). The Conversations list shows rename/delete on every row (no longer hover-only)\n  so deleting a conversation is always discoverable. The **Scope** control moved into the composer's input row\n  as a filter icon (same size as send/stop, positioned between them) instead of a separate row.\n- **Conversation thread switcher.** All three dashboards' Ask/chat surfaces gain a conversation switcher: a\n  dropdown listing the subject's recent threads (title + last-activity) with select-to-rehydrate (a past\n  conversation's turns — answers, citations, thinking, and per-turn stats — are reloaded into the chat),\n  \"New conversation\", inline rename, and inline delete-with-confirm. The admin + creator **History** views add\n  a Conversation column and filter (turns grouped by thread). Backed by the existing `/v1.0/threads` CRUD and\n  `threadId`-on-turn plumbing; the switcher is a shared self-contained component so it drops into each\n  dashboard's chrome. (Completes the deferred UI half of the conversation-threads feature.)\n- **Async, cancellable eval runs with live progress.** RAG evaluation runs are no longer synchronous:\n  `POST /v1.0/eval/runs` now **queues** a `Pending` run and returns immediately; a background\n  `EvalWorkerService` claims it (`EvalRuns.ClaimNextQueuedAsync`, all four providers), answers + LLM-judges each\n  fact, and persists per-fact tallies as it goes — each fact admitted through the shared model-runner gate so\n  eval yields to interactive query/chat traffic. New `GET /v1.0/eval/runs/{id}/stream` streams live progress\n  over SSE (metadata → per-fact `result` → `progress` → `complete`), and `POST /v1.0/eval/runs/{id}/cancel`\n  stops a queued/running run (observed between facts; progress writes are guarded so a cancel is never\n  clobbered). The Eval dashboards (admin + creator) gain a **live progress modal** (progress bar + streaming\n  per-fact pass/fail + pass/partial/fail tallies) with cancel, a results view filterable by\n  category/verdict/failure-mode with an aggregate pass rate, run **status pills**, per-row cancel, and\n  bulk-delete. (Completes the deferred async/SSE half of the eval-harness feature.)\n- **Ingestion labels & tags (scoped retrieval).** Content links can now carry operator-supplied **labels**\n  (plain strings) and **tags** (key/value) at submission — single (`POST /v1.0/subjects/{id}/links`) and bulk\n  (`.../links/bulk`, applied to every URL). They are persisted on the link and its ingestion job (schema v15,\n  `labelsjson`/`tagsjson` on `subjectlinks` + `ingestionjobs`, all four providers) and stamped onto **every\n  chunk** the link produces (RecallDB) and its **source graph node** (LiteGraph). Each label `L` becomes a\n  distinct `label:L` chunk tag (so a chunk carries several labels at once) and the document type is exposed the\n  same way; reserved provenance keys are never overwritten. Retrieval can then be scoped to these facets: the\n  existing `metadataFilter` on `/v1.0/query` + `/v1.0/query/stream` now targets them, `/v1.0/chat/stream` gains\n  a per-request `metadataFilter` threaded through the agentic search tool (merged with the subject default and\n  recorded on the turn), and the search routes (`/v1.0/search`, `/v1.0/subjects/{id}/search`) accept an optional\n  URL-encoded `filter` and apply the subject's default filter. The ingestion labels/tags UX mirrors the Create\n  Subject facet editor: a shared labels+tags editor in the creator/admin link-submit modals, a link-detail\n  chips view, and a collapsible **Scope** control on every dashboard's Ask/chat page. Mirrored across the C#,\n  JS, and Python SDKs (request models + `RetrievalFilter`/`metadataFilter`) and documented in `REST_API.md`.",
          "format": "md",
          "labels": [
            "changelog"
          ],
          "tags": {}
        },
        {
          "id": "changelog--unreleased-added-2",
          "category": "changelog",
          "title": "Changelog — [Unreleased] Added (part 2: metrics, analytics, threads, filters)",
          "summary": "Added: answer-path metrics, more MCP read tools, slash commands, eval harness, analytics, threads, facet filters, per-stage telemetry.",
          "body": "- **Retrieval/answer metrics + Grafana section + TELEMETRY.md.** New Prometheus metrics cover the answer path\n  (`pneuma_chat_answers_total`, `pneuma_chat_answer_duration_seconds`, `pneuma_chat_stage_duration_seconds` by\n  stage), complementing the existing HTTP/ingestion/integration metrics and the ingestion + integration\n  traces. The Grafana observability dashboard gains a **Retrieval & Answer** domain section (answer rate,\n  answer p95, per-stage p95) alongside Overview/HTTP/Ingestion/Integrations. A new **TELEMETRY.md** walks an\n  operator through the full metrics/traces inventory, exposure (`/metrics`, OTLP), collection\n  (Prometheus/Tempo), Grafana access, and how to use each dashboard section and traces.\n- **Broadened MCP surface.** Six new read tools make the new data operable by an agent:\n  `pneuma_get_history_turn` (turn + feedback + tool-call trace + telemetry), `pneuma_enumerate_threads`,\n  `pneuma_enumerate_feedback`, `pneuma_analytics`, `pneuma_enumerate_eval_runs`, and `pneuma_get_eval_run` —\n  each RBAC-mapped (Subject / Read) exactly like the REST twins and advertised in `tools/list`.\n- **Chat slash commands.** All three dashboards' chat composers handle `/help`, `/clear` / `/new` (start a\n  new conversation), `/context` (context usage), and `/compact` (informational) client-side without sending\n  them to the model.\n- **RAG evaluation harness.** Ground-truth facts per subject (`evalfacts`), LLM-judged runs (`evalruns`) that\n  answer each fact through the real grounded pipeline and grade the result against the expected answer with a\n  seeded `eval.judge` prompt, and per-fact results (`evalresults`) with verdict (Pass/Partial/Fail), 0–10\n  score, reason, and failure mode (schema v14). REST at `/v1.0/eval/facts` (list/create/delete) and\n  `/v1.0/eval/runs` (list/start/get-with-results/delete); an **Evaluation** view (admin + creator) manages\n  facts, starts runs, and reviews results. Eval data is removed on subject cascade. (Runs execute\n  synchronously; SSE live progress is a planned enhancement.)\n- **Per-subject chat analytics.** `GET /v1.0/analytics?subjectId=&days=` returns a windowed report — turn\n  volume, latency percentiles (p50/p95/p99 generation), avg TTFT/tokens/throughput, per-stage average+p95\n  latency (from the v11 performance events), a daily time series, and feedback tallies — computed in-process\n  (provider-neutral percentiles). A new **Analytics** view (admin + creator dashboards) renders it with\n  hand-rolled SVG charts (volume-per-day bars, per-stage latency bars) and metric tiles.\n- **Conversation threads + persisted tool-call trace.** Chat turns are now grouped into named conversation\n  threads (schema v13): every agentic turn creates or continues a thread (id returned on the `complete` SSE\n  event), and the three dashboards thread the `threadId` through so a conversation stays together. New\n  `chatthreads` table + thread CRUD at `/v1.0/threads` (list/create/get-with-turns/rename/delete, cascading\n  its turns + tool calls); `GET /v1.0/history?threadId=` filters by thread. The agentic tool-call trace\n  (tool, args, output, success, duration) is persisted to a new `chattoolcalls` table and returned on\n  `GET /v1.0/history/{id}`, rendered as a \"Tool activity\" table in the History detail modal. Threads, turns,\n  and tool calls are pruned with the subject's retention window and removed on subject cascade.\n- **Metadata / facet retrieval filters.** A subject can carry a default retrieval facet filter\n  (`retrievalFilterJson`, schema v12) and `/v1.0/query` accepts a per-request `metadataFilter`; both are\n  chunk-tag predicates (`required`/`excluded` with conditions Equals/NotEquals/Contains/StartsWith/EndsWith/\n  GreaterThan/LessThan/IsNull/IsNotNull) pushed down natively to RecallDB's tag filter. The per-request filter\n  is merged with the subject default (union — narrows, never widens). Applied on both the grounded and agentic\n  search paths; the effective filter is recorded on the chat turn and shown in the History detail modal, and a\n  subject's default filter is editable in the admin/creator subject forms.\n- **Per-stage chat telemetry.** Every answered agentic turn now records structured, provider-agnostic\n  performance telemetry (schema v11): a serialized `TurnPerformance` on the turn's `performanceJson` (ordered\n  stages — prompt rewrite, compaction, each tool call, final inference — with duration, time-to-first-token,\n  tokens, provider/model) **and** one queryable `chatturnperfevents` row per stage for analytics. Surfaced on\n  `GET /v1.0/history/{id}`; the admin and creator History detail modals render a stage-details table and drive\n  their timing bars from real per-stage data. Perf events are pruned with their subject's retention window and\n  removed on subject cascade.",
          "format": "md",
          "labels": [
            "changelog"
          ],
          "tags": {}
        },
        {
          "id": "changelog--unreleased-added-3",
          "category": "changelog",
          "title": "Changelog — [Unreleased] Added (part 3: subject controls, tenancy, collections)",
          "summary": "Added: subject chat controls, history/feedback, background deletion, duplicate action, table atoms, per-tenant graphs and collections.",
          "body": "- **Subject-scoped chat controls (thinking, prompts, slug, retention).** Subjects (schema v6) gained\n  `urlSlug`, `thinkingEnabled`, `systemPrompt`, `ontologyClassifyPrompt`, `ontologyDefinitionPrompt`,\n  `historyRetentionDays` (clamped ≥ 1), and a `deletionStatus`, all editable via REST (`PUT\n  /v1.0/subjects/{id}`) and the admin/creator dashboards. Model **thinking** (`<think>…</think>`) is now\n  stripped from chat answers server-side and streamed separately; per subject it is either hidden or shown\n  in a collapsed section with a *Thinking time* statistic. A subject's **system prompt** is appended after\n  the global one for its chats, and its **ontology prompts** are appended after the global\n  `ontology.classify` / `ontology.definition` during ingestion (global base + subject appended). Each\n  subject has a unique **URL slug** (`GET /v1.0/subjects/by-slug/{slug}`; explicit clashes → 409); the user\n  dashboard now shows a card per subject and opens `/{slug}` as that subject's chat.\n- **Chat history + feedback.** Every completed chat turn is persisted (question, answer, thinking, model,\n  tokens, timing, citations) with per-subject retention pruning (schema v7). Users can rate any answer\n  (👍/👎 + optional comment) inline. New endpoints: `GET /v1.0/history`, `GET /v1.0/history/{id}`,\n  `GET /v1.0/feedback`, `POST /v1.0/feedback`. Admin and creator dashboards gained **History** and\n  **Feedback** views with full-detail modals.\n- **Background subject deletion.** Deleting a subject now returns `202` immediately and runs the heavy\n  cascade (links, jobs, events, artifacts, graph, index, history, feedback) in a background worker; the\n  subject is marked *Deleting* (greyed in the UI) until removed, and interrupted deletions resume on\n  startup. The dashboards show a dismissible \"deleting in the background — you may close this window\" notice.\n- **Duplicate row action.** Config-heavy admin tables (Model Runners, Subjects, Collections, Roles, Prompts)\n  gain a **Duplicate** action that opens the create form pre-filled from the selected record (name/key/slug\n  auto-suffixed so it doesn't collide) — copy a definition, tweak one field, save.\n- **Ingestion \"View Performance\".** The admin Ingestion Jobs action menu gains **View Performance**, a\n  per-stage bar chart sized by each stage's duration with discrete timings and share-of-total; the creator\n  dashboard's ingestion detail modal shows the same \"Time per stage\" bars.\n- **Ingestion Queue / Jobs subject filter.** Both admin pages gained a subject dropdown (`?subjectId=` on\n  `GET /v1.0/jobs`).\n- **Richer table-atom ingestion.** DocumentAtom tables are now flattened to one valid markdown cell per\n  data row (header + separator + row) so column context travels with every value; all atom types are\n  ingested except binary.\n- **MCP + SDK coverage for subjects/history/feedback.** New MCP tools `pneuma_create_subject` /\n  `pneuma_update_subject` (mirroring the REST create/update, including slug uniqueness). The C# SDK\n  (`sdk/csharp`) gained the new subject fields, `GetSubjectBySlugAsync`, and history/feedback models +\n  `ListHistoryAsync` / `GetHistoryTurnAsync` / `ListFeedbackAsync` / `SubmitFeedbackAsync`. Positive and\n  negative tests were added for the DB layer, the MCP tools, and the SDK smoke harness.\n- **Per-tenant LiteGraph isolation.** Each Pneuma tenant now gets its own isolated LiteGraph tenant and\n  graph, created and hydrated when the tenant is provisioned; the LiteGraph tenant/graph GUIDs are recorded\n  on the Pneuma tenant record (`LiteGraphTenantGuid`/`LiteGraphGraphGuid`, schema v4). All graph operations\n  (ingestion writes, search/query reads, cascade deletes, MCP graph tools) route through a per-tenant\n  `IGraphRepositoryFactory`, so one tenant can never read or write another's graph — with a fallback to the\n  configured default graph for tenants not yet provisioned. Postgres now installs pgvector (via the postgres\n  image build) with the `recalldb` database + extensions created in init.\n- **Per-tenant collections + automatic provisioning.** Each Pneuma tenant maps to a RecallDB tenant of the\n  same id; collection administration and retrieval (ingestion store, vector/full-text search, cascade\n  delete) are all tenant-scoped. RecallDB is the authority for tenants and collections — Pneuma relays and\n  assigns no ids of its own. Creating a Pneuma tenant (and first-boot seeding) now provisions the tenant on\n  RecallDB and creates a **default collection** for it, via a best-effort, idempotent, extensible\n  `TenantProvisioningService` (`ITenantProvisioner`). Configurable default collection name/dimensionality.\n- **User-managed vector collections.** Operators define collections in the Pneuma dashboard\n  (`GET/PUT/DELETE /v1.0/collections`, proxied to RecallDB); a collection's `dimensionality` is fixed at\n  creation. Ingestion now **requires** a collection: link submission takes a `collectionId`, persisted on\n  the ingestion job and used as the store/search target. The admin dashboard gains a Collections page and\n  a collection selector on link submission (single + bulk); the subject dashboard gains the selector.\n- **Collection-scoped search.** `GET /v1.0/search` and `GET /v1.0/subjects/{id}/search` accept an optional\n  `collection` query parameter; grounded query/chat and MCP search resolve a default collection when none\n  is specified.",
          "format": "md",
          "labels": [
            "changelog"
          ],
          "tags": {}
        },
        {
          "id": "changelog--unreleased-changed",
          "category": "changelog",
          "title": "Changelog — [Unreleased] Changed",
          "summary": "Changed: MCP filters/paging, domain-neutral ontology, Cell nodes instead of Chunk nodes, Verbex→RecallDB, UI refinements, split Grafana.",
          "body": "### Changed\n- **`metadataFilter` on the MCP retrieval tools, paged MCP enumerations, and answer-stage traces.** The MCP\n  `pneuma_search` and `pneuma_query` tools now accept an optional `metadataFilter` (required/excluded labels +\n  tag conditions), matching the REST `/v1.0/query` contract, so an agent can scope retrieval. The\n  `pneuma_enumerate_threads`, `pneuma_enumerate_feedback`, and `pneuma_enumerate_eval_runs` tools are now\n  properly **paged** (they return the `EnumerationResult` envelope with `totalRecords`/`endOfResults` and small\n  summaries) instead of returning the full set, and every enumerate tool's description spells out the paging\n  protocol. The agentic answer path now emits an **OTLP span per stage** (prompt rewrite, compaction, each model\n  inference, each tool call) when telemetry is enabled, complementing the per-stage metrics.\n- **Default ontology and prompts are now domain-neutral (horizontal).** The built-in ontology and default\n  prompts, as well as example subjects in the docs/SDKs/Postman collection and tests, were previously tied to a\n  single vertical; they are replaced with a generic, cross-domain set that fits any kind of subject —\n  Subject/Person/Organization/Work/Collection/Event/Place/Topic/Source/Media nodes and HAS_PART/CREATED_BY/\n  CONTRIBUTED_TO/PUBLISHED_BY/AFFILIATED_WITH/COLLABORATED_WITH/LOCATED_AT/OCCURRED_ON/ABOUT/INFLUENCED_BY\n  edges — and the grounded-answer prompt now answers a general user's question about the subject. Existing\n  deployments self-heal the `ontology.definition` and `user.answer` prompts on next boot (sentinel-guarded,\n  admin edits preserved); the change ships in the server image, so `docker/` deployments pick it up on rebuild.\n  The runtime graph merge already accepts whatever types the ontology defines, so no data migration is required.\n- **Cells are the graph's unit of source content; chunks live only in RecallDB.** Ingestion no longer\n  creates a `Chunk` node per chunk. Instead the graph-merge stage materializes a `Cell` node per extracted\n  semantic cell (carrying its text, linked to its `Source` via `HAS_CELL`), and each chunk is stored only as\n  a RecallDB document whose `litegraphNodeId` points back at its originating cell node (falling back to the\n  source). Retrieval hits still resolve to a graph node for structure and neighbor expansion, but the graph\n  is no longer inflated with one node per chunk. Legacy `Chunk`/`HAS_CHUNK` types are retained so any\n  pre-existing chunk nodes still resolve; cascade deletion removes cell nodes by the job's `assertedByJob`\n  tag as before.\n- **Retrieval store migrated from Verbex to RecallDB.** Verbex is removed entirely. RecallDB (Postgres +\n  pgvector, `:8600`) is now the retrieval store for both vector and full-text search, hidden behind the\n  same `IVectorRepository` / `IInvertedIndex` interfaces plus a new `ICollectionStore`. Vectors are no\n  longer stored on LiteGraph nodes — each chunk is stored in RecallDB as one document carrying its content,\n  embedding, and provenance tags (`litegraphNodeId` round-trips on hits so retrieval still resolves to the\n  chunk's graph node). Cascade deletion removes a job's documents by its `jobId` tag. Pneuma operates under\n  a dedicated RecallDB tenant (`pneuma`), ensured at startup.\n- **Structured retrieval-filter editor (labels + tags).** The subject's default retrieval filter — previously\n  a raw-JSON textbox on the create/edit subject form in both the admin and creator dashboards — is now a\n  structured editor: labels (`List<string>`) render as one textbox per row with a delete icon and an add\n  icon on the last row; tags (`Dictionary<string,string>`) render as side-by-side key/value textboxes with\n  the same delete/add affordances. `RetrievalFilter` gained `requiredLabels`/`excludedLabels` (matched\n  against each chunk's `label` tag, populated at ingestion from the source document type) alongside the\n  existing `requiredTags`/`excludedTags`, and the per-request `metadataFilter` shares the shape.\n- **Chat-turn detail: timing KPIs as horizontal bars.** The history-turn detail modal (admin + creator) now\n  renders the duration KPIs (TTFT, thinking, generation, time-to-last-token, pipeline wall) and the recorded\n  pipeline stages as horizontal bars, AssistantHub-style, keeping only the scalar metrics (throughput, tokens,\n  context) as compact cards.\n- **Richer chat-turn measurements.** The history-turn detail modal (admin + creator) now reports\n  time-to-last-token, pipeline wall time, and both generation-only and overall throughput, matching the depth\n  of AssistantHub's turn instrumentation.\n- **Analytics readability + per-stage latency-over-time.** The Analytics view now humanizes stage names\n  (snake_case → Title Case, `tool:` prefixes surfaced), adds a timeframe selector, and renders a stacked bar\n  chart of per-stage latency per day (bucketed) in addition to the existing per-stage averages.\n- **`/context` renders a formatted table.** The in-chat `/context` command (all three dashboards) now shows a\n  Markdown table — model (where surfaced), context window, context used with percentage, prompt/completion\n  tokens, and turn count — matching how `/help` renders.\n- **`/help` and `/?` render an in-chat command menu.** The slash-command help now renders as a Markdown table\n  inside the conversation window rather than a single \"Commands\" line.\n- **Feedback modal focus.** Clicking thumbs-up/down in Ask (all three dashboards) now moves focus straight to\n  the \"Share more feedback\" textbox.\n- **Grafana dashboards split by domain.** The single \"Pneuma Observability\" dashboard is replaced by five\n  per-domain dashboards (Overview, HTTP, Ingestion, Chat & Retrieval, Integrations), provisioned by default.",
          "format": "md",
          "labels": [
            "changelog"
          ],
          "tags": {}
        },
        {
          "id": "changelog--unreleased-fixed",
          "category": "changelog",
          "title": "Changelog — [Unreleased] Fixed",
          "summary": "Fixed: placeholder subject names in rewrites, leaked internal node kinds, off-screen menus, nav labels, fresh-install migration.",
          "body": "### Fixed\n- **Ask answers no longer invent a placeholder subject name.** A vague follow-up (\"Tell me more about the\n  side effects\") could be rewritten by the query-rewrite step into a question about a hypothetical\n  \"medication X\", which the model then answered as if real. The rewrite step is now grounded in the subject's\n  display name and explicitly forbidden from introducing a placeholder or hypothetical name, so vague and\n  pronoun references resolve to the actual subject.\n- **Answers no longer leak internal object kinds (e.g. \"(source: Cell)\").** The grounded-answer context that\n  is sent to the model previously prefixed each source with its internal graph node type and name\n  (`[1] (Cell) cell_9f3a…: …`), which the model would echo back to readers as \"(source: Cell)\". Sources are now\n  presented as bare numbered excerpts (`[1] …`), and the system-level answering prompts (`user.answer`,\n  `assistant.system`) plus the default subject system prompt (admin + creator create-subject forms) explicitly\n  forbid naming internal object kinds/storage labels. Existing deployments self-heal the two prompts on next\n  boot (sentinel-guarded), and the change ships to `docker/` and `docker/factory/` on image rebuild.\n- **Row action menus stay on-screen.** The row `⋯` action menu (every table in the admin and creator\n  dashboards) dropped straight down from the trigger and ran off the bottom of the viewport for the last rows;\n  it now flips above the trigger (or clamps and scrolls) so it is always fully visible.\n- **Admin dashboard nav labels.** The Analytics and Evaluation nav entries showed raw i18n keys\n  (`nav.analytics`, `nav.eval`); the missing translations are added.\n- **Fresh-install migration for the queue-duration column.** The `ingestionjobevents.queuedurationms`\n  column (and the new subject columns) were being created in both the baseline schema and a migration,\n  which failed on a fresh SQLite/MySQL database with a duplicate-column error. Baseline `CREATE` statements\n  are now the original v1 shape and the versioned migrations are the sole source of later columns.",
          "format": "md",
          "labels": [
            "changelog"
          ],
          "tags": {}
        },
        {
          "id": "changelog--0-1-0-added-1",
          "category": "changelog",
          "title": "Changelog — [0.1.0] Added (part 1)",
          "summary": "0.1.0 additions: discrete processing stages, per-source lexical docs, grouped search, fallbacks, faster cascades, MCP, streaming, role interfaces.",
          "body": "## [0.1.0] - 2026-08-18\n\n### Added\n- **Discrete Summarization, Chunking, and Embedding ingestion steps.** These now call Partio's separate\n  `/v1.0/summarize`, `/v1.0/chunk`, and `/v1.0/embed` endpoints (Partio 0.4.0) and run as three distinct,\n  independently-timed pipeline stages, each with its own duration recorded in the job log (the Follow Logs\n  modal shows per-step and total runtime). Embedding is done in bounded batches.\n- **One Verbex document per source.** Ingestion indexes a source's full text as a single lexical document\n  (tagged with `linkId`/`tenantId`/`subjectId`/`jobId`/`sourceUrl`/`documentType`) rather than one document\n  per chunk, so a search returns one hit per ingested source. Chunk-level graph nodes and vectors are still\n  created for semantic retrieval.\n- **Subject search grouped per source.** `GET /v1.0/subjects/{id}/search` rolls hits up to one result per\n  source link (best score + `matchCount`), and the admin Search table shows Score, Matches, Title, URL,\n  Link ID, and the top passage.\n- **Answering model falls back to the Partio completion endpoint** when no explicit Pneuma model runner is\n  configured, so grounded query / chat works out of the box with the configured completion model.\n- **Parallelized cascade deletion.** Verbex document deletes and LiteGraph node/edge deletes run with bounded\n  concurrency, so deleting a link/subject with many indexed documents is dramatically faster.\n- **Clearer ingestion event log.** The Categorization phase is recorded as a single `Completed` event (with\n  prompt provenance folded in), and Hydration emits a matching `Completed` event, so no phase lingers as\n  `Processing`.\n- Admin dashboard: health histogram capped to 10 bars in the table / 50 in the modal; the Follow Logs modal\n  shows total runtime across all stages at the top.\n- Docker compose pins Partio to `v0.4.0` (adds `/v1.0/chunk` and `/v1.0/embed`).\n- **Integrated MCP server.** The Pneuma host now serves a Model Context Protocol endpoint at `POST /mcp`\n  (JSON-RPC 2.0) in-process, so agents drive the platform through the *same* `AuthenticateRequest` hook,\n  `AuthorizationService`, and audit trail as the REST API — the same bearer/API-key credentials apply and\n  every decision is audited identically. Collections are never returned unbounded: `pneuma_enumerate_*` tools\n  page through the `EnumerationQuery`/`EnumerationResult` envelope with small summary projections (the\n  first page carries `totalRecords`), and full objects are fetched one at a time with `pneuma_get_*`. Ships\n  `pneuma_capabilities`, `pneuma_enumerate_subjects`/`pneuma_get_subject`, and `pneuma_enumerate_jobs`/`pneuma_get_job`,\n  plus discovery via `tools/list`. Documented in `MCP_API.md`.\n- **Streaming grounded chat.** A new `POST /v1.0/query/stream` streams the grounded answer over\n  server-sent events (`metadata` / `delta` / `complete` / `error`, with an `insufficientSupport` refusal).\n  All three dashboards (admin, subject, user) now have a live \"Ask\" surface that renders the answer as it\n  arrives, backed by a dependency-free `sse.js` client.\n- **Provider-neutral integration interfaces.** Ingestion and retrieval now depend on role interfaces —\n  `IGraphRepository`, `IVectorRepository`, `IAtomizer`, `ISemanticProcessor`, `IInvertedIndex`,\n  `IServiceProbe` — rather than vendor client names, so a backend can be swapped without touching\n  orchestration.",
          "format": "md",
          "labels": [
            "changelog"
          ],
          "tags": {}
        },
        {
          "id": "changelog--0-1-0-added-2",
          "category": "changelog",
          "title": "Changelog — [0.1.0] Added (part 2)",
          "summary": "0.1.0 additions: resilient outbound HTTP, startup diagnostics, two-phase ingestion, chunk nodes, session refresh, CI, search page, crawling.",
          "body": "- **Resilient outbound HTTP.** All integration clients (DocumentAtom, Partio, Verbex, LiteGraph) extend a\n  shared `IntegrationClientBase` with transient-failure retry, per-attempt timeout, a per-service\n  concurrency bulkhead, uniform structured failures, and per-call telemetry. Writes never retry, so a\n  non-idempotent create is never duplicated. Configurable via `Integrations.Resilience`.\n- **Startup connectivity diagnostics.** On boot Pneuma probes every external dependency concurrently and logs\n  a secret-safe reachable / erroring / unreachable report; an optional `Diagnostics.FailFastOnStartupProbe`\n  aborts startup on an unreachable required service.\n- **Automated two-stage ingestion.** Ingestion runs as an explicit `Categorize` phase (fetch → atomize →\n  classify into a candidate plan) followed by a `Hydrate` phase (commit to the graph, embeddings, and\n  index), visible as phase events in \"Follow Logs\". Each run also records **prompt provenance** — the\n  version and content-hash of the prompts that shaped it — so a past run stays reproducible even after\n  prompts are edited.\n- **Chunks are first-class graph nodes.** Each semantic chunk becomes a `Chunk` node linked to its source,\n  carrying its text and (when embedded) its vector, so semantic search resolves to chunk-level content.\n- **Session refresh.** `POST /v1.0/token/refresh` rotates the session — issuing a fresh token and revoking\n  the prior one, so a leaked earlier token stops working — and records it in the audit stream.\n- **Grounded question answering** blends lexical (inverted-index) and semantic (graph vector) retrieval\n  with optional graph-neighbor expansion, exposed over REST (`/v1.0/query` + SSE stream) and the MCP\n  `pneuma_query` tool from one shared service.\n- **Continuous integration.** A `.github/workflows/ci.yaml` builds the solution, runs the automated/xUnit/\n  NUnit suites (each Touchstone case now reported individually) and a live-PostgreSQL job, and builds and\n  tests the three dashboards (vitest). Root `build.bat`/`test.bat` mirror it locally.\n- **Search page (Content).** A new admin **Search** view lets an operator pick an subject and full-text\n  search that subject's ingested documents (Verbex), with results paginated and ranked by score and each\n  hit linked back to the content link it came from. Backed by\n  `GET /v1.0/subjects/{subjectId}/search?q=&maxResults=&skip=` (Verbex search filtered by `subjectId`,\n  resolving each document's job back to its link).\n- **Fuller web-page capture for ingestion.** The headless-browser (Playwright/Chromium) content fetcher\n  now renders at a real 1920×1080 desktop viewport, auto-scrolls (bounded) to trigger lazy/infinite-scroll\n  content, waits for the network to settle, and force-opens collapsible `<details>` regions before\n  capturing the DOM — so DocumentAtom's cell extraction sees the complete page instead of only the\n  above-the-fold/initial content. All steps are best-effort and time-boxed.\n- **Subject delete now cascades** through every subordinate object — all of the subject's links (each\n  with its full link cascade: jobs, processing logs, S3 pipeline artifacts + raw blobs, and Verbex\n  index documents) and the subject's entire LiteGraph subgraph (matched on the `subjectId` tag). The\n  link, job, and subject cascades are now centralized in a single `CascadeDeletionService`.",
          "format": "md",
          "labels": [
            "changelog"
          ],
          "tags": {}
        },
        {
          "id": "changelog--0-1-0-fixed-and-added",
          "category": "changelog",
          "title": "Changelog — [0.1.0] Fixed / Added (scaffold and early features)",
          "summary": "0.1.0 fixes (bypass auditing, SQLite lock scope, endpoint reset) and the initial scaffold, health monitoring, pagination, settings, S3 artifacts.",
          "body": "### Fixed\n- **Administrator bypasses are now audited.** System-admin and tenant-admin authorization bypasses were\n  recorded only in memory; they are now persisted as `AuthorizationBypass` audit records, matching the\n  documented \"all denials and bypasses are audited\" contract.\n- **Server-grade databases are no longer globally serialized.** The SQLite single-writer semaphore was\n  also throttling PostgreSQL, MySQL, and SQL Server (which open pooled connections per call); it is now\n  scoped to SQLite only, removing an unnecessary bottleneck on the deployed database.\n- **Model endpoint edits no longer reset on restart.** Partio reconciles its `id=\"default\"` embedding\n  endpoint back to `partio.json`'s `DefaultEmbeddingEndpoints` on every boot, which silently reverted\n  dashboard edits (new URL/model) to the seeded values. `DefaultEmbeddingEndpoints` and\n  `DefaultInferenceEndpoints` are now empty in `partio.json` (disabling the reconcile), and Pneuma instead\n  seeds a default embedding + completion endpoint on startup **only when none exist** (create-only, never\n  overwriting), so operator edits persist across server and stack restarts.\n\n### Added\n- **Model endpoint health monitoring:** a background monitor probes each model endpoint's base URL,\n  **deduplicated by base URL** (a host shared by multiple endpoints is checked once and the result\n  fanned out), with two-check healthy/unhealthy hysteresis and an in-memory rolling 24h history,\n  uptime %, latency, and last HTTP status code. Exposed as `GET /v1.0/model-runners/health` (all\n  endpoints) and `GET /v1.0/model-runners/{id}/health`. The admin **Model Runners** page shows a\n  per-row health pill + history sparkline (polled every 15s) that opens a health-details modal;\n  the health cell does not trigger the row's edit modal. REST_API.md and the Postman collection updated.\n- Initial repository scaffold: solution, core library, Watson 7.1 server, four-provider data layer,\n  three dashboards (admin, subject, user), SDKs (C#, JS, Python), and Docker Compose stack.\n- Knowledge-graph ontology for subject archives (subject-neutral) backed by LiteGraph.\n- Ingestion pipeline: DocumentAtom type detection + cell extraction, PolyPrompt ontology\n  classification, LiteGraph subgraph merge, Partio chunk/embed/summarize, Verbex indexing.\n- Full RBAC (tenants, users, credentials, roles, permissions, assignments, sessions, audit).\n- Observability via Radiant traces, Prometheus metrics, Grafana dashboards, and Tempo.\n- Admin-editable prompts for every model interaction, and a **natural-language ontology** (the\n  `ontology.definition` prompt) that drives classification and can be rewritten without code changes;\n  the graph merge accepts whatever node/edge types the admin's ontology defines.\n- Test suite (Touchstone, runnable via console/xUnit/NUnit): 33 cases spanning crypto, RBAC,\n  token codec, four-provider database contracts, graph merge/entity-resolution, the ingestion state\n  machine (fakes), and end-to-end HTTP auth/RBAC/request-history/metrics against an in-process server —\n  including list pagination, subject slug derivation, settings read/write masking, and job stop/log.\n- **Paginated list APIs:** every collection `GET` now returns an `EnumerationResult<T>` envelope\n  (`success`, `maxResults`, `skip`, `totalRecords`, `recordsRemaining`, `endOfResults`, `objects`) and\n  honors `maxResults`, `skip`, `order` (asc/desc), and `search` query parameters. REST_API.md, the\n  Postman collection, all three SDKs (C#/JS/Python), and the dashboards consume the new shape.\n- **Server settings API:** `GET /v1.0/settings` (secrets masked) and `PUT /v1.0/settings` (overwrites\n  the settings file; a masked secret submitted unchanged preserves the stored value), with metadata\n  annotating which sections require a server restart. Backed by a styled admin settings form.\n- **Subject graph root node id** is derived from the display name as a slug (e.g. \"The Example Project\" →\n  `the-example-project`) when not supplied.\n- **Ingestion job control:** `POST /v1.0/jobs/{id}/stop` cancels a queued or in-flight job (mid-flight\n  cancellation is honored between pipeline stages) and `GET /v1.0/jobs/{id}/log` returns the live\n  per-stage log for a follow-logs view.\n- **Per-link model selection:** submitting a link now requires an embedding and completion endpoint id\n  (`GET /v1.0/ingestion/endpoints` lists the available Partio-backed models); `POST /v1.0/subjects/{subjectId}/links/bulk`\n  submits many URLs at once. The chosen ids are stored on the ingestion job (new columns; schema migration v2)\n  and passed through to Partio. Surfaced in the admin/subject dashboards (required dropdowns + \"Add Multiple\").\n- **Headless-browser crawling:** the ingestion content fetcher renders pages with headless Chromium\n  (Playwright) so JavaScript-heavy sites are captured, falling back to a plain HTTP fetch; the server image\n  installs Chromium at build time (`Ingestion.UseHeadlessBrowser`, `BrowserNavigationTimeoutMs`).\n- **Custom themed 404 page** in all three dashboards (catch-all routes and unknown sections).\n- **Job detail / follow-logs modals** gained a copy-to-clipboard control that copies the processing log as JSON.\n\n- **Object storage (Less3 / S3):** added single-node `less3` + `less3-ui` (v4.0.0) to the stack. Pipeline\n  artifacts are persisted to five S3 buckets — source (on crawl), atoms (DocumentAtom), chunks and\n  embeddings (Partio), and subgraph JSON (model) — each keyed by the originating link id. S3 endpoint,\n  credentials, region, and bucket names live in server settings; buckets are auto-created at startup.\n  New `GET /v1.0/links/{id}/{source|atoms|chunks|vectors|subgraph}` endpoints plus Links action-menu\n  items (View Source Document / View Atoms / View Chunks / View Vectors / View Subgraph).\n- **LiteGraph startup initialization:** the server ensures the LiteGraph tenant, user, credential, and\n  \"Pneuma\" graph exist on boot (idempotent), so the knowledge graph is ready without manual setup.",
          "format": "md",
          "labels": [
            "changelog"
          ],
          "tags": {}
        },
        {
          "id": "changelog--0-1-0-changed-fixed",
          "category": "changelog",
          "title": "Changelog — [0.1.0] Changed / Fixed",
          "summary": "0.1.0 changes and fixes: ID format, shared Postgres, stage timeout default, network binding, casing, pinned image versions.",
          "body": "### Changed\n- Audit list endpoint dropped its bespoke `?max=` parameter in favor of the standard pagination params.\n- **Identifiers** are now 32 characters total (`{prefix}{ksortable}_{random}`).\n- **All subordinate data services use the shared PostgreSQL** instance (each with its own database:\n  `less3`, `litegraph`, `partio`, `verbex`) instead of SQLite; the databases are created on first boot.\n- Dashboard \"server URL\" env vars now point at browser-reachable host ports for the DocumentAtom,\n  Verbex, Partio, LiteGraph, and Less3 UIs.\n- `Ingestion.StageTimeoutSeconds` default raised to 1800s to accommodate slow local LLM inference.\n\n### Fixed\n- Integration clients (DocumentAtom, LiteGraph, Partio, Verbex) bind all interfaces (`*`) in the Docker\n  settings so they are reachable across the compose network (they shipped bound to container loopback).\n- Partio and Verbex request bodies are serialized in PascalCase to match those services' case-sensitive\n  deserializers (previously camelCase produced `\"… is required\"` 400s).\n- Partio embedding/completion endpoints point at the `ollama` service (was container `localhost`).\n- Compose image tags pinned to known-good versions (Tempo `2.6.1`, Prometheus `v3.5.4`, Grafana `13.0.2`,\n  Ollama `0.31.2`, Partio images by digest) after `grafana/tempo:latest` broke on a v3.0.0 dev build.",
          "format": "md",
          "labels": [
            "changelog"
          ],
          "tags": {}
        },
        {
          "id": "partio-removal-plan--overview",
          "category": "partio-removal-plan",
          "title": "Partio Removal Plan — Overview / How to use this document",
          "summary": "Goal and rationale for absorbing Partio's functions into Pneuma, and how to annotate the plan.",
          "body": "# Partio Removal — Native Processing in Pneuma\n\n**Target release:** `0.1.0`.\n**Status:** Not started.\n**Owner:** _unassigned_\n\nPneuma delegates chunking, embedding, and summarization to Partio and treats Partio as the system of record for model endpoints. This plan brings all of that in-house so Pneuma owns the processing tier end to end, deletes the Partio server and dashboard from the stack, and — the recurring operational pain — removes the second service whose endpoint health and config drift out of sync with Pneuma's. Completions already run direct to the provider, so this is finishing a job that's half done, not starting a new one.\n\nThe work is tractable because the seam already exists. `ISemanticProcessor` (chunk / embed / summarize / process) is a Pneuma abstraction; `IPartioClient : ISemanticProcessor` only adds endpoint administration on top. Ingestion and retrieval depend on those interfaces, not on Partio, so most of this plan swaps implementations behind interfaces the call sites already use.\n\n---\n\n## How to use this document\n\nEvery task is a checkbox. Annotate in place:\n\n- `- [ ]` not started · `- [~] (abc 09-20)` in progress · `- [x] (abc 09-21)` done — initials and date in parentheses.\n- When a task spawns follow-ups, nest them beneath it.\n- Keep the **Product-surface coverage** matrix (§7) honest — it is the definition of \"full-product-facing\" and the release gate.\n\nOut of scope (tracked elsewhere): the `#5`/`#12` ingestion **step-split** in `CKG_IMPROVEMENTS.md` (making ontology canonicalization and relationship consolidation their own timed stages) is a separate, paused effort. It does not block this plan and should not be folded in.",
          "format": "md",
          "labels": [
            "partio-removal-plan"
          ],
          "tags": {}
        },
        {
          "id": "partio-removal-plan--locked-decisions",
          "category": "partio-removal-plan",
          "title": "Partio Removal Plan — 1. Locked decisions",
          "summary": "Binding decisions: full removal, prompt merge modes, tokenizer parity, exact chunk parity, version 0.1.0.",
          "body": "## 1. Locked decisions\n\nThe following were decided at planning time and are binding for `0.1.0`. The overriding goal: **Partio ceases to be a component of Pneuma — every function is fully absorbed, manageable end to end across all surfaces, well documented, and well tested.**\n\n- [x] **Scope — full removal.** Endpoint registry, embedding, summarization, and chunking all come in-house; the Partio server and dashboard are deleted from `docker/compose.yaml` and `docker/factory/compose.yaml`; `IPartioClient`/`PartioClient` and the Partio config block are removed. No intermediate stop.\n- [x] **Prompt override semantics — operator-selectable per prompt.** Each prompt supports a mode of **default / append / replace**, defaulting to append (preserves today's behavior). Global is always the fallback when a subject has no override. This shapes the schema (§4.5) and the dashboard editor (§6).\n- [x] **Tokenizer scope — full Partio parity.** Port SharpToken (`cl100k_base`) **and** `Microsoft.ML.Tokenizers` (BERT/WordPiece with embedded vocab), Partio's `TokenizationProfileResolver`, and the structure-aware table/list chunkers, so token counts are model-exact and chunking behavior matches Partio across every strategy and model family.\n- [x] **Chunk parity — exact.** The native chunker must reproduce Partio's chunk boundaries so vectors stay stable across cutover (no forced re-index). A byte-level parity harness against Partio output on a fixed corpus is a required gate (§4.4, §8).\n- [x] **Version — `0.1.0`.** All markers (csprojs, README badge, `DOCKERHUB_README`, `CHANGELOG`, compose image tags, SDK versions) read `0.1.0`.",
          "format": "md",
          "labels": [
            "partio-removal-plan"
          ],
          "tags": {}
        },
        {
          "id": "partio-removal-plan--current-use-and-target",
          "category": "partio-removal-plan",
          "title": "Partio Removal Plan — 2. What Pneuma uses Partio for today / 3. Target architecture",
          "summary": "The four functions Partio provided with removal cost, and the target native architecture.",
          "body": "## 2. What Pneuma uses Partio for today\n\nFour functions, with very different removal costs:\n\n| Function | Path today | Removal cost |\n|---|---|---|\n| Endpoint management (`cep_`/`eep_`, config, health, concurrency, request history) | Pneuma Model Runners UI proxies Partio | Medium — a DB-backed registry, reusing existing health/gate/retry |\n| Embedding | `PartioClient.EmbedAsync` / `ProcessAsync` → Partio → provider | Small — `CompletionClientBase.EmbedAsync` already exists in PolyPrompt |\n| Summarization | `PartioClient.SummarizeAsync` → Partio → provider | Small — a completion; prompt already owned (`cell.summarize`) |\n| Chunking | `PartioClient.ChunkAsync` → Partio (algorithmic, tokenizer-backed) | Medium — port `Partio.Core/Chunking/` + a tokenizer |\n\nCompletions (answering, chat, classification, rerank, rewrite) already bypass Partio entirely — Pneuma builds a `ModelRunner` from the endpoint config it read from Partio and calls the provider via `ModelClientFactory.Create(...)` → `CompletionClientBase`. That same base class exposes `EmbedAsync`, `GenerateAsync`, `ListModelsAsync`, and `GetModelInformationAsync`, which is why embedding, model discovery, and endpoint validation come in-house with no new dependency.\n\n---\n\n## 3. Target architecture\n\nPneuma keeps `ISemanticProcessor` as the internal contract and gains a native implementation plus a real endpoint registry:\n\n- **`IModelEndpointStore`** — a Pneuma-owned, DB-backed registry of embedding and completion endpoints (provider, base URL, model, API format, encrypted key, health/concurrency config), replacing Partio as the system of record. Backed by the provider-neutral data layer across all four databases.\n- **`NativeSemanticProcessor : ISemanticProcessor`** — embedding via `CompletionClientBase.EmbedAsync` + L2 normalization + the existing embedding cache (#14); summarization via a direct completion using the `cell.summarize` prompt; chunking via the ported library.\n- **`Pneuma.Chunking`** — a new `src/` library holding the ported chunking engine and strategies, plus a `ITokenizerAdapter` (SharpToken).\n- **Prompt management** — the existing global-default + per-subject-override system, completed so every LLM operation's prompt is per-subject with a system default, and exposed as a first-class dashboard surface.\n\n`IPartioClient` and `PartioClient` are deleted at the end. The DI wiring in `Bootstrapper`/`PneumaServer` swaps `PartioClient` for `NativeSemanticProcessor` + `IModelEndpointStore`; `IngestionStages` and `GroundedQueryService` change only where they call endpoint-admin methods (which move to the store).",
          "format": "md",
          "labels": [
            "partio-removal-plan"
          ],
          "tags": {}
        },
        {
          "id": "partio-removal-plan--workstreams",
          "category": "partio-removal-plan",
          "title": "Partio Removal Plan — 4. Workstreams",
          "summary": "Task checklists for the endpoint registry, embedding, summarization, chunking port, prompt management, and Partio deletion.",
          "body": "## 4. Workstreams\n\n### 4.1 Endpoint registry (server)\n- [ ] Model: `ModelEndpoint` (embedding + completion), PrettyId-prefixed, `TenantId`-scoped (nullable-tenant global fallback, mirroring the `prompts` table), encrypted key via the existing `Aes256Cipher`, plus provider-specific fields (deployment/apiVersion for Azure, region for Bedrock/Vertex, project for Vertex, second encrypted secret for AWS) to support every provider.\n- [ ] **Full provider coverage (all PolyPrompt providers).** Extend `ModelRunnerProviderEnum` and `ModelClientFactory.Create` to build every PolyPrompt client — OpenAI, OpenAICompatible/vLLM, Azure OpenAI, Gemini, Ollama, Anthropic, Bedrock, Voyage, Vertex — for **both** completions and embeddings. Anthropic exposes no embeddings API (throws `NotSupportedException`), so it must be selectable only for completion endpoints and rejected with a clear validation error for embedding endpoints.\n- [ ] `IModelEndpointStore` + provider implementations under `Sqlite`/`Mysql`/`Postgresql`/`SqlServer` `Implementations/` + `Queries/`, handwritten SQL, composite unique index `(tenant_id, …)`.\n- [ ] Versioned, idempotent migration adding the `modelendpoints` table to all four providers.\n- [ ] Endpoint validation using `GetModelInformationAsync` / a tiny `EmbedAsync` probe; health via the existing `ModelHealthMonitor` (2xx-only, already fixed); concurrency via `ModelRunnerGate`; outbound retry via `IntegrationClientBase`.\n- [ ] Model discovery endpoint using `ListModelsAsync` (populate the dashboard's model picker without hand-typing).\n- [ ] Repoint `GroundedQueryService` endpoint resolution and `IngestionStages` completion-endpoint resolution from `IPartioClient` to `IModelEndpointStore`.\n\n### 4.2 Embedding (server)\n- [ ] `NativeSemanticProcessor.EmbedAsync` → `CompletionClientBase.EmbedAsync`, batched (keep the 64-batch), L2-normalized, cache-backed (#14 `EmbeddingCache`).\n- [ ] Query-time embedding (retrieval) moves off `PartioClient.ProcessAsync` onto the native embedder.\n- [ ] Dimensionality guard: record model→dimension and validate against the target RecallDB collection's fixed dimensionality at subject-config time and before batch insert.\n\n### 4.3 Summarization (server)\n- [ ] `NativeSemanticProcessor.SummarizeAsync` → direct completion using the resolved completion endpoint and the `cell.summarize` prompt (with per-subject override, §4.5).\n- [ ] Preserve the top-down / max-token behavior as prompt + parameters (now Pneuma-managed settings, not Partio config).\n\n### 4.4 Chunking (`src/Pneuma.Chunking` library) — full parity\n- [ ] Port **all** of `Partio.Core/Chunking/` into `Pneuma.Chunking`: the engine + `FixedTokenChunker`, `SentenceChunker`, `ParagraphChunker`, `RegexChunker`, `WholeListChunker`, `ListEntryChunker`, `TableChunker`, and `ChunkingHelpers` — renamed into Pneuma's namespace and conformed to `CODE_STYLE.md` (no `var`, no tuples, usings inside namespace, `_PascalCase` privates, XML docs, `.ConfigureAwait(false)`, `CancellationToken` on async).\n- [ ] `ITokenizerAdapter` with **both** adapters for full parity: `SharpTokenTokenizerAdapter` (SharpToken, `cl100k_base`) and `BertWordPieceTokenizerAdapter` (`Microsoft.ML.Tokenizers` + embedded vocab), plus Partio's `TokenizationProfileResolver` so the right tokenizer is chosen per model family. Add the `SharpToken` and `Microsoft.ML.Tokenizers` NuGets to the chunking library only.\n- [ ] Adapter from Pneuma's `ExtractedCell` (already flattened text) to the chunker input; wire `NativeSemanticProcessor.ChunkAsync` to it, honoring the per-subject `ChunkStrategy`/`ChunkMaxTokens`/`ChunkOverlapTokens` from #6.\n- [ ] **Exact-parity harness (release gate):** a test that runs a fixed corpus through both Partio and `Pneuma.Chunking` for every strategy × tokenizer profile and asserts byte-identical chunk boundaries and token counts. Must be green before the default flips (§8).\n- [ ] Confirm `Pneuma.Chunking` lands under `src/` (REPOSITORY_REQUIREMENTS §6) and is referenced by `Pneuma.Server`.\n\n### 4.5 Prompt management — per-subject with system defaults (server)\n- [ ] Complete override coverage: add per-subject override fields (or a normalized per-subject prompt table) for every LLM prompt, including the ones that lack them today — **`cell.summarize`** and the **community-summary** prompt (currently hardcoded in `CommunityService`, promote to a seeded, managed prompt).\n- [ ] Implement the chosen merge semantics (§1): default / append / replace, resolved centrally (extend `MergePromptAsync`) and applied uniformly across `ontology.classify`, `ontology.definition`, `cell.summarize`, `community.summarize`, `user.answer`, `reranking`, `prompt.rewrite`, `SystemPrompt`.\n- [ ] Effective-prompt resolution API: return the resolved text plus provenance (default vs override, version + hash — Pneuma already records prompt provenance at ingestion), with **global as the fallback** whenever a subject has no override for a given prompt key.\n- [ ] Dashboard-facing list/query contract: support filtering prompts by **scope** — a single subject or global — and, for a subject filter, indicate for each prompt whether the value is a subject override or inherited from the global default (so the UI can show the fallback explicitly).\n- [ ] Migration + idempotent first-boot seeding for any new prompt keys / override columns across all four providers.\n\n### 4.6 Remove Partio (server + stack)\n- [ ] Swap DI in `Bootstrapper`/`PneumaServer`: `NativeSemanticProcessor` for `ISemanticProcessor`, `IModelEndpointStore` for endpoint admin.\n- [ ] Delete `IPartioClient`, `PartioClient`, and `PartioEndpoint`-as-wire-model once no references remain.\n- [ ] Remove the Partio integration block from `pneuma.json` (and `docker/pneuma.json`, `docker/factory/pneuma.json`); add native processing/endpoint config where needed.\n- [ ] Behind a config flag during rollout so native vs Partio can be toggled per environment (§8).",
          "format": "md",
          "labels": [
            "partio-removal-plan"
          ],
          "tags": {}
        },
        {
          "id": "partio-removal-plan--migration-and-surfaces",
          "category": "partio-removal-plan",
          "title": "Partio Removal Plan — 5. Data & migration / 6. Product-surface coverage",
          "summary": "Data migration tasks and the per-surface release-gate checklist (dashboards, SDKs, docs, compose, Grafana).",
          "body": "## 5. Data & migration\n- [ ] One-time importer: read existing Partio endpoints, write them into `IModelEndpointStore`, and remap each subject's `EmbeddingModel`/`InferenceModel`/`RerankingModel`/`PromptRewriteModel` references to the new endpoint ids.\n- [ ] Dimensionality reconciliation for existing RecallDB collections (see §4.2 guard).\n- [ ] Document the chunk-parity re-index expectation (§1) and provide the \"reingest to normalize\" operator note.\n- [ ] Verify cascade deletion and provisioning paths no longer touch Partio.\n\n---\n\n## 6. Product-surface coverage (the release gate)\n\nEach row must be genuinely covered, not just the server.\n\n- [ ] **Server** — endpoint registry, native processor, chunking library, prompt system; telemetry and tests below.\n- [ ] **Admin dashboard** — \"Model Endpoints\" management (create/edit/validate/health/model-discovery) replacing the Partio proxy, with a provider dropdown covering all PolyPrompt providers and the provider-specific fields (deployment/apiVersion/region/project/creds) shown conditionally; Prompts editor with a **scope filter (subject vs. global)** where global is the fallback and inherited-vs-overridden is shown per prompt (effective value, default-vs-override, reset-to-default, provenance); per-subject chunking already present (#6). All strings via i18next (`I18N.md`); no hardcoded text.\n- [ ] **Subject dashboard** — Prompts editor with the same subject/global scope filter and global-fallback behavior, plus chunking + models, consistent with admin.\n- [ ] **User dashboard** — no new surface expected; verify nothing referenced Partio.\n- [ ] **SDKs** (`sdk/csharp`, `sdk/js`, `sdk/python`) — model-endpoint management + prompt-management methods for the new/changed REST routes, each with test-harness coverage and README updates; loopback base URLs use `127.0.0.1` (REPOSITORY_REQUIREMENTS §7).\n- [ ] **REST_API.md** — document new/changed endpoints (model endpoints, prompt management, model discovery); remove anything Partio-proxy-specific; keep in sync (REPOSITORY_REQUIREMENTS §13).\n- [ ] **Postman** (`assets/postman/…`) — add/adjust requests + folder/collection descriptions, variables for base URL/ports/tokens, in sync with REST_API.md.\n- [ ] **MCP_API.md** — update if any MCP tool surface changes (e.g., model-endpoint or prompt tools); keep in sync (REPOSITORY_REQUIREMENTS §14).\n- [ ] **README.md** — update the architecture (drop Partio from the service list and diagram; describe native processing); reconcile the version badge.\n- [ ] **DOCKERHUB_README.md** — mirror the README changes (REPOSITORY_REQUIREMENTS §4).\n- [ ] **CHANGELOG.md** — a `0.1.0` entry (Keep a Changelog format) describing the native processing move and the Partio removal as the headline.\n- [ ] **docker/compose.yaml** — remove `partio-server` and `partio-dashboard`; fix `depends_on` chains that referenced them; confirm `pneuma-*` services use named+tagged images at the release version (REPOSITORY_REQUIREMENTS §9, §11, §12).\n- [ ] **docker/factory/compose.yaml** — same removals; update the factory seed so it provisions native endpoint-registry rows instead of Partio `cep_`/`eep_` defaults; verify `reset.bat`/seed still work.\n- [ ] **docker/update.bat** — still valid after the image changes (pull/down/up/ps) (REPOSITORY_REQUIREMENTS §10).\n- [ ] **Grafana / observability** — remove or repurpose the Partio integration board; ensure the native processing spans/metrics render (see §7 telemetry).",
          "format": "md",
          "labels": [
            "partio-removal-plan"
          ],
          "tags": {}
        },
        {
          "id": "partio-removal-plan--requirements-checklist",
          "category": "partio-removal-plan",
          "title": "Partio Removal Plan — 7. Requirements compliance checklist",
          "summary": "Compliance checks against the style, telemetry, i18n, architecture, testing, repository, and auth requirement documents.",
          "body": "## 7. Requirements compliance checklist\n\nTrace each against `C:\\code\\agents\\requirements`.\n\n- [ ] **CODE_STYLE.md** — new server code and the ported chunker conform (no `var`/tuples/partial classes; usings inside namespace; `_PascalCase` privates; explicit getters/setters with validation; `.ConfigureAwait(false)`; `CancellationToken` on async; XML docs incl. nullability/thread-safety; async variant for any new `IEnumerable`-returning method).\n- [ ] **TELEMETRY_REQUIREMENTS.md** — embedding, chunking, summarization, and endpoint-health each emit application meters + `ActivitySource` spans nested under the request/job span, same as the existing pipeline stages; no regression to the per-stage timing already surfaced in ingestion; Grafana boards updated; home-page links intact.\n- [ ] **I18N.md** — every new user-visible dashboard string comes from the i18n layer across all three dashboards; locale-aware formatting via shared helpers; no raw strings.\n- [ ] **BACKEND_ARCHITECTURE.md** — registrars/hooks, `RequestContext`, RBAC gating, request capture, `127.0.0.1` loopback, provider folders and handwritten SQL, versioned idempotent migrations, first-boot seeding.\n- [ ] **BACKEND_TEST_ARCHITECTURE.md** — Touchstone descriptors in `Test.Shared`, run via `Test.Automated`/`Xunit`/`Nunit`; four-provider contract suites for the new tables; tests bind `127.0.0.1`.\n- [ ] **REPOSITORY_REQUIREMENTS.md** — items covered in §6 (README, DOCKERHUB_README, CHANGELOG, src/ layout, SDKs, docker named images, healthchecks, depends_on, REST_API + Postman, MCP_API).\n- [ ] **AUTHENTICATION.md** — model-endpoint and prompt routes are RBAC-gated (endpoints are tenant-owned; keys write-only, never returned); all decisions audited.",
          "format": "md",
          "labels": [
            "partio-removal-plan"
          ],
          "tags": {}
        },
        {
          "id": "partio-removal-plan--rollout-and-version",
          "category": "partio-removal-plan",
          "title": "Partio Removal Plan — 8. Rollout / 9. Version bump",
          "summary": "Five-phase strangler rollout and the version-bump tasks.",
          "body": "## 8. Rollout (strangler)\n\nShip in reversible phases; each is independently valuable.\n\n1. [ ] **Endpoint registry** live; Model Endpoints UI reads/writes it; Partio still running. Importer migrates existing endpoints.\n2. [ ] **Native embedding + summarization** behind `ISemanticProcessor`, selected by a per-environment config flag; run against the same providers and compare outputs to Partio.\n3. [ ] **Native chunking** (`Pneuma.Chunking` + both tokenizer adapters); the exact-parity harness (§4.4) must assert byte-identical boundaries against Partio before proceeding.\n4. [ ] **Prompt system completion** + dashboard editor.\n5. [ ] **Flip default to native**, soak, then **remove Partio** from both compose files, delete the client, and drop the config block.\n\nEach phase gates on green `dotnet run --project src/Test.Automated`, all three dashboard builds, and the file-size guardrail.\n\n---\n\n## 9. Version bump (`0.1.0`)\n- [ ] Bump `<Version>` in `src/Pneuma.Core/Pneuma.Core.csproj` and `src/Pneuma.Server/Pneuma.Server.csproj` (and any other packable csprojs).\n- [ ] Reconcile the README badge (currently `0.1.0`) and `DOCKERHUB_README`.\n- [ ] Set the `pneuma-*` image tags in `docker/compose.yaml` and `docker/factory/compose.yaml` to the release version (currently `v0.1.0`).\n- [ ] Move the `CHANGELOG.md` `[Unreleased]` content under a dated `## [0.1.0]` heading with the Partio removal as the lead item.\n- [ ] Bump SDK package versions where they track the server version.",
          "format": "md",
          "labels": [
            "partio-removal-plan"
          ],
          "tags": {}
        },
        {
          "id": "partio-removal-plan--risks-and-done",
          "category": "partio-removal-plan",
          "title": "Partio Removal Plan — 10. Risks / Definition of done",
          "summary": "Main risks of the migration and the definition of done.",
          "body": "## 10. Risks\n- **Chunk/tokenizer parity** is the highest-risk correctness item. The decision is exact parity, so the risk is concentrated in the §4.4 harness: if native boundaries ever diverge from Partio's, re-ingested vectors shift under an unchanged collection. Mitigated by making byte-identical parity a hard release gate before the default flips.\n- **Dimensionality mismatch** between an in-house embedder and a collection's fixed dimensionality causes insert/search failures; the §4.2 guard is mandatory, not optional.\n- **Provider-coverage parity** — confirm PolyPrompt covers every provider currently configured (OpenAI/Azure/Gemini/Ollama/Bedrock verified in source; check anything else).\n- **Lost Partio surfaces** — its dashboard and per-model request history go away; Pneuma's own request-history and telemetry must be confirmed to cover the operator's needs before deletion.\n- **Ownership** — Pneuma now maintains a chunker, a tokenizer dependency, and an endpoint registry; the ported chunker is a fork that can diverge from upstream Partio.\n\n## Definition of done\nPartio is no longer a component of Pneuma: the server and dashboard are gone from both compose files, `IPartioClient`/`PartioClient` and the Partio config block are deleted, and no reference to Partio remains anywhere in the tree. Ingestion and retrieval run entirely on native processing with byte-identical chunk parity proven by the §4.4 harness. Every function is manageable end to end across all surfaces (§6), the requirements checklist (§7) passes, and the full test suite, all three dashboard builds, and the file-size guardrail are green. The release is `0.1.0` with the CHANGELOG, README, DOCKERHUB_README, REST_API, MCP_API, and Postman collection all in sync. All work is committed on `main` (no feature branch).",
          "format": "md",
          "labels": [
            "partio-removal-plan"
          ],
          "tags": {}
        },
        {
          "id": "ports--ports-and-credentials",
          "category": "ports",
          "title": "Ports & Default Credentials — Dashboards / Services",
          "summary": "Every dashboard URL and service port in the stack with its default login or token.",
          "body": "# Pneuma — Ports & Default Credentials\n\nEverything is published on `localhost` by `docker compose up`. **Change these defaults before exposing any service.**\n\n## Dashboards\n\n| Dashboard | URL | Login |\n|-----------|-----|-------|\n| Pneuma Admin | http://localhost:3010 | `admin@pneuma` / `password` |\n| Pneuma Subject | http://localhost:3011 | `admin@pneuma` / `password` |\n| Pneuma User | http://localhost:3012 | `admin@pneuma` / `password` |\n| Grafana | http://localhost:3000 | `admin` / `admin` |\n| LiteGraph UI | http://localhost:3001 | token `litegraphadmin` |\n| DocumentAtom UI | http://localhost:3002 | none |\n| RecallDB dashboard | http://localhost:8601 | token `recalldbadmin` |\n\n## Services\n\n| Service | Port | Auth |\n|---------|------|------|\n| Pneuma API | 8080 | `admin@pneuma` / `password`; admin key `pneumaadmin` |\n| LiteGraph | 8701 | `litegraphadmin` |\n| DocumentAtom | 8000 | none |\n| RecallDB | 8600 | `recalldbadmin` |\n| Ollama | 11434 | none |\n| Prometheus | 9090 | none |\n| Tempo | 3200 (OTLP 4317/4318) | none |\n| Postgres | 15432 | `pneuma` / `pneuma` |",
          "format": "md",
          "labels": [
            "ports"
          ],
          "tags": {}
        },
        {
          "id": "test-plan--current-state",
          "category": "test-plan",
          "title": "Test Plan — 1. Current state",
          "summary": "Existing test harness, case counts per suite, strengths, and CI jobs.",
          "body": "# Pneuma Test Plan — coverage assessment and extension plan\n\nStatus: **draft for review.** This document assesses the current automated-test infrastructure, identifies\ngaps by layer, and proposes a prioritized plan to reach \"every layer, every service, every interface, every\nAPI — with positive and negative cases.\" It does not change any test yet.\n\n## 1. Current state\n\n**Harness.** Touchstone descriptors in `Test.Shared/Suites/*` (no console output), run three ways:\n`Test.Automated` (CLI), `Test.Xunit`, `Test.Nunit`. Shared helpers in `Test.Shared/Support/`:\n`TestServer` (in-process `PneumaServer` over SQLite with **fake** integrations —\n`FakeRecallDbClient`, `FakeLiteGraphClient`, `FakeDocumentAtomClient`, `FakeContentFetcher`,\n`FakeGraphRepositoryFactory`), `TestDatabase` (provider chosen by `PNEUMA_TEST_DB_TYPE`, default SQLite),\nand HTTP message handlers for resilience testing (`Recording`/`Sequenced`/`Delaying`).\n\n**Volume.** 110 cases across 8 suites: Security (12), Database (30), Graph (4), GraphTenancy (2),\nIngestion (5), ExternalServices (18), Api (37), Collections (2).\n\n**Strengths.** Good negative-case discipline already exists in spots (wrong password, unknown access key,\ntamper token, malformed AES, slug clash, missing displayName, unknown document type, no-collection failure,\ntransaction rollback, oversized-maxResults clamp). Resilience (retry/timeout/bulkhead/write-no-retry),\nprovenance immutability, retention pruning, tenant isolation (users, collections), and cascade deletes are\ncovered. The RBAC decision engine has focused unit tests (deny-wins, implicit-deny, wildcard, write-expansion).\n\n**CI.** `backend` job (SQLite, net8), `postgresql-live` job (live Postgres via `PNEUMA_TEST_DB_TYPE`),\n`dashboards` matrix (build + `vitest run`), `file-size-guardrail`. Tests run on **net8 only** in CI though the\nprojects multi-target net8/net10.",
          "format": "md",
          "labels": [
            "test-plan"
          ],
          "tags": {}
        },
        {
          "id": "test-plan--gap-analysis",
          "category": "test-plan",
          "title": "Test Plan — 2. Gap analysis (by layer)",
          "summary": "Coverage gaps by layer: provider matrix, HTTP routes, data layer, services, integrations, MCP, SDKs, dashboards.",
          "body": "## 2. Gap analysis (by layer)\n\n### 2.1 Provider matrix — **the single biggest gap**\nCLAUDE.md and the requirements state the shared DB contract suites must run against **all four** providers\n(Sqlite, Postgresql, Mysql, SqlServer). CI runs only **SQLite** (default) and **Postgresql** (live job).\n**MySQL and SQL Server are never exercised** — handwritten SQL dialect differences (e.g. `IF NOT EXISTS`\nvs `IF COL_LENGTH(...)`, identifier quoting, `LIMIT` vs `TOP`, boolean/JSON column handling) are unverified,\nso a provider-specific migration or query bug ships undetected.\n\n### 2.2 API / HTTP layer — large coverage holes\n117 registered endpoints; ApiSuite exercises ~30 of them. Whole route families have **zero HTTP-level tests**\n(they may have DB-layer coverage, but not the route: validation, status codes, authz-by-role, tenant scoping,\nserialization shape):\n\n- **RBAC admin CRUD**: `tenants`, `users`, `credentials`, `roles`, `permissions`, `assignments`,\n  `model-runners`, `prompts` — only one route is touched (`NonAdmin_Forbidden`). No create/read/update/delete\n  + 400/404/403/cross-tenant tests.\n- **audit** (`GET /v1.0/audit`) — no API test.\n- **graph** (`GET /v1.0/graph/nodes/{id}`, `/edges`, `/neighbors`) — no API test.\n- **search** (`GET /v1.0/search`, `GET /v1.0/subjects/{id}/search`) — no API test.\n- **history** (`GET /v1.0/history`, `/{id}`) — DB layer only.\n- **feedback** (`GET /v1.0/feedback`, `POST /v1.0/feedback`) — no API test.\n- **ingestion endpoints** (`GET /v1.0/ingestion/endpoints`), **jobs** `summary`/`restart`, **link artifacts**\n  (`source`/`atoms`/`chunks`/`vectors`/`subgraph`/`log`) — untested.\n- **subject update/delete** (`PUT`/`DELETE /v1.0/subjects/{id}` → 202 + background worker) — untested at API.\n- **eval facts REST** (`POST`/`GET`/`DELETE /v1.0/eval/facts`) — MCP twin tested, REST not directly.\n- **warmup** (`POST /v1.0/warmup`), **model-runner health REST**, **by-slug** — untested at API.\n- **CORS preflight / PostRouting hooks / 404 + 405 handling** — untested.\n\n### 2.3 Data layer — interfaces with no round-trip test\nDatabaseSuite covers subjects, tenants, users, credentials, chat turns/threads/tool-calls/perf-events/feedback,\neval facts/runs/results, ingestion jobs+events, links. **No DB round-trip/negative tests** for:\n`accounts`, `administrators`, `audit`, `authsessions`, `permissions`, `roles`, `rolepermissionmaps`,\n`userroleassignments`, `userrolemaps`, `credentialscopeassignments`, `modelrunners`, `requesthistory`.\nAlso missing: composite-unique-index violation behavior, `TenantId` scoping on every enumerate, and\npagination/order/search parameters on `EnumerationResult` per repository.\n\n### 2.4 Services — untested or thinly-tested\nNo dedicated tests for: `AgenticChatService` (tool loop, max-iteration cap, compaction trigger, think-parse,\ntitle summarization, tool-trace persistence, effective-filter merge), `GroundedQueryService`\n(`RewriteQuestionAsync` subject-grounding + no-placeholder, `ResolveEffectiveFilterAsync` union-merge, rerank,\nneighbor expansion, insufficient-support refusal), `ModelRunnerGate` (admission, `ModelRunnerBusyException`,\nqueue-depth), `ModelHealthMonitor` (base-URL dedup, healthy/unhealthy hysteresis, rolling history),\n`RequestHistoryCaptureService` (secret redaction + body truncation + retention prune — only \"captured\" is\nasserted), `IngestionWorkerService`/`EvalWorkerService` (claim-loop, gate yielding), `SubjectDeletionWorker`\n(background cascade + resume-on-startup), `CollectionResolver`,\n`LiteGraphInitializer`, `SettingsRedactor` (unit), `CascadeDeletionService` (direct, beyond the DB/ingestion\npaths). `AnalyticsService`, provisioners, and diagnostics are reasonably covered.\n\n### 2.5 Integration interfaces — real-client contract holes\nRole interfaces have fakes; ExternalServices covers LiteGraph (clamp/read/round-trip), DocumentAtom\n(flatten/skip-binary), resilience, and vector/inverted-index facet honoring, plus a live RecallDB test gated on\n`PNEUMA_LIVE_STACK=1`. Gaps: the native chunking/embedding/summarization path (`Pneuma.Chunking` + PolyPrompt)\nhas no dedicated processing-contract test, **`IContentFetcher`** (headless fetch/scroll —\nhard, at least a plain-HTTP-fallback test), and **`IServiceProbe`** per-service classification beyond the\naggregate diagnostics test. Live-stack tests exist for RecallDB only; LiteGraph/DocumentAtom have no\nopt-in live contract test.\n\n### 2.6 MCP surface\nGood breadth now (tools/list, paging, clamp, subject CRUD, query/search/stream, eval write, threads,\nfacets, ops). Gaps: **per-tool RBAC negative** (a Read-only credential is 403'd on each write/delete/admin\ntool — currently only one generic unauthenticated test), the **admin-only ops tools denied to a non-admin**,\nand **enumeration `endOfResults`/`recordsRemaining` correctness across multiple pages** for the newly-paged\ntools.\n\n### 2.7 SDKs\n`sdk/js/test`, `sdk/python/tests`, and `sdk/csharp/src/Test.Automated` exist but **no CI job runs them** — SDK\ndrift from the REST contract is undetected. Coverage completeness within each SDK is also unaudited.\n\n### 2.8 Dashboards\nAll three dashboards have `vitest` wired and a `dashboards` CI job, but there appear to be **no `*.test.jsx`\nfiles** — the harness runs green with zero tests. No coverage of `ApiClient`, i18n fallbacks, `RetrievalFilter`\nhelpers, `streamSse`, or critical components (ScopeFilter, ThreadSwitcher, DataTable paging, ConversationsView).\n\n### 2.9 Positive/negative balance\nNegative cases exist but are concentrated in auth/security/ingestion. Most CRUD and every new route family lack\nthe negative half: 400 (validation), 404 (missing), 403 (wrong role), 409 (uniqueness), and cross-tenant\nisolation at the HTTP layer. Target: **every mutating endpoint has at least one positive and one negative\ncase; every enumerate has a tenant-isolation case.**",
          "format": "md",
          "labels": [
            "test-plan"
          ],
          "tags": {}
        },
        {
          "id": "test-plan--plan-and-conventions",
          "category": "test-plan",
          "title": "Test Plan — 3. Proposed plan / 4. Infrastructure / 5. Conventions / 6. Sizing",
          "summary": "Prioritized P0–P3 plan (with done items), infrastructure improvements, test conventions, and sizing.",
          "body": "## 3. Proposed plan (phased, prioritized)\n\n### P0 — Close the correctness-critical gaps (do first)\n1. **[DONE] Run the DB contract suite on all four providers in CI.** Added `mysql-live` and `sqlserver-live`\n   jobs mirroring `postgresql-live`, each setting `PNEUMA_TEST_DB_TYPE`. SQLite remains the default job. The\n   runner (`Test.Automated`) also now accepts DB connection details as **CLI arguments**\n   (`--type/--host/--port/--user/--pass/--schema/--database`, mapped onto the `PNEUMA_TEST_DB_*` env vars that\n   `TestDatabase` reads, so xUnit/NUnit honor the same config); `TestDatabase` supports `--schema` and a\n   supplied maintenance/base database, preserving per-case isolation.\n2. **[DONE] DB round-trip + negative tests for the untested repositories** (2.3): added `AuthDatabaseSuite`\n   (accounts, administrators, authsessions, credentials, credentialscopeassignments), `RbacDatabaseSuite`\n   (permissions, roles, rolepermissionmaps, userroleassignments, userrolemaps, audit), and `OpsDatabaseSuite`\n   (modelrunners, requesthistory, prompts). Each: create/read/update/delete, read-missing→null, unknown\n   natural-key lookup, tenant-scope isolation, and prune where applicable — run across all four providers via\n   P0.1. (Remaining nice-to-have: composite-unique-violation assertions.)\n3. **HTTP CRUD + authz + negative for the RBAC admin route families** (2.2): tenants, users, credentials,\n   roles, permissions, assignments, model-runners, prompts. Each: happy CRUD, 400 on bad body, 404 on missing,\n   403 for an under-privileged role, and cross-tenant 404/deny. Extend `TestServer` with a helper to mint a\n   scoped non-admin credential.\n\n### P1 — Service-level unit tests (2.4)\n4. `GroundedQueryService`: rewrite grounds in subject name and never invents a placeholder (regression for the\n   \"medication X\" fix); effective-filter union-merge (narrows never widens); insufficient-support refusal;\n   neighbor-expansion bound; rerank ordering (fake reranker).\n5. `AgenticChatService`: max-iteration cap forces a final answer; compaction triggers past the threshold;\n   think-parse strips `<think>`; title summarization fires past 500 chars and freezes on manual rename;\n   tool-call trace persists; per-request filter merges with the subject default.\n6. `ModelRunnerGate` (admission + `ModelRunnerBusyException` + queue depth), `ModelHealthMonitor` (base-URL\n   dedup, healthy/unhealthy hysteresis, rolling history), `RequestHistoryCaptureService` (redaction +\n   truncation + retention prune), `CollectionResolver`, `SettingsRedactor` (unit), `SubjectDeletionWorker`\n   (cascade + resume), worker services (claim-loop + gate yield).\n\n### P2 — Full API + MCP breadth (2.2, 2.6)\n7. Remaining endpoints: graph reads, search (both), history, feedback, ingestion endpoints, job summary/restart,\n   link artifacts, subject update/delete (202 + worker), eval facts REST, warmup, by-slug, model-runner health\n   REST, `/metrics` content, 404/405, CORS preflight. Each with a positive and a negative case.\n8. MCP per-tool RBAC negative matrix (a table-driven test: for each write/delete/admin tool, a low-privilege\n   credential gets a JSON-RPC \"Not permitted\" error) and multi-page enumeration correctness\n   (`endOfResults`/`recordsRemaining`).\n\n### P3 — Integration contracts, SDKs, dashboards (2.5, 2.7, 2.8)\n9. Real-client contract tests behind `PNEUMA_LIVE_STACK=1` for LiteGraph and DocumentAtom, plus a\n   native-processing test for `Pneuma.Chunking` + PolyPrompt (summarize/chunk/embed), mirroring the existing\n   gated RecallDB test; plus a plain-HTTP `IContentFetcher` fallback test.\n10. **CI job to run the SDK test suites** (js/pytest/csharp) against an in-process or live server; audit each\n    SDK for method-per-endpoint parity with the REST surface.\n11. Dashboard `vitest` tests for `ApiClient` (request building, error mapping), `streamSse`, i18n fallback\n    resolution, `RetrievalFilter`/`isEmptyFilter` helpers, and the high-value components (DataTable paging,\n    ScopeFilter, ThreadSwitcher, ConversationsView).\n\n## 4. Infrastructure improvements\n- **Harness helpers**: `TestServer.CreateScopedCredentialAsync(role)` for authz-negative tests;\n  `McpCall(tool, argsJson)` + `McpResultText` (already added) promoted to a shared helper; a\n  `ProviderMatrix` note in `BACKEND_TEST_ARCHITECTURE.md`.\n- **Coverage measurement**: add `coverlet` to the test run and publish a coverage summary in CI (target a\n  ratchet, not a hard number initially) so regressions in coverage are visible.\n- **Run tests on net10 in CI** too (currently net8 only), since the code multi-targets.\n- **Live-stack CI lane** (optional, nightly): a compose-up job that flips `PNEUMA_LIVE_STACK=1` and runs the\n  gated integration + SDK contract tests against the real services.\n\n## 5. Conventions for new tests (per requirements)\n- Touchstone descriptors in `Test.Shared/Suites`, no console output; register in `PneumaSuites`.\n- Every mutating endpoint/tool: ≥1 positive and ≥1 negative (400/404/403/409) case.\n- Every enumerate: a tenant-isolation case and a paging (`endOfResults`) case.\n- DB contract tests live in `DatabaseSuite`-style suites so they run across all four providers automatically.\n- Bind test servers to `127.0.0.1`; keep fakes deterministic; no reliance on wall-clock (`Date.now` etc.).\n\n## 6. Rough sizing\n- P0: ~1 CI change + ~50–70 DB/HTTP cases. P1: ~30–40 service cases. P2: ~60–80 API/MCP cases.\n- P3: ~20–30 integration/SDK cases + a dashboard test baseline.\n- End state: on the order of ~350–400 backend cases with the four-provider matrix, plus SDK and dashboard lanes\n  wired into CI.",
          "format": "md",
          "labels": [
            "test-plan"
          ],
          "tags": {}
        }
      ],
      "queries": [
        {
          "id": "q001",
          "text": "Is this meant to be a polished app for end users, or a foundation that developers build on top of?",
          "type": "paraphrase",
          "relevant": [
            "readme--overview"
          ],
          "answer": "It is positioned as an intelligence layer you build on, not a finished end-user product.",
          "evidence": [
            "you build on — not a finished end-user product"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q002",
          "text": "What does the platform offer people who are building retrieval-augmented generation applications?",
          "type": "paraphrase",
          "relevant": [
            "readme--overview"
          ],
          "answer": "A production-grade RAG backend: hybrid retrieval, graph-neighbor expansion, prompt rewrite, optional re-ranking, compaction, cited answers with an explicit refusal, an eval harness, and MCP.",
          "evidence": [
            "get a **production-grade RAG backend** out of the box"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q003",
          "text": "What does the operator's landing page show right after signing in to the admin console?",
          "type": "paraphrase",
          "relevant": [
            "readme--screenshots"
          ],
          "answer": "Deployment state (tenants, users, subjects, links, queued/failed ingestion jobs) above a live success-vs-failed request-activity chart with selectable time ranges.",
          "evidence": [
            "The admin home surfaces the current state of a deployment"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q004",
          "text": "Do I have to install .NET or Node on my machine just to run the thing?",
          "type": "paraphrase",
          "relevant": [
            "readme--quick-start"
          ],
          "answer": "No. Only Docker and Docker Compose are needed; no language toolchain is required to run it.",
          "evidence": [
            "You do **not** need the .NET SDK, Node, or any language toolchain to run it"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q005",
          "text": "How do I wipe my local deployment and start over with fresh databases and the seeded admin?",
          "type": "paraphrase",
          "relevant": [
            "readme--quick-start"
          ],
          "answer": "Run docker/reset.bat (Windows) to reset the stack to factory defaults; docker/update.bat pulls the latest images and restarts.",
          "evidence": [
            "use `docker/reset.bat` (Windows)"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q006",
          "text": "Which library sits between the server and the S3-compatible store that keeps pipeline artifacts?",
          "type": "paraphrase",
          "relevant": [
            "readme--architecture"
          ],
          "answer": "Object storage is Less3 (S3-compatible), accessed via Blobject.",
          "evidence": [
            "**Less3** (S3-compatible) via **Blobject**"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q007",
          "text": "Which CPU architectures are the published container images built for?",
          "type": "paraphrase",
          "relevant": [
            "readme--building-sdks-contributing"
          ],
          "answer": "The build scripts use docker buildx to publish multi-architecture linux/amd64 and linux/arm64 images.",
          "evidence": [
            "multi-architecture (`linux/amd64` + `linux/arm64`) images"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q008",
          "text": "What shape does the body of a failed API call have?",
          "type": "paraphrase",
          "relevant": [
            "rest-api--authentication"
          ],
          "answer": "A JSON object with an error code, a message, and an optional context field.",
          "evidence": [
            "Errors use `{ \"error\": \"<code>\", \"message\": \"<text>\", \"context\": <optional> }`"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q009",
          "text": "What's the cheapest way to learn how many records a list endpoint holds without downloading them?",
          "type": "paraphrase",
          "relevant": [
            "rest-api--pagination"
          ],
          "answer": "Request the list with ?maxResults=1 and read totalRecords, which is the post-filter, pre-paging count.",
          "evidence": [
            "so a client can read a total cheaply with `?maxResults=1`"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q010",
          "text": "If the graph or vector backends are down when I add a new organization, does that operation fail?",
          "type": "paraphrase",
          "relevant": [
            "rest-api--tenancy-rbac-audit"
          ],
          "answer": "No. Provisioning is best-effort: an unavailable subordinate service never blocks tenant creation and is retried on next boot.",
          "evidence": [
            "an unavailable subordinate service never blocks tenant creation and is retried on next boot"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q011",
          "text": "Can I view a machine key's secret again after it has been issued?",
          "type": "paraphrase",
          "relevant": [
            "rest-api--tenancy-rbac-audit"
          ],
          "answer": "No. The raw secretKey is returned only once, at credential creation.",
          "evidence": [
            "the raw `secretKey` is returned **once**"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q012",
          "text": "Where does the short one-line blurb configured for a subject appear to users?",
          "type": "paraphrase",
          "relevant": [
            "rest-api--subjects"
          ],
          "answer": "The tagline is shown as the subtitle beneath the subject's name on its ask page.",
          "evidence": [
            "`tagline` is the subtitle shown beneath the subject's name on its ask page"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q013",
          "text": "If I save a subject's custom instruction override as an empty string, what happens?",
          "type": "paraphrase",
          "relevant": [
            "rest-api--subject-prompts"
          ],
          "answer": "Empty content clears the override, so the subject falls back to the global prompt.",
          "evidence": [
            "**Empty `content` clears the override**"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q014",
          "text": "Can someone leave a remark on an answer without giving it a thumbs up or down?",
          "type": "paraphrase",
          "relevant": [
            "rest-api--chat-history-feedback"
          ],
          "answer": "Yes. Feedback requires a rating and/or a comment, so a comment alone is accepted (an unknown turnId returns 404).",
          "evidence": [
            "Requires a rating and/or comment"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q015",
          "text": "When I force a source to be processed again, how does the system avoid skipping work because nothing seems to have changed?",
          "type": "paraphrase",
          "relevant": [
            "rest-api--links-ingestion"
          ],
          "answer": "Reingest clears the link's stored content hash first so the delta-skip cannot short-circuit, then enqueues a brand-new job (202 Accepted).",
          "evidence": [
            "The link's stored content hash is cleared first so the delta-skip can't short-circuit the pipeline"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q016",
          "text": "If two sources both mention the same entity and I remove one source, does the entity disappear from the graph?",
          "type": "paraphrase",
          "relevant": [
            "rest-api--links-ingestion"
          ],
          "answer": "No. Deleting a link cascades its own graph assertions, but shared entity nodes reused by other links are preserved.",
          "evidence": [
            "Shared entity nodes reused by other links are preserved."
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q017",
          "text": "If I remove a single processing job, are the source's stored pipeline outputs deleted too?",
          "type": "paraphrase",
          "relevant": [
            "rest-api--links-ingestion"
          ],
          "answer": "No. Job deletion cascades its log, graph assertions, chunk documents, and raw blob, but the link and its per-link S3 artifacts are left intact.",
          "evidence": [
            "The link and its per-link S3 pipeline artifacts are left intact"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q018",
          "text": "Can I trigger an immediate reachability probe of one model backend, and what if probing is switched off for it?",
          "type": "paraphrase",
          "relevant": [
            "rest-api--model-runners-prompts-settings"
          ],
          "answer": "Yes: POST /v1.0/model-runners/{id}/health/check runs one probe immediately; it returns 400 if health checks are disabled for that endpoint and 404 if not found.",
          "evidence": [
            "Run a single health probe against the endpoint **immediately**",
            "`400` if health checks are disabled for the endpoint"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q019",
          "text": "Which parts of the server configuration take effect without restarting the process?",
          "type": "paraphrase",
          "relevant": [
            "rest-api--model-runners-prompts-settings"
          ],
          "answer": "Most sections need a restart; cors and requestHistory apply live, as does the ingestion tuning saved via /v1.0/settings/ingestion.",
          "evidence": [
            "`cors` and `requestHistory` are applied live",
            "**Applied live — no restart required**"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q020",
          "text": "Is there a way to rename or otherwise edit a vector store after it has been created?",
          "type": "paraphrase",
          "relevant": [
            "rest-api--graph-and-collections"
          ],
          "answer": "No. Collections support only create, read, list, and delete; there is no update.",
          "evidence": [
            "Collections are create/read/list/delete (no update)."
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q021",
          "text": "After the server condenses a long chat to fit the model, what should the client do with the messages it is holding?",
          "type": "paraphrase",
          "relevant": [
            "rest-api--search"
          ],
          "answer": "When the complete event has compacted=true, the client should replace its prior message history with compactedSummary.",
          "evidence": [
            "when `compacted` is true the client should replace its prior message history with `compactedSummary`"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q022",
          "text": "How is a tool's output packaged when it comes back to an agent over the protocol?",
          "type": "paraphrase",
          "relevant": [
            "mcp-api--authentication-and-methods"
          ],
          "answer": "As MCP content: a content array with one text item holding the tool's JSON payload, plus isError false.",
          "evidence": [
            "{ \"content\": [ { \"type\": \"text\", \"text\": \"<JSON>\" } ], \"isError\": false }"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q023",
          "text": "Is there any agent tool that returns an entire collection in a single call?",
          "type": "paraphrase",
          "relevant": [
            "mcp-api--enumerating-objects"
          ],
          "answer": "No. Collections are only read through paged enumerate tools plus get-by-id tools; there is no list-everything tool.",
          "evidence": [
            "There is no \"list everything\" tool."
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q024",
          "text": "Can an agent submit a new URL for processing through the tool interface today?",
          "type": "paraphrase",
          "relevant": [
            "mcp-api--tools"
          ],
          "answer": "Not yet. An ingest-a-link tool is listed as a future tool that will follow the same contract when it lands.",
          "evidence": [
            "Further tools (ingest a link) follow the same contract as they land."
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q025",
          "text": "Through the agent interface, how do I wipe all of a subject's custom per-stage parallelism limits at once?",
          "type": "paraphrase",
          "relevant": [
            "mcp-api--concurrency-overrides"
          ],
          "answer": "Call pneuma_update_subject with concurrencyOverrides set to null, which clears the subject's overrides.",
          "evidence": [
            "passing `concurrencyOverrides: null` clears the subject's overrides"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q026",
          "text": "When an agent asks for a streamed answer, how can it tell which event carries the final result?",
          "type": "paraphrase",
          "relevant": [
            "mcp-api--metadata-filter-and-streaming"
          ],
          "answer": "The response is an SSE stream of metadata and delta events, and the final SSE event is the JSON-RPC result message.",
          "evidence": [
            "the **final** SSE event is the JSON-RPC result message"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q027",
          "text": "Is per-answer timing only visible in the monitoring stack, or is it also kept in the application database?",
          "type": "paraphrase",
          "relevant": [
            "telemetry--traces-and-collection"
          ],
          "answer": "It is also persisted on each chat turn (performanceJson and the chatturnperfevents table), a DB-native complement to Prometheus and Tempo.",
          "evidence": [
            "DB-native complement to the Prometheus metrics and Tempo spans"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q028",
          "text": "Do the counters survive a server restart?",
          "type": "paraphrase",
          "relevant": [
            "telemetry--workflows-and-notes"
          ],
          "answer": "No. Metrics are per-process and in-memory and reset on restart; Prometheus retains the history.",
          "evidence": [
            "Metrics are per-process and in-memory (reset on restart); Prometheus retains the history."
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q029",
          "text": "How are the proposed improvements ranked, and how are ties broken?",
          "type": "paraphrase",
          "relevant": [
            "ckg-improvements--scoring-and-table"
          ],
          "answer": "Each item is scored 1–5 on Simplicity and Value, Total is their sum (max 10), and ties are ordered by Value then Simplicity.",
          "evidence": [
            "Ties are ordered by Value, then Simplicity."
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q030",
          "text": "Before the fusion change, how were keyword hits and embedding hits combined?",
          "type": "paraphrase",
          "relevant": [
            "ckg-improvements--items-1-4"
          ],
          "answer": "A naive dedup-union: full-text hits first, vector hits appended, duplicates dropped by graph-node id, with no score normalization or fusion.",
          "evidence": [
            "merges them with a naive dedup-union"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q031",
          "text": "Why might two spellings of the same company end up as separate entities in the graph?",
          "type": "paraphrase",
          "relevant": [
            "ckg-improvements--items-9-11"
          ],
          "answer": "Entity resolution is an exact (nodeType, canonicalName) string match, so spelling, casing, or abbreviation variants create distinct nodes.",
          "evidence": [
            "an **exact `(nodeType, canonicalName)` string match**"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q032",
          "text": "Why did the single-shot streamed answer used to have no better time-to-first-token than the non-streaming call?",
          "type": "paraphrase",
          "relevant": [
            "ckg-improvements--items-12-15-and-sequencing"
          ],
          "answer": "It generated the full answer first and then re-chunked it into fake SSE deltas.",
          "evidence": [
            "then re-chunks it client-side into fake SSE deltas"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q033",
          "text": "Am I allowed to declare local variables with implicit typing in the server code?",
          "type": "paraphrase",
          "relevant": [
            "claude--code-style"
          ],
          "answer": "No. `var` is banned; use the actual type.",
          "evidence": [
            "**No `var`.** Use the actual type."
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q034",
          "text": "What should I use instead of printing to the console inside library code?",
          "type": "paraphrase",
          "relevant": [
            "claude--code-style"
          ],
          "answer": "No Console.WriteLine in library code; use SyslogLogging.",
          "evidence": [
            "**No `Console.WriteLine` in library code** — use SyslogLogging."
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q035",
          "text": "Can the dashboards pull in a third-party charting package?",
          "type": "paraphrase",
          "relevant": [
            "claude--auth-and-frontend"
          ],
          "answer": "No. Charts use a hand-rolled SVG ActivityChart; no charting library (and no axios).",
          "evidence": [
            "hand-rolled SVG `ActivityChart` (no charting lib)"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q036",
          "text": "Why were ingestion jobs sometimes stuck forever waiting for a free slot, and what replaced the limiter?",
          "type": "paraphrase",
          "relevant": [
            "changelog--unreleased-latest"
          ],
          "answer": "A contended stage's abandoned acquire leaked the permit; the stage runner now always awaits and releases, and the third-party Padlock limiter was replaced by a SemaphoreSlim-based gate.",
          "evidence": [
            "Padlock limiter was replaced with a self-contained `SemaphoreSlim`-based gate"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q037",
          "text": "When does a new conversation get renamed by the model instead of keeping the first question as its title?",
          "type": "paraphrase",
          "relevant": [
            "changelog--unreleased-added-1"
          ],
          "answer": "Once a turn's prompt plus response exceeds 500 characters, the answering model summarizes it into a short chat title.",
          "evidence": [
            "once a turn's prompt + response exceeds 500 characters"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q038",
          "text": "Why could a vague follow-up question end up answered about an imaginary product that doesn't exist?",
          "type": "paraphrase",
          "relevant": [
            "changelog--unreleased-fixed"
          ],
          "answer": "The query-rewrite step invented a placeholder name; it is now grounded in the subject's display name and forbidden from introducing hypothetical names.",
          "evidence": [
            "The rewrite step is now grounded in the subject's"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q039",
          "text": "Which model calls never went through the external processing service even before the migration?",
          "type": "paraphrase",
          "relevant": [
            "partio-removal-plan--current-use-and-target"
          ],
          "answer": "Completions (answering, chat, classification, rerank, rewrite) already went direct to the provider.",
          "evidence": [
            "Completions (answering, chat, classification, rerank, rewrite) already bypass Partio entirely"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q040",
          "text": "Do the web dashboards have any unit tests in place?",
          "type": "paraphrase",
          "relevant": [
            "test-plan--gap-analysis"
          ],
          "answer": "Effectively no: vitest and a CI job are wired, but there appear to be no *.test.jsx files, so the harness runs green with zero tests.",
          "evidence": [
            "the harness runs green with zero tests"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q041",
          "text": "What does `pneuma_ingestion_stage_duration_seconds` measure?",
          "type": "lexical",
          "relevant": [
            "telemetry--metrics"
          ],
          "answer": "A per-stage ingestion duration histogram, labeled by stage — where stalls show up.",
          "evidence": [
            "`pneuma_ingestion_stage_duration_seconds{stage}` — per-stage duration histogram"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q042",
          "text": "What does the `pneuma_authz_decisions_total` metric count?",
          "type": "lexical",
          "relevant": [
            "telemetry--metrics"
          ],
          "answer": "Authorization permit/deny decisions, labeled by result.",
          "evidence": [
            "`pneuma_authz_decisions_total{result}` — permit/deny decisions."
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q043",
          "text": "What does `GET /v1.0/jobs/live` return?",
          "type": "lexical",
          "relevant": [
            "rest-api--links-ingestion",
            "changelog--unreleased-latest"
          ],
          "answer": "An IngestionLiveSnapshot with running, waitingForSlot, and queued lists of jobs (jobId, subjectId, sourceUrl, stage, stateSinceUtc), longest-in-state first.",
          "evidence": [
            "**Live pipeline snapshot** → `IngestionLiveSnapshot`"
          ],
          "hops": 2,
          "category": null,
          "filter": null
        },
        {
          "id": "q044",
          "text": "What is the `x-tenant-guid` header used for?",
          "type": "lexical",
          "relevant": [
            "rest-api--authentication"
          ],
          "answer": "Together with x-email and x-password it performs header login, used only for session creation.",
          "evidence": [
            "Header login (session-creation only)"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q045",
          "text": "What does `NeighborExpansionMaxHops` control?",
          "type": "lexical",
          "relevant": [
            "ckg-improvements--graphrag-implementation-note"
          ],
          "answer": "How far retrieval neighbor-expansion goes: default 1 keeps single-hop behavior; values above 1 use LiteGraph's bounded server-side subgraph.",
          "evidence": [
            "honours a `NeighborExpansionMaxHops` setting (default 1 = unchanged; >1 uses the bounded server-side subgraph)"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q046",
          "text": "What does `IngestionSettings.EmbeddingCacheSize` configure?",
          "type": "lexical",
          "relevant": [
            "ckg-improvements--litegraph-9-and-later-notes"
          ],
          "answer": "The global size limit of the bounded LRU embedding cache keyed by (embedding model, content hash); 0 disables the cache.",
          "evidence": [
            "with a **global system size limit** via `IngestionSettings.EmbeddingCacheSize` (0 disables)"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q047",
          "text": "What does `Diagnostics.FailFastOnStartupProbe` do?",
          "type": "lexical",
          "relevant": [
            "changelog--0-1-0-added-2"
          ],
          "answer": "When enabled, it aborts startup if the boot-time connectivity probe finds a required service unreachable.",
          "evidence": [
            "an optional `Diagnostics.FailFastOnStartupProbe`"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q048",
          "text": "What runs on port 11434 in the compose stack?",
          "type": "lexical",
          "relevant": [
            "ports--ports-and-credentials",
            "readme--quick-start"
          ],
          "answer": "Ollama, the local LLM inference engine, with no auth.",
          "evidence": [
            "Local LLM inference engine (embeddings + completions)"
          ],
          "hops": 2,
          "category": null,
          "filter": null
        },
        {
          "id": "q049",
          "text": "What does `POST /v1.0/links/reingest` accept and return?",
          "type": "lexical",
          "relevant": [
            "rest-api--links-ingestion"
          ],
          "answer": "A body of { ids: [...] }; it queues a fresh ingestion job per link and returns 202 Accepted with { queued, skipped }.",
          "evidence": [
            "Returns **202 Accepted** with `{ queued, skipped }`"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q050",
          "text": "What does `GET /v1.0/subjects/{id}/retrieval/tags` return?",
          "type": "lexical",
          "relevant": [
            "rest-api--search"
          ],
          "answer": "The distinct retrieval tag keys and values applied to the subject's links as { subjectId, tags: { key: [value, …] } }, a bounded aggregate.",
          "evidence": [
            "The distinct retrieval tag keys and their values applied to the subject's links"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q051",
          "text": "What parameters does the `pneuma_ingestion_summary` tool take?",
          "type": "lexical",
          "relevant": [
            "mcp-api--tools"
          ],
          "answer": "Optional subjectId, a fromUtc/toUtc window, and bucketMinutes (1–1440, default 15).",
          "evidence": [
            "Optional `subjectId`, `fromUtc`/`toUtc` window, and `bucketMinutes` (1–1440, default 15)"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q052",
          "text": "What is the `retrievalFilterJson` field on a subject?",
          "type": "lexical",
          "relevant": [
            "changelog--unreleased-added-2",
            "rest-api--search"
          ],
          "answer": "The subject's default retrieval facet filter (schema v12); per-request filters are merged with it as a union that narrows, never widens.",
          "evidence": [
            "(`retrievalFilterJson`, schema v12)"
          ],
          "hops": 2,
          "category": null,
          "filter": null
        },
        {
          "id": "q053",
          "text": "What does the `GF_SECURITY_ADMIN_*` setting control?",
          "type": "lexical",
          "relevant": [
            "telemetry--grafana-dashboards"
          ],
          "answer": "Grafana's default admin credentials (admin / admin), which should be changed for non-local deployments.",
          "evidence": [
            "(set via `GF_SECURITY_ADMIN_*`; change them for"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q054",
          "text": "What is `IGraphRepositoryFactory.ForTenantAsync` used for?",
          "type": "lexical",
          "relevant": [
            "claude--integration-contracts"
          ],
          "answer": "Resolving a per-tenant LiteGraph client for graph operations, falling back to the configured default tenant/graph when unprovisioned.",
          "evidence": [
            "resolve a per-tenant client via `IGraphRepositoryFactory.ForTenantAsync(tenantId)`"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q055",
          "text": "What does `PNEUMA_TEST_DB_TYPE` select?",
          "type": "lexical",
          "relevant": [
            "test-plan--current-state",
            "test-plan--plan-and-conventions"
          ],
          "answer": "The database provider the tests run against (default SQLite); the CI live jobs set it for Postgres, MySQL, and SQL Server.",
          "evidence": [
            "`TestDatabase` (provider chosen by `PNEUMA_TEST_DB_TYPE`, default SQLite)"
          ],
          "hops": 2,
          "category": null,
          "filter": null
        },
        {
          "id": "q056",
          "text": "What is stored in the `chatturnperfevents` table?",
          "type": "lexical",
          "relevant": [
            "changelog--unreleased-added-2",
            "telemetry--traces-and-collection"
          ],
          "answer": "One queryable row per answer-pipeline stage of each chat turn, used for analytics alongside performanceJson.",
          "evidence": [
            "one queryable `chatturnperfevents` row per stage for analytics"
          ],
          "hops": 2,
          "category": null,
          "filter": null
        },
        {
          "id": "q057",
          "text": "What were the `cep_` / `eep_` identifiers?",
          "type": "lexical",
          "relevant": [
            "partio-removal-plan--current-use-and-target",
            "partio-removal-plan--migration-and-surfaces"
          ],
          "answer": "Partio's completion/embedding endpoint records, used for endpoint management that Pneuma proxied, and seeded as Partio defaults by the factory stack.",
          "evidence": [
            "Endpoint management (`cep_`/`eep_`, config, health, concurrency, request history)",
            "instead of Partio `cep_`/`eep_` defaults"
          ],
          "hops": 2,
          "category": null,
          "filter": null
        },
        {
          "id": "q058",
          "text": "What does `PNEUMA_ADMIN_EMAIL` override?",
          "type": "lexical",
          "relevant": [
            "readme--quick-start"
          ],
          "answer": "The seeded administrator's email (with PNEUMA_ADMIN_PASSWORD for the password), see docker/pneuma.json.",
          "evidence": [
            "Override the seeded admin with `PNEUMA_ADMIN_EMAIL` / `PNEUMA_ADMIN_PASSWORD`"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q059",
          "text": "What values can a subject's `deletionStatus` take?",
          "type": "lexical",
          "relevant": [
            "rest-api--subjects"
          ],
          "answer": "None, Pending, Deleting, or Failed (read-only).",
          "evidence": [
            "a read-only `deletionStatus` (`None`/`Pending`/`Deleting`/`Failed`)"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q060",
          "text": "What is the `eval.judge` prompt?",
          "type": "lexical",
          "relevant": [
            "changelog--unreleased-added-2"
          ],
          "answer": "The seeded prompt used to LLM-grade each evaluation answer against the expected answer, producing a Pass/Partial/Fail verdict and 0–10 score.",
          "evidence": [
            "grade the result against the expected answer with a",
            "with verdict (Pass/Partial/Fail), 0–10"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q061",
          "text": "What has to be set on a subject before it can ingest content, and how would an agent configure that?",
          "type": "multi",
          "relevant": [
            "rest-api--subjects",
            "mcp-api--tools"
          ],
          "answer": "embeddingModel, inferenceModel, and collection are required; an agent sets them with pneuma_create_subject or pneuma_update_subject.",
          "evidence": [
            "are **required** before links can be ingested to it or questions answered about it",
            "`embeddingModel`/`inferenceModel`/`collection` are required before the subject can ingest or answer"
          ],
          "hops": 2,
          "category": null,
          "filter": null
        },
        {
          "id": "q062",
          "text": "How do labels get attached to content, and how does a question later get restricted to only chunks with a given label?",
          "type": "multi",
          "relevant": [
            "rest-api--links-ingestion",
            "rest-api--search"
          ],
          "answer": "Labels supplied at link submission are stamped on every chunk as label:L tags; a metadataFilter's requiredLabels then matches those tags (every required label must be present).",
          "evidence": [
            "Each label `L` is stored as a distinct chunk tag `label:L`",
            "Labels are a `List<string>` matched against each chunk's per-label `label:<value>` tags"
          ],
          "hops": 2,
          "category": null,
          "filter": null
        },
        {
          "id": "q063",
          "text": "Where do I open the vector store's own web UI, and does Pneuma keep its own copy of collection definitions?",
          "type": "multi",
          "relevant": [
            "ports--ports-and-credentials",
            "rest-api--graph-and-collections"
          ],
          "answer": "The RecallDB dashboard is at http://localhost:8601 (token recalldbadmin); Pneuma keeps no local collection state and relays administration to RecallDB, the authority.",
          "evidence": [
            "http://localhost:8601",
            "Pneuma simply relays administration to it and keeps no local collection state"
          ],
          "hops": 2,
          "category": null,
          "filter": null
        },
        {
          "id": "q064",
          "text": "The test plan flagged MySQL and SQL Server as untested — was that gap closed, and how?",
          "type": "multi",
          "relevant": [
            "test-plan--gap-analysis",
            "test-plan--plan-and-conventions"
          ],
          "answer": "Yes: CI originally ran only SQLite and Postgres; P0.1 is done, adding mysql-live and sqlserver-live jobs that set PNEUMA_TEST_DB_TYPE.",
          "evidence": [
            "**MySQL and SQL Server are never exercised**",
            "Added `mysql-live` and `sqlserver-live`"
          ],
          "hops": 2,
          "category": null,
          "filter": null
        },
        {
          "id": "q065",
          "text": "What did the LiteGraph 9 upgrade provide, and what did Pneuma build on it for GraphRAG?",
          "type": "multi",
          "relevant": [
            "ckg-improvements--litegraph-9-and-later-notes",
            "ckg-improvements--graphrag-implementation-note"
          ],
          "answer": "v9 adds server-side depth-limited subgraph extraction, path finding, and graph algorithms (incl. Louvain); Pneuma added GetSubgraphAsync/DetectCommunitiesAsync, a CommunityService that summarizes communities, and a global query mode.",
          "evidence": [
            "**depth-limited subgraph extraction**",
            "a new `CommunityService` runs Louvain over the tenant graph"
          ],
          "hops": 2,
          "category": null,
          "filter": null
        },
        {
          "id": "q066",
          "text": "How did hybrid retrieval merge lexical and vector results originally, and was the proposed fix delivered?",
          "type": "multi",
          "relevant": [
            "ckg-improvements--items-1-4",
            "ckg-improvements--scoring-and-table"
          ],
          "answer": "Originally a naive dedup-union with no fusion; Reciprocal-rank fusion (item 1) was proposed and is marked Done.",
          "evidence": [
            "merges them with a naive dedup-union",
            "Reciprocal-rank fusion for hybrid retrieval"
          ],
          "hops": 2,
          "category": null,
          "filter": null
        },
        {
          "id": "q067",
          "text": "How does agent-side paging compare with REST list paging in defaults and termination?",
          "type": "multi",
          "relevant": [
            "mcp-api--enumerating-objects",
            "rest-api--pagination"
          ],
          "answer": "Both use the EnumerationResult envelope; REST defaults maxResults to 100 (clamped 1..1000) with skip/order/search, and an agent advances skip by page size until endOfResults is true.",
          "evidence": [
            "Page size, clamped to `1..1000`",
            "Stop when `endOfResults` is `true`"
          ],
          "hops": 2,
          "category": null,
          "filter": null
        },
        {
          "id": "q068",
          "text": "Which metric captures latency of outbound calls to dependencies, and which Grafana board shows it?",
          "type": "multi",
          "relevant": [
            "telemetry--metrics",
            "telemetry--grafana-dashboards"
          ],
          "answer": "pneuma_integration_request_duration_seconds{service,operation}; the Pneuma — Integrations dashboard shows p95 latency per service+operation.",
          "evidence": [
            "`pneuma_integration_request_duration_seconds{service,operation}` — per-integration latency histogram",
            "the p95 latency per service+operation"
          ],
          "hops": 2,
          "category": null,
          "filter": null
        },
        {
          "id": "q069",
          "text": "Which keyed prompts exist globally, and how can a subject layer its own version on top?",
          "type": "multi",
          "relevant": [
            "rest-api--model-runners-prompts-settings",
            "rest-api--subject-prompts"
          ],
          "answer": "Global keyed prompts include ontology.classify, cell.summarize, and user.answer; a subject can override any key with mergeMode Append (after global) or Replace (override alone).",
          "evidence": [
            "Keyed prompts: `ontology.classify`, `cell.summarize`, `user.answer`.",
            "`Append` layers the override after the global prompt; `Replace` uses the override alone."
          ],
          "hops": 2,
          "category": null,
          "filter": null
        },
        {
          "id": "q070",
          "text": "Where are system-wide ingestion parallelism defaults set, and how does a subject's own setting interact with them?",
          "type": "multi",
          "relevant": [
            "rest-api--model-runners-prompts-settings",
            "rest-api--subjects",
            "mcp-api--concurrency-overrides"
          ],
          "answer": "System defaults are the IngestionTuning at /v1.0/settings/ingestion (applied live); a subject's concurrencyOverrides field wins when set, otherwise the default is inherited (override ?? system default).",
          "evidence": [
            "the system defaults every ingestion job uses",
            "(effective value = override ?? system default)"
          ],
          "hops": 3,
          "category": null,
          "filter": null
        },
        {
          "id": "q071",
          "text": "What REST route and what agent tool give per-subject chat analytics, and what window do they default to?",
          "type": "multi",
          "relevant": [
            "rest-api--chat-history-feedback",
            "mcp-api--tools"
          ],
          "answer": "GET /v1.0/analytics and the pneuma_analytics tool; both default to a 30-day window (REST clamps 1–365).",
          "evidence": [
            "`days?` (default 30, clamped 1–365)",
            "Per-subject chat analytics over a window (`subjectId?`, `days?` default 30)"
          ],
          "hops": 2,
          "category": null,
          "filter": null
        },
        {
          "id": "q072",
          "text": "Why was the external processing service removed, and what now does the chunking?",
          "type": "multi",
          "relevant": [
            "partio-removal-plan--overview",
            "changelog--unreleased-latest"
          ],
          "answer": "To own processing end to end and eliminate a second service whose endpoint health and config drifted out of sync; chunking now runs in-process in Pneuma.Chunking (cl100k_base and BERT tokenizers).",
          "evidence": [
            "removes the second service whose endpoint health and config drift out of sync with Pneuma's",
            "Chunking now runs in-process in the new **`Pneuma.Chunking`** library"
          ],
          "hops": 2,
          "category": null,
          "filter": null
        },
        {
          "id": "q073",
          "text": "In what order are authorization rules evaluated, and are administrator bypasses persisted to the audit log?",
          "type": "multi",
          "relevant": [
            "claude--auth-and-frontend",
            "changelog--0-1-0-fixed-and-added"
          ],
          "answer": "Explicit deny > permit > implicit deny; IsAdmin/IsTenantAdmin bypass RBAC, and bypasses are now persisted as AuthorizationBypass audit records.",
          "evidence": [
            "Explicit deny > permit > implicit deny.",
            "they are now persisted as `AuthorizationBypass` audit records"
          ],
          "hops": 2,
          "category": null,
          "filter": null
        },
        {
          "id": "q074",
          "text": "How are SQLite writes serialized, and did that ever slow down the server-grade databases?",
          "type": "multi",
          "relevant": [
            "claude--backend-architecture",
            "changelog--0-1-0-fixed-and-added"
          ],
          "answer": "SQLite writes are serialized with a SemaphoreSlim; the single-writer semaphore once throttled PostgreSQL/MySQL/SQL Server too but is now scoped to SQLite only.",
          "evidence": [
            "SQLite writes serialized with `SemaphoreSlim`.",
            "**Server-grade databases are no longer globally serialized.**"
          ],
          "hops": 2,
          "category": null,
          "filter": null
        },
        {
          "id": "q075",
          "text": "When chat answers feel slow, where do I look in aggregate, and where can I see one specific turn's stage timings?",
          "type": "multi",
          "relevant": [
            "telemetry--grafana-dashboards",
            "rest-api--chat-history-feedback"
          ],
          "answer": "The Chat & Retrieval dashboard shows per-stage p95 latency; a single turn's performanceJson (via GET /v1.0/history/{id}) drives the History detail stage table and timing bars.",
          "evidence": [
            "answer rate by outcome, answer p95 latency, and **per-stage p95 latency**",
            "The turn's `performanceJson` drives the dashboard History detail stage table and timing bars"
          ],
          "hops": 2,
          "category": null,
          "filter": null
        },
        {
          "id": "q076",
          "text": "In the proposed approval gate, what state would a job sit in while waiting for review?",
          "type": "detail",
          "relevant": [
            "ckg-improvements--items-1-4"
          ],
          "answer": "PendingReview, entered after Categorization; approve proceeds to Hydration and reject discards the candidate.",
          "evidence": [
            "the job pauses after Categorization in a `PendingReview` state"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q077",
          "text": "Before per-subject chunking settings, what window and overlap were used for every document?",
          "type": "detail",
          "relevant": [
            "ckg-improvements--items-5-7"
          ],
          "answer": "A fixed 256-token window with 32-token overlap.",
          "evidence": [
            "hard-coded to a fixed 256-token window with 32-token overlap"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q078",
          "text": "At what viewport size does the headless browser render pages it captures?",
          "type": "detail",
          "relevant": [
            "changelog--0-1-0-added-2"
          ],
          "answer": "A real 1920×1080 desktop viewport, with bounded auto-scroll and forced-open details regions.",
          "evidence": [
            "now renders at a real 1920×1080 desktop viewport"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q079",
          "text": "How many object-storage buckets hold pipeline artifacts, and what goes in them?",
          "type": "detail",
          "relevant": [
            "changelog--0-1-0-fixed-and-added"
          ],
          "answer": "Five S3 buckets: source, atoms, chunks, embeddings, and subgraph JSON, each keyed by the originating link id.",
          "evidence": [
            "artifacts are persisted to five S3 buckets"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q080",
          "text": "What event types can the agentic chat stream emit?",
          "type": "detail",
          "relevant": [
            "rest-api--search"
          ],
          "answer": "delta, tool_call, tool_result, compacting, complete, and error.",
          "evidence": [
            "SSE event types: `delta`, `tool_call`, `tool_result`, `compacting`"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q081",
          "text": "What fields does a progress tick carry on the live evaluation-run stream?",
          "type": "detail",
          "relevant": [
            "rest-api--chat-history-feedback"
          ],
          "answer": "completed, total, pass, partial, fail, and status.",
          "evidence": [
            "{type:\"progress\",completed,total,pass,partial,fail,status}"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q082",
          "text": "What does a model endpoint's health record look like before its base URL has ever been probed?",
          "type": "detail",
          "relevant": [
            "rest-api--model-runners-prompts-settings"
          ],
          "answer": "Its timestamps are null, representing a pending state.",
          "evidence": [
            "Before a base URL's first probe, timestamps are null (a \"pending\" state)."
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q083",
          "text": "How many test cases does the Database suite contain?",
          "type": "detail",
          "relevant": [
            "test-plan--current-state"
          ],
          "answer": "30 of the 110 total cases.",
          "evidence": [
            "Security (12), Database (30), Graph (4)"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q084",
          "text": "Which HTTP library does the Python client SDK use?",
          "type": "detail",
          "relevant": [
            "readme--building-sdks-contributing"
          ],
          "answer": "httpx, with typed models.",
          "evidence": [
            "httpx client, typed models"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q085",
          "text": "Which graph algorithms does LiteGraph 9 provide beyond community detection?",
          "type": "detail",
          "relevant": [
            "ckg-improvements--litegraph-9-and-later-notes"
          ],
          "answer": "PageRank; degree, closeness, betweenness, and eigenvector centrality; connected components; clustering coefficient; and k-core.",
          "evidence": [
            "PageRank, degree/closeness/betweenness/eigenvector centrality, connected components, clustering coefficient, and k-core"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q086",
          "text": "Which chunker classes were slated to be ported into the in-house chunking library?",
          "type": "detail",
          "relevant": [
            "partio-removal-plan--workstreams"
          ],
          "answer": "FixedTokenChunker, SentenceChunker, ParagraphChunker, RegexChunker, WholeListChunker, ListEntryChunker, TableChunker, plus ChunkingHelpers.",
          "evidence": [
            "`FixedTokenChunker`, `SentenceChunker`, `ParagraphChunker`, `RegexChunker`, `WholeListChunker`"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q087",
          "text": "Which schema migration introduced classification batching?",
          "type": "detail",
          "relevant": [
            "changelog--unreleased-latest"
          ],
          "answer": "Schema migration 27, applied automatically on startup.",
          "evidence": [
            "Schema migration 27 applies"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q088",
          "text": "Which version of Grafana is the compose stack pinned to?",
          "type": "detail",
          "relevant": [
            "changelog--0-1-0-changed-fixed"
          ],
          "answer": "Grafana 13.0.2 (alongside Tempo 2.6.1, Prometheus v3.5.4, Ollama 0.31.2).",
          "evidence": [
            "Grafana `13.0.2`"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q089",
          "text": "How many API and MCP test cases does phase P2 of the test plan estimate?",
          "type": "detail",
          "relevant": [
            "test-plan--plan-and-conventions"
          ],
          "answer": "Roughly 60–80 API/MCP cases.",
          "evidence": [
            "P2: ~60–80 API/MCP cases."
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q090",
          "text": "What columns does the History detail's tool activity table draw on for each tool call?",
          "type": "detail",
          "relevant": [
            "rest-api--chat-history-feedback"
          ],
          "answer": "tool, args, output, success, and duration from the turn's toolCalls.",
          "evidence": [
            "`toolCalls` (tool, args, output, success, duration) drives the Tool activity table"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q091",
          "text": "What gets set up automatically when an administrator creates a new organization?",
          "type": "filter",
          "relevant": [
            "rest-api--tenancy-rbac-audit",
            "rest-api--graph-and-collections"
          ],
          "answer": "Its first administrator and API key, a same-id RecallDB tenant with a default collection, and an isolated LiteGraph tenant + graph whose GUIDs are recorded on the tenant.",
          "evidence": [
            "provision its first administrator + API key",
            "creates a **default collection** for it"
          ],
          "hops": 2,
          "category": null,
          "filter": {
            "requiredLabels": [
              "rest-api"
            ]
          }
        },
        {
          "id": "q092",
          "text": "How do agent calls prove who they are?",
          "type": "filter",
          "relevant": [
            "mcp-api--authentication-and-methods"
          ],
          "answer": "Like REST: a bearer session token or access_/secret_ API key through the AuthenticateRequest hook; each tool authorizes via AuthorizationService, and unauthenticated calls get 401.",
          "evidence": [
            "MCP calls authenticate exactly like REST calls."
          ],
          "hops": 1,
          "category": null,
          "filter": {
            "requiredLabels": [
              "mcp-api"
            ]
          }
        },
        {
          "id": "q093",
          "text": "What was fixed about recording administrator overrides of permission checks?",
          "type": "filter",
          "relevant": [
            "changelog--0-1-0-fixed-and-added"
          ],
          "answer": "System-admin and tenant-admin bypasses were only recorded in memory; they are now persisted as AuthorizationBypass audit records.",
          "evidence": [
            "**Administrator bypasses are now audited.**"
          ],
          "hops": 1,
          "category": null,
          "filter": {
            "requiredLabels": [
              "changelog"
            ]
          }
        },
        {
          "id": "q094",
          "text": "What username and password get me into the dashboards for metrics and traces?",
          "type": "filter",
          "relevant": [
            "telemetry--grafana-dashboards"
          ],
          "answer": "Grafana at http://localhost:3000 with admin / admin by default.",
          "evidence": [
            "Log in with the default credentials **`admin` / `admin`**"
          ],
          "hops": 1,
          "category": null,
          "filter": {
            "requiredLabels": [
              "telemetry"
            ]
          }
        },
        {
          "id": "q095",
          "text": "How can splitting of documents into pieces be tuned per subject?",
          "type": "filter",
          "relevant": [
            "ckg-improvements--items-5-7",
            "ckg-improvements--litegraph-9-and-later-notes"
          ],
          "answer": "Strategy, target size, and overlap are per-subject settings over a global default; the strategy is validated against Pneuma.Chunking's FixedTokenCount, SentenceBased, ParagraphBased.",
          "evidence": [
            "Surface the chunking configuration (strategy, target size, overlap) as **per-subject settings**",
            "(`FixedTokenCount`, `SentenceBased`, `ParagraphBased`)"
          ],
          "hops": 2,
          "category": null,
          "filter": {
            "requiredLabels": [
              "ckg-improvements"
            ]
          }
        },
        {
          "id": "q096",
          "text": "What rules apply to asynchronous methods and awaiting?",
          "type": "filter",
          "relevant": [
            "claude--code-style"
          ],
          "answer": "Every async method doing cancellable work takes a CancellationToken, and every await in library/server code uses .ConfigureAwait(false).",
          "evidence": [
            "Every async method that does cancellable work takes a `CancellationToken`."
          ],
          "hops": 1,
          "category": null,
          "filter": {
            "requiredLabels": [
              "claude"
            ]
          }
        },
        {
          "id": "q097",
          "text": "What can I do with labels and tags attached to ingested sources?",
          "type": "filter",
          "relevant": [
            "readme--features"
          ],
          "answer": "They are stamped onto every chunk and graph node, and any search or answer can be scoped with a per-request metadataFilter merged with the subject default; distinct values are discoverable over REST and MCP.",
          "evidence": [
            "Ingest links with **labels** and **tags** that are stamped onto every chunk and graph node"
          ],
          "hops": 1,
          "category": null,
          "filter": {
            "requiredLabels": [
              "readme"
            ]
          }
        },
        {
          "id": "q098",
          "text": "What agent-interface testing is missing?",
          "type": "filter",
          "relevant": [
            "test-plan--gap-analysis"
          ],
          "answer": "Per-tool RBAC negative tests, admin-only ops tools denied to non-admins, and multi-page enumeration correctness (endOfResults/recordsRemaining).",
          "evidence": [
            "**per-tool RBAC negative**",
            "**admin-only ops tools denied to a non-admin**"
          ],
          "hops": 1,
          "category": null,
          "filter": {
            "requiredLabels": [
              "test-plan"
            ]
          }
        },
        {
          "id": "q099",
          "text": "Once the external service is gone, where will model endpoint definitions live?",
          "type": "filter",
          "relevant": [
            "partio-removal-plan--current-use-and-target"
          ],
          "answer": "In IModelEndpointStore, a Pneuma-owned DB-backed registry of embedding and completion endpoints across all four databases.",
          "evidence": [
            "**`IModelEndpointStore`** — a Pneuma-owned, DB-backed registry of embedding and completion endpoints"
          ],
          "hops": 1,
          "category": null,
          "filter": {
            "requiredLabels": [
              "partio-removal-plan"
            ]
          }
        },
        {
          "id": "q100",
          "text": "Which ports does the tracing backend expose?",
          "type": "filter",
          "relevant": [
            "ports--ports-and-credentials"
          ],
          "answer": "Tempo on 3200, with OTLP ingest on 4317/4318; no auth.",
          "evidence": [
            "3200 (OTLP 4317/4318)"
          ],
          "hops": 1,
          "category": null,
          "filter": {
            "requiredLabels": [
              "ports"
            ]
          }
        },
        {
          "id": "q101",
          "text": "How many days of captured request history are kept before pruning by default?",
          "type": "negative",
          "relevant": [],
          "answer": "NOT_IN_CORPUS",
          "evidence": [],
          "hops": 0,
          "category": null,
          "filter": null
        },
        {
          "id": "q102",
          "text": "Which password hashing algorithm protects user account passwords?",
          "type": "negative",
          "relevant": [],
          "answer": "NOT_IN_CORPUS",
          "evidence": [],
          "hops": 0,
          "category": null,
          "filter": null
        },
        {
          "id": "q103",
          "text": "What is the maximum file size allowed for a document submitted for ingestion?",
          "type": "negative",
          "relevant": [],
          "answer": "NOT_IN_CORPUS",
          "evidence": [],
          "hops": 0,
          "category": null,
          "filter": null
        },
        {
          "id": "q104",
          "text": "Does Pneuma support SAML or OpenID Connect single sign-on?",
          "type": "negative",
          "relevant": [],
          "answer": "NOT_IN_CORPUS",
          "evidence": [],
          "hops": 0,
          "category": null,
          "filter": null
        },
        {
          "id": "q105",
          "text": "How long is a dashboard session token valid before it expires?",
          "type": "negative",
          "relevant": [],
          "answer": "NOT_IN_CORPUS",
          "evidence": [],
          "hops": 0,
          "category": null,
          "filter": null
        },
        {
          "id": "q106",
          "text": "Which languages are the dashboards currently translated into?",
          "type": "negative",
          "relevant": [],
          "answer": "NOT_IN_CORPUS",
          "evidence": [],
          "hops": 0,
          "category": null,
          "filter": null
        },
        {
          "id": "q107",
          "text": "Is there a Helm chart for deploying Pneuma on Kubernetes?",
          "type": "negative",
          "relevant": [],
          "answer": "NOT_IN_CORPUS",
          "evidence": [],
          "hops": 0,
          "category": null,
          "filter": null
        },
        {
          "id": "q108",
          "text": "What per-credential request quota (requests per minute) does the API enforce?",
          "type": "negative",
          "relevant": [],
          "answer": "NOT_IN_CORPUS",
          "evidence": [],
          "hops": 0,
          "category": null,
          "filter": null
        },
        {
          "id": "q109",
          "text": "How do I back up and restore the Postgres database?",
          "type": "negative",
          "relevant": [],
          "answer": "NOT_IN_CORPUS",
          "evidence": [],
          "hops": 0,
          "category": null,
          "filter": null
        },
        {
          "id": "q110",
          "text": "Can Pneuma ingest audio or video by transcribing it?",
          "type": "negative",
          "relevant": [],
          "answer": "NOT_IN_CORPUS",
          "evidence": [],
          "hops": 0,
          "category": null,
          "filter": null
        },
        {
          "id": "q111",
          "text": "Which OCR engine is used to read scanned PDFs?",
          "type": "negative",
          "relevant": [],
          "answer": "NOT_IN_CORPUS",
          "evidence": [],
          "hops": 0,
          "category": null,
          "filter": null
        },
        {
          "id": "q112",
          "text": "What GPU does Ollama need to run the default models?",
          "type": "negative",
          "relevant": [],
          "answer": "NOT_IN_CORPUS",
          "evidence": [],
          "hops": 0,
          "category": null,
          "filter": null
        },
        {
          "id": "q113",
          "text": "Can Pneuma call a webhook when an ingestion job finishes?",
          "type": "negative",
          "relevant": [],
          "answer": "NOT_IN_CORPUS",
          "evidence": [],
          "hops": 0,
          "category": null,
          "filter": null
        },
        {
          "id": "q114",
          "text": "What sampling temperature is used when generating grounded answers?",
          "type": "negative",
          "relevant": [],
          "answer": "NOT_IN_CORPUS",
          "evidence": [],
          "hops": 0,
          "category": null,
          "filter": null
        },
        {
          "id": "q115",
          "text": "How often, in seconds, does Prometheus scrape the Pneuma metrics endpoint?",
          "type": "negative",
          "relevant": [],
          "answer": "NOT_IN_CORPUS",
          "evidence": [],
          "hops": 0,
          "category": null,
          "filter": null
        },
        {
          "id": "q116",
          "text": "What is the name of the embedding model seeded on the default Ollama endpoint?",
          "type": "negative",
          "relevant": [],
          "answer": "NOT_IN_CORPUS",
          "evidence": [],
          "hops": 0,
          "category": null,
          "filter": null
        },
        {
          "id": "q117",
          "text": "How does a user reset a forgotten dashboard password?",
          "type": "negative",
          "relevant": [],
          "answer": "NOT_IN_CORPUS",
          "evidence": [],
          "hops": 0,
          "category": null,
          "filter": null
        },
        {
          "id": "q118",
          "text": "Does the login flow support multi-factor authentication?",
          "type": "negative",
          "relevant": [],
          "answer": "NOT_IN_CORPUS",
          "evidence": [],
          "hops": 0,
          "category": null,
          "filter": null
        },
        {
          "id": "q119",
          "text": "How do I configure a TLS certificate so the API serves HTTPS?",
          "type": "negative",
          "relevant": [],
          "answer": "NOT_IN_CORPUS",
          "evidence": [],
          "hops": 0,
          "category": null,
          "filter": null
        },
        {
          "id": "q120",
          "text": "What is the default value of MaxAttempts for automatic ingestion retries?",
          "type": "negative",
          "relevant": [],
          "answer": "NOT_IN_CORPUS",
          "evidence": [],
          "hops": 0,
          "category": null,
          "filter": null
        },
        {
          "id": "q121",
          "text": "What are the default concurrency cap and queue depth for an individual model endpoint?",
          "type": "confusable",
          "relevant": [
            "rest-api--model-runners-prompts-settings"
          ],
          "answer": "maxConcurrentRequests defaults to 2 (minimum 1) and maxQueueDepth defaults to 0, which rejects over-limit requests immediately with 429.",
          "evidence": [
            "(minimum 1, default 2)",
            "default 0 rejects over-limit requests immediately with 429"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q122",
          "text": "What are the defaults of the process-wide gate that admits grounded-answer and chat requests?",
          "type": "confusable",
          "relevant": [
            "rest-api--search"
          ],
          "answer": "MaxConcurrentRequests 4 and MaxQueueDepth 16 in the ModelRunner settings block; beyond that, requests get HTTP 429 before streaming starts.",
          "evidence": [
            "(`MaxConcurrentRequests`, default 4; `MaxQueueDepth`, default 16)"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q123",
          "text": "Does ingestion currently create one graph node per chunk?",
          "type": "confusable",
          "relevant": [
            "changelog--unreleased-changed"
          ],
          "answer": "No. The graph gets a Cell node per extracted semantic cell; chunks are stored only as RecallDB documents pointing back at their cell node.",
          "evidence": [
            "Cells are the graph's unit of source content; chunks live only in RecallDB."
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q124",
          "text": "Are community summaries recomputed automatically on a schedule?",
          "type": "confusable",
          "relevant": [
            "ckg-improvements--graphrag-implementation-note"
          ],
          "answer": "No. Scheduled recompute was not built; recomputation is operator-triggered via the build endpoint.",
          "evidence": [
            "scheduled community recompute (currently operator-triggered via the build endpoint)"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q125",
          "text": "What did the human review/approval gate actually ship as?",
          "type": "confusable",
          "relevant": [
            "ckg-improvements--scoring-and-table"
          ],
          "answer": "It was reduced to a per-subject PublishedForChat attribute (default on) that gates the subject's visibility in consumer chat, not a per-ingestion approval step.",
          "evidence": [
            "per-subject `PublishedForChat` attribute (default on)"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q126",
          "text": "How many automated test cases does the backend suite have right now?",
          "type": "confusable",
          "relevant": [
            "test-plan--current-state"
          ],
          "answer": "110 cases across 8 suites.",
          "evidence": [
            "110 cases across 8 suites"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q127",
          "text": "Why can't Anthropic be selected for an embedding endpoint?",
          "type": "confusable",
          "relevant": [
            "partio-removal-plan--workstreams"
          ],
          "answer": "Anthropic exposes no embeddings API (throws NotSupportedException), so it is allowed only for completion endpoints and rejected for embedding endpoints.",
          "evidence": [
            "Anthropic exposes no embeddings API (throws `NotSupportedException`)"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q128",
          "text": "When upgrading from the Partio-era build, are existing model endpoints carried over automatically?",
          "type": "confusable",
          "relevant": [
            "readme--quick-start"
          ],
          "answer": "No. You must re-create model endpoints on the admin Model Endpoints page (Ollama defaults are seeded) and re-select each subject's models.",
          "evidence": [
            "**Re-create your model endpoints** from the admin **Model Endpoints** page"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q129",
          "text": "Can the standalone search routes run vector or hybrid search now, not just full-text?",
          "type": "confusable",
          "relevant": [
            "ckg-improvements--litegraph-9-and-later-notes"
          ],
          "answer": "Yes. A mode parameter (text | vector | hybrid, default hybrid) was added to /v1.0/search and /v1.0/subjects/{id}/search.",
          "evidence": [
            "adds a `mode` parameter (`text` | `vector` | `hybrid`, default hybrid)"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        },
        {
          "id": "q130",
          "text": "What routes were added for answering thematic, whole-subject questions from community summaries?",
          "type": "confusable",
          "relevant": [
            "ckg-improvements--graphrag-implementation-note"
          ],
          "answer": "POST /v1.0/subjects/{id}/communities/build, GET /v1.0/subjects/{id}/communities, and POST /v1.0/query/global.",
          "evidence": [
            "`POST /v1.0/subjects/{id}/communities/build`, `GET /v1.0/subjects/{id}/communities`, and `POST /v1.0/query/global`"
          ],
          "hops": 1,
          "category": null,
          "filter": null
        }
      ]
    }
  ]
}
